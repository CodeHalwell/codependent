# Example Integration Pack

This directory contains a complete Codypendent Integration Pack example demonstrating:
- A signed skill manifest (`skill.toml`) declaring brokered secrets (`permissions.secrets = ["github_token"]`).
- Instruction documentation (`SKILL.md`).
- Validation lifecycle hooks (`hooks/pre-tool-audit.toml`).
