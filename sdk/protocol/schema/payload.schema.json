{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Actor": {
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Human"
              ],
              "type": "string"
            },
            "user_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "user_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "agent_id": {
              "format": "uuid",
              "type": "string"
            },
            "model": {
              "type": "string"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Agent"
              ],
              "type": "string"
            }
          },
          "required": [
            "agent_id",
            "model",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "client_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Client"
              ],
              "type": "string"
            }
          },
          "required": [
            "client_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "integration_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Integration"
              ],
              "type": "string"
            }
          },
          "required": [
            "integration_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "System"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AgentMode": {
      "description": "A mode preset: a bundle of policy and interaction defaults, not merely a prompt (Chapter 20). Modes are enforced by the policy engine — an `Explore` run proposing a write is denied regardless of what the model says.",
      "oneOf": [
        {
          "description": "Explain, answer, retrieve. Writes and commands denied.",
          "properties": {
            "type": {
              "enum": [
                "Ask"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Investigate the repository. Read-only tools; writes denied.",
          "properties": {
            "type": {
              "enum": [
                "Explore"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Produce an execution plan. May write plan artifacts only.",
          "properties": {
            "type": {
              "enum": [
                "Plan"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Implement approved work in the worktree write scope.",
          "properties": {
            "type": {
              "enum": [
                "Build"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Inspect code or a change set. Read plus comment.",
          "properties": {
            "type": {
              "enum": [
                "Review"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AnalyticsBucket": {
      "description": "A result bucket. Dimension keys correspond in order to `query.group_by`.",
      "properties": {
        "dimensions": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "metrics": {
          "$ref": "#/definitions/AnalyticsMetrics"
        }
      },
      "required": [
        "metrics"
      ],
      "type": "object"
    },
    "AnalyticsCompletion": {
      "description": "Completion outcome used both as a filter and an aggregate dimension.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "successful"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "cancelled"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "incomplete"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AnalyticsDimensionCoverage": {
      "description": "Coverage is explicit per nullable metric, making partial aggregates visible.",
      "properties": {
        "cached_tokens": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "completion_count": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "cost": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "cost_per_successful_task": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "escalation_count": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "grader_score": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "input_tokens": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "latency": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "output_tokens": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "reasoning_tokens": {
          "$ref": "#/definitions/MeasurementCoverage"
        },
        "retry_count": {
          "$ref": "#/definitions/MeasurementCoverage"
        }
      },
      "required": [
        "cached_tokens",
        "completion_count",
        "cost",
        "cost_per_successful_task",
        "escalation_count",
        "grader_score",
        "input_tokens",
        "latency",
        "output_tokens",
        "reasoning_tokens",
        "retry_count"
      ],
      "type": "object"
    },
    "AnalyticsExportFormat": {
      "description": "Supported analytics export encodings. The request must choose explicitly.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "json"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "csv"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AnalyticsExportRequest": {
      "description": "Request for a server-bounded export of an analytics query.",
      "properties": {
        "format": {
          "$ref": "#/definitions/AnalyticsExportFormat"
        },
        "max_rows": {
          "default": 0,
          "description": "Requested row ceiling. The server may impose a smaller ceiling; zero selects the server default.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "query": {
          "$ref": "#/definitions/AnalyticsQuery"
        }
      },
      "required": [
        "format",
        "query"
      ],
      "type": "object"
    },
    "AnalyticsExportResult": {
      "description": "Metadata for a completed export. Bulk JSON/CSV bytes live in the artifact.",
      "properties": {
        "artifact": {
          "$ref": "#/definitions/ArtifactRef"
        },
        "format": {
          "$ref": "#/definitions/AnalyticsExportFormat"
        },
        "generated_at": {
          "format": "date-time",
          "type": "string"
        },
        "row_count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "truncated": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "artifact",
        "format",
        "generated_at",
        "row_count"
      ],
      "type": "object"
    },
    "AnalyticsFilters": {
      "description": "Optional restrictions on observations. Empty lists do not restrict.",
      "properties": {
        "completions": {
          "items": {
            "$ref": "#/definitions/AnalyticsCompletion"
          },
          "type": "array"
        },
        "models": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "providers": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repositories": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "routes": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "task_classes": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnalyticsTimeRange"
            },
            {
              "type": "null"
            }
          ]
        },
        "workflows": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "AnalyticsGrouping": {
      "description": "Dimensions by which observations may be grouped.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "model"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "provider"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "workflow"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "task_class"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "time"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "completion"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "route"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AnalyticsMetrics": {
      "description": "Aggregate values for a grouping bucket.",
      "properties": {
        "cached_tokens": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "completion_count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cost_micros": {
          "description": "Measured USD cost in millionths of a dollar.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cost_per_successful_task_micros": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "coverage": {
          "allOf": [
            {
              "$ref": "#/definitions/AnalyticsDimensionCoverage"
            }
          ],
          "default": {
            "cached_tokens": {
              "measured": 0,
              "total": 0
            },
            "completion_count": {
              "measured": 0,
              "total": 0
            },
            "cost": {
              "measured": 0,
              "total": 0
            },
            "cost_per_successful_task": {
              "measured": 0,
              "total": 0
            },
            "escalation_count": {
              "measured": 0,
              "total": 0
            },
            "grader_score": {
              "measured": 0,
              "total": 0
            },
            "input_tokens": {
              "measured": 0,
              "total": 0
            },
            "latency": {
              "measured": 0,
              "total": 0
            },
            "output_tokens": {
              "measured": 0,
              "total": 0
            },
            "reasoning_tokens": {
              "measured": 0,
              "total": 0
            },
            "retry_count": {
              "measured": 0,
              "total": 0
            }
          }
        },
        "escalation_count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "grader_score_micros": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "input_tokens": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "latency_ms": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "output_tokens": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "reasoning_tokens": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "retry_count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AnalyticsPage": {
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/AnalyticsBucket"
          },
          "type": "array"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "AnalyticsQuery": {
      "description": "A bounded, cursor-paged aggregate query.",
      "properties": {
        "cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "filters": {
          "allOf": [
            {
              "$ref": "#/definitions/AnalyticsFilters"
            }
          ],
          "default": {}
        },
        "group_by": {
          "items": {
            "$ref": "#/definitions/AnalyticsGrouping"
          },
          "type": "array"
        },
        "limit": {
          "default": 0,
          "description": "Requested page size. The server applies its own upper bound; zero means the server default.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "AnalyticsTimeRange": {
      "description": "Inclusive start and exclusive end of an analytics query.",
      "properties": {
        "from": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ApprovalDecision": {
      "description": "The decision an approver returns for a proposed action.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Approve"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Reject"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ApprovalScope": {
      "description": "How widely an approval applies (Chapter 04 / STEP 1.6).",
      "oneOf": [
        {
          "description": "This single proposal only.",
          "properties": {
            "type": {
              "enum": [
                "Once"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Every identical proposal for the remainder of the run.",
          "properties": {
            "type": {
              "enum": [
                "Run"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A recorded pattern of similar proposals.",
          "properties": {
            "type": {
              "enum": [
                "Pattern"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Any matching proposal in this repository.",
          "properties": {
            "type": {
              "enum": [
                "Repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ArtifactRef": {
      "description": "A pointer to a stored artifact plus the metadata needed to handle it safely.\n\n`id` and `sha256` are deliberately independent: identical bytes dedup to one blob (keyed by `sha256`) but every occurrence is its own `ArtifactRef` with its own id and `sensitivity` (Chapter 14 / STEP 1.4). Classification checks always read the ref in hand, never a row looked up by hash.",
      "properties": {
        "byte_length": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "media_type": {
          "description": "IANA media type, e.g. `text/plain` or `application/json`.",
          "type": "string"
        },
        "sensitivity": {
          "$ref": "#/definitions/DataClassification"
        },
        "sha256": {
          "description": "Lowercase hex SHA-256 of the blob's bytes (the content address).",
          "type": "string"
        }
      },
      "required": [
        "byte_length",
        "id",
        "media_type",
        "sensitivity",
        "sha256"
      ],
      "type": "object"
    },
    "AutomationApprovalMode": {
      "oneOf": [
        {
          "enum": [
            "inherit",
            "always_require",
            "policy_driven",
            "unknown"
          ],
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "preapproved": {
              "properties": {
                "approval_receipt": {
                  "type": "string"
                }
              },
              "required": [
                "approval_receipt"
              ],
              "type": "object"
            }
          },
          "required": [
            "preapproved"
          ],
          "type": "object"
        }
      ]
    },
    "AutomationBinding": {
      "properties": {
        "created_at": {
          "format": "date-time",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "filters": {
          "allOf": [
            {
              "$ref": "#/definitions/TriggerFilters"
            }
          ],
          "default": {}
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "invocation": {
          "allOf": [
            {
              "$ref": "#/definitions/InvocationPolicy"
            }
          ],
          "default": {
            "approval_mode": "inherit",
            "concurrency": "allow",
            "deduplication": {
              "window_seconds": 86400
            },
            "missed_run": "skip",
            "retry": {
              "backoff_multiplier": 2,
              "initial_delay_seconds": 30,
              "max_attempts": 0
            }
          }
        },
        "name": {
          "type": "string"
        },
        "repository_id": {
          "format": "uuid",
          "type": "string"
        },
        "source": {
          "$ref": "#/definitions/TriggerSource"
        },
        "updated_at": {
          "format": "date-time",
          "type": "string"
        },
        "workflow_id": {
          "format": "uuid",
          "type": "string"
        },
        "workflow_version": {
          "type": "string"
        }
      },
      "required": [
        "created_at",
        "id",
        "name",
        "repository_id",
        "source",
        "updated_at",
        "workflow_id",
        "workflow_version"
      ],
      "type": "object"
    },
    "AutomationBindingDraft": {
      "properties": {
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "filters": {
          "allOf": [
            {
              "$ref": "#/definitions/TriggerFilters"
            }
          ],
          "default": {}
        },
        "invocation": {
          "allOf": [
            {
              "$ref": "#/definitions/InvocationPolicy"
            }
          ],
          "default": {
            "approval_mode": "inherit",
            "concurrency": "allow",
            "deduplication": {
              "window_seconds": 86400
            },
            "missed_run": "skip",
            "retry": {
              "backoff_multiplier": 2,
              "initial_delay_seconds": 30,
              "max_attempts": 0
            }
          }
        },
        "name": {
          "type": "string"
        },
        "repository_id": {
          "format": "uuid",
          "type": "string"
        },
        "source": {
          "$ref": "#/definitions/TriggerSource"
        },
        "workflow_id": {
          "format": "uuid",
          "type": "string"
        },
        "workflow_version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "repository_id",
        "source",
        "workflow_id",
        "workflow_version"
      ],
      "type": "object"
    },
    "AutomationBindingPage": {
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/AutomationBinding"
          },
          "type": "array"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "AutomationBindingPatch": {
      "description": "Sparse update. Nested policy values are replaced as a normalized unit.",
      "properties": {
        "enabled": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "filters": {
          "anyOf": [
            {
              "$ref": "#/definitions/TriggerFilters"
            },
            {
              "type": "null"
            }
          ]
        },
        "invocation": {
          "anyOf": [
            {
              "$ref": "#/definitions/InvocationPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "repository_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "anyOf": [
            {
              "$ref": "#/definitions/TriggerSource"
            },
            {
              "type": "null"
            }
          ]
        },
        "workflow_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "workflow_version": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AutomationBindingQuery": {
      "properties": {
        "cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "repository_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "workflow_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AutomationBindingRequest": {
      "description": "Normalized CRUD requests. The containing command provides idempotency.",
      "oneOf": [
        {
          "properties": {
            "binding": {
              "$ref": "#/definitions/AutomationBindingDraft"
            },
            "type": {
              "enum": [
                "create"
              ],
              "type": "string"
            }
          },
          "required": [
            "binding",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "get"
              ],
              "type": "string"
            }
          },
          "required": [
            "id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "query": {
              "$ref": "#/definitions/AutomationBindingQuery"
            },
            "type": {
              "enum": [
                "list"
              ],
              "type": "string"
            }
          },
          "required": [
            "query",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "patch": {
              "$ref": "#/definitions/AutomationBindingPatch"
            },
            "type": {
              "enum": [
                "update"
              ],
              "type": "string"
            }
          },
          "required": [
            "id",
            "patch",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "delete"
              ],
              "type": "string"
            }
          },
          "required": [
            "id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BlackboardItemDraft": {
      "description": "A client-authored blackboard artifact before it is stored (Phase B kanban — the write half `PostBlackboardItem` carries). The author is **not** here: the daemon builds it server-side from the issuing connection, exactly as the workflow executor builds an agent's author — a client never supplies its own attribution.",
      "properties": {
        "assignee": {
          "description": "Who the card is assigned to, if anyone.",
          "type": [
            "string",
            "null"
          ]
        },
        "confidence": {
          "description": "The author's confidence in `[0, 1]`, if given.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "evidence": {
          "description": "Evidence references grounding the artifact. Claim-like kinds require at least one (the store enforces it); a `task` needs none.",
          "items": true,
          "type": "array"
        },
        "kind": {
          "description": "The typed artifact kind (`task` for board cards; any store kind is legal).",
          "type": "string"
        },
        "ordinal": {
          "description": "The card's position within its column (lower sorts first). Appended to the end of the column when absent.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "payload": {
          "description": "The artifact body (opaque JSON — for a `task`, conventionally `{ \"title\": …, \"description\": … }`)."
        },
        "status": {
          "description": "The board column (`todo` / `doing` / `review` / `done`, or a validated free string). Defaults server-side to `todo` for a `task`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "payload"
      ],
      "type": "object"
    },
    "BlackboardItemView": {
      "description": "One stored blackboard artifact, projected for a client.\n\nA read-command reply carries a `Vec` of these (the run's board, kind-filtered); a subscription delivers one as each post/supersede lands. The `workflow_run_id` travels with the item so a client routes a live [`Payload::BlackboardPosted`](crate::envelope::Payload::BlackboardPosted) to the right board without consulting the enclosing frame.",
      "properties": {
        "assignee": {
          "description": "Who the card is assigned to, if anyone.",
          "type": [
            "string",
            "null"
          ]
        },
        "author": {
          "description": "Who produced it — the daemon builds this server-side from the authoring node's run context (`{role, run_id, node_id, workflow_run_id}`), never from model-supplied identity."
        },
        "board_scope": {
          "description": "The repository this item's board serves, when the item lives on a repository task board rather than a real workflow run (its `workflow_run_id` is then the synthetic [`board_scope_id`]). Additive: an older daemon sends none and every field below parses back defaulted.",
          "type": [
            "string",
            "null"
          ]
        },
        "confidence": {
          "description": "The author's self-reported confidence in `[0, 1]`, if given.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "evidence": {
          "description": "Evidence references grounding the artifact (opaque JSON). Claim-like kinds require at least one; the store enforces it.",
          "items": true,
          "type": "array"
        },
        "id": {
          "description": "The artifact's stable id (a UUIDv7 string).",
          "type": "string"
        },
        "kind": {
          "description": "The typed artifact kind (`finding`, `decision`, `hypothesis`, …), as the manifest-facing string the `BlackboardStore` records.",
          "type": "string"
        },
        "ordinal": {
          "description": "The card's position within its column (lower sorts first).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "payload": {
          "description": "The artifact body (opaque JSON — a client renders it)."
        },
        "revision": {
          "description": "The artifact's revision within its supersession chain (1 for an original).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "status": {
          "description": "The board column (`todo` / `doing` / `review` / `done`, or a validated free string), when this item is a board card.",
          "type": [
            "string",
            "null"
          ]
        },
        "superseded_by": {
          "description": "The id of the item that superseded this one, if any — a live item has `None`.",
          "type": [
            "string",
            "null"
          ]
        },
        "workflow_run_id": {
          "description": "The workflow run whose board holds it.",
          "type": "string"
        }
      },
      "required": [
        "author",
        "id",
        "kind",
        "payload",
        "revision",
        "workflow_run_id"
      ],
      "type": "object"
    },
    "BlackboardScope": {
      "description": "Which durable board a client-side blackboard write targets: a workflow run's board, or a repository's task board (the kanban surface). A wire enum with an [`Unknown`](BlackboardScope::Unknown) fallback so a scope from a newer client is rejected structurally rather than failing the frame.",
      "oneOf": [
        {
          "description": "A durable workflow run's board.",
          "properties": {
            "type": {
              "enum": [
                "WorkflowRun"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "A repository's task board — resolved server-side to the synthetic board run [`board_scope_id`] names, created on first use. `repository` is the canonical filesystem root, exactly as `StartRun.repository` carries it.",
          "properties": {
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RepositoryBoard"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BudgetCeiling": {
      "properties": {
        "cost_micros": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tokens": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tool_calls": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "wall_time_seconds": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BudgetDimension": {
      "description": "Which budget a `BudgetWarning` is about. The unit of the reported `used`/`limit` is implied by the dimension (tokens, minor currency units, seconds, or a count of calls).",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Tokens"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Cost"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "WallClock"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ToolCalls"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BundleCollisionPolicy": {
      "description": "How an importer handles a source identity that already exists locally.",
      "oneOf": [
        {
          "description": "Reject the complete import without making changes.",
          "properties": {
            "type": {
              "enum": [
                "Reject"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Allocate a fresh local identity and preserve the source in provenance.",
          "properties": {
            "type": {
              "enum": [
                "Remap"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Skip colliding records while importing independent records.",
          "properties": {
            "type": {
              "enum": [
                "Skip"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BundleEntryKind": {
      "description": "Semantic role of an archive entry.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "TranscriptEvents"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "RoutingMetadata"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Approvals"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ArtifactManifest"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Patch"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "EnvironmentDiagnostics"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BundleEntryManifest": {
      "description": "One regular-file entry in the archive.",
      "properties": {
        "byte_length": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "classification": {
          "$ref": "#/definitions/DataClassification"
        },
        "kind": {
          "$ref": "#/definitions/BundleEntryKind"
        },
        "media_type": {
          "description": "IANA media type.",
          "type": "string"
        },
        "path": {
          "description": "Normalized relative archive path. Importers still validate this value.",
          "type": "string"
        },
        "sha256": {
          "description": "Lowercase hexadecimal SHA-256 of the uncompressed entry bytes.",
          "type": "string"
        }
      },
      "required": [
        "byte_length",
        "classification",
        "kind",
        "media_type",
        "path",
        "sha256"
      ],
      "type": "object"
    },
    "BundleExportReceipt": {
      "description": "Successful export. Archive bytes remain behind an artifact reference.",
      "properties": {
        "bundle": {
          "$ref": "#/definitions/ArtifactRef"
        },
        "manifest": {
          "$ref": "#/definitions/BundleManifest"
        }
      },
      "required": [
        "bundle",
        "manifest"
      ],
      "type": "object"
    },
    "BundleExportRequest": {
      "description": "Request a deterministic bundle export.",
      "properties": {
        "inclusion": {
          "$ref": "#/definitions/BundleInclusionPolicy"
        },
        "redaction_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleRedactionPolicy"
            }
          ],
          "default": {
            "type": "Standard"
          }
        },
        "source_session_ids": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "inclusion"
      ],
      "type": "object"
    },
    "BundleIdentityKind": {
      "description": "Kind of durable identity rewritten by an import.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Session"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Run"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Artifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Approval"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ChangeSet"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BundleIdentityMapping": {
      "description": "Mapping from an opaque source identity to its newly allocated local one.",
      "properties": {
        "kind": {
          "$ref": "#/definitions/BundleIdentityKind"
        },
        "local_id": {
          "type": "string"
        },
        "provenance": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleImportProvenance"
            }
          ],
          "description": "Provenance attached to the corresponding imported durable record."
        },
        "source_id": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "local_id",
        "provenance",
        "source_id"
      ],
      "type": "object"
    },
    "BundleImportProvenance": {
      "description": "Provenance attached to every durable record created by an import.",
      "properties": {
        "bundle_sha256": {
          "description": "Lowercase hexadecimal SHA-256 of the imported archive bytes.",
          "type": "string"
        },
        "imported_at": {
          "format": "date-time",
          "type": "string"
        },
        "manifest_sha256": {
          "description": "Lowercase hexadecimal SHA-256 asserted by the verified manifest.",
          "type": "string"
        },
        "source_session_ids": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "bundle_sha256",
        "imported_at",
        "manifest_sha256"
      ],
      "type": "object"
    },
    "BundleImportReceipt": {
      "description": "Successful import result. No approvals or credentials are restored.",
      "properties": {
        "identity_mappings": {
          "default": [],
          "items": {
            "$ref": "#/definitions/BundleIdentityMapping"
          },
          "type": "array"
        },
        "imported_session_ids": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "provenance": {
          "$ref": "#/definitions/BundleImportProvenance"
        },
        "skipped_entries": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "provenance"
      ],
      "type": "object"
    },
    "BundleImportRequest": {
      "description": "Request an import from a previously uploaded bundle artifact.",
      "properties": {
        "bundle": {
          "$ref": "#/definitions/ArtifactRef"
        },
        "collision_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleCollisionPolicy"
            }
          ],
          "default": {
            "type": "Remap"
          }
        }
      },
      "required": [
        "bundle"
      ],
      "type": "object"
    },
    "BundleInclusionPolicy": {
      "description": "Exact categories the caller permits an exporter to include.\n\nEvery switch defaults to `false`; omission therefore cannot accidentally broaden an export when a newer exporter adds another category.",
      "properties": {
        "approvals": {
          "default": false,
          "type": "boolean"
        },
        "artifact_manifests": {
          "default": false,
          "type": "boolean"
        },
        "environment_diagnostics": {
          "default": false,
          "type": "boolean"
        },
        "patches": {
          "default": false,
          "type": "boolean"
        },
        "routing_metadata": {
          "default": false,
          "type": "boolean"
        },
        "transcript_events": {
          "default": false,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "BundleManifest": {
      "description": "Self-describing manifest stored in every bundle.",
      "properties": {
        "created_at": {
          "format": "date-time",
          "type": "string"
        },
        "entries": {
          "default": [],
          "items": {
            "$ref": "#/definitions/BundleEntryManifest"
          },
          "type": "array"
        },
        "format_version": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "inclusion": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleInclusionPolicy"
            }
          ],
          "default": {
            "approvals": false,
            "artifact_manifests": false,
            "environment_diagnostics": false,
            "patches": false,
            "routing_metadata": false,
            "transcript_events": false
          }
        },
        "manifest_sha256": {
          "description": "Lowercase hexadecimal SHA-256 of the canonical entry manifest.",
          "type": "string"
        },
        "redaction_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleRedactionPolicy"
            }
          ],
          "default": {
            "type": "Standard"
          }
        },
        "redaction_summary": {
          "allOf": [
            {
              "$ref": "#/definitions/BundleRedactionSummary"
            }
          ],
          "default": {
            "artifact_bodies_omitted": 0,
            "credentials_omitted": 0,
            "diagnostics_fields_omitted": 0,
            "entries_omitted": 0,
            "values_replaced": 0
          }
        },
        "source_session_ids": {
          "default": [],
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "created_at",
        "format_version",
        "manifest_sha256"
      ],
      "type": "object"
    },
    "BundleRedactionPolicy": {
      "description": "Redactions an exporter must perform before hashing archive entries.",
      "oneOf": [
        {
          "description": "Built-in safe policy: credentials are omitted and recognized secrets and local identifying values are replaced with stable placeholders.",
          "properties": {
            "type": {
              "enum": [
                "Standard"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "More restrictive support export, omitting all artifact bodies.",
          "properties": {
            "type": {
              "enum": [
                "SupportSafe"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A policy supplied by a newer peer. Receivers must not interpret this as less restrictive than `Standard`.",
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "BundleRedactionSummary": {
      "description": "Auditable aggregate of material removed or replaced during export.",
      "properties": {
        "artifact_bodies_omitted": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "credentials_omitted": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "diagnostics_fields_omitted": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "entries_omitted": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "values_replaced": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Byte": {
      "maximum": 255.0,
      "minimum": 0.0,
      "type": "integer"
    },
    "CanaryMetrics": {
      "description": "Objective canary metrics compared by the daemon. Rates are basis points (0..=10,000); latency is milliseconds.",
      "properties": {
        "baseline_error_rate_bps": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "baseline_p95_latency_ms": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "error_rate_bps": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "p95_latency_ms": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "sample_count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "baseline_error_rate_bps",
        "baseline_p95_latency_ms",
        "error_rate_bps",
        "p95_latency_ms",
        "sample_count"
      ],
      "type": "object"
    },
    "Catchup": {
      "description": "The daemon's answer to an attach: replay or snapshot.",
      "oneOf": [
        {
          "description": "The client was close enough to replay the gap event-by-event.",
          "properties": {
            "events": {
              "items": {
                "$ref": "#/definitions/SessionEvent"
              },
              "type": "array"
            },
            "from": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "through": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "Events"
              ],
              "type": "string"
            }
          },
          "required": [
            "events",
            "from",
            "through",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The client was too far behind; here is a snapshot as of `through`.",
          "properties": {
            "projection": {
              "$ref": "#/definitions/SessionProjection"
            },
            "through": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "Snapshot"
              ],
              "type": "string"
            }
          },
          "required": [
            "projection",
            "through",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "CheckpointKind": {
      "description": "How a filesystem checkpoint is materialized (Adoption 04).",
      "oneOf": [
        {
          "description": "A stash-shaped snapshot commit; restore = reset to `^1` + `stash apply`.",
          "properties": {
            "type": {
              "enum": [
                "Stash"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A plain commit (clean tree at capture time); restore = reset to it.",
          "properties": {
            "type": {
              "enum": [
                "Commit"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ClientCapabilities": {
      "description": "What a connected client can render and accept.\n\nAll flags default to `false`: a client only gets richer projections after it explicitly opts in, so an unknown or minimal client is always served the safe, plain-text baseline.",
      "properties": {
        "analytics": {
          "description": "Can query measured usage and consume bounded exports.",
          "type": "boolean"
        },
        "audio_capture": {
          "default": false,
          "description": "Can capture microphone audio.",
          "type": "boolean"
        },
        "automation": {
          "description": "Can manage trigger and schedule automation bindings.",
          "type": "boolean"
        },
        "bundles": {
          "description": "Can export and import versioned redacted bundles.",
          "type": "boolean"
        },
        "diff_view": {
          "default": false,
          "description": "Can render a side-by-side or unified diff.",
          "type": "boolean"
        },
        "editor_actions": {
          "description": "Can invoke ordinary runs from editor-native actions.",
          "type": "boolean"
        },
        "editor_mutations": {
          "default": false,
          "description": "Owns an editor buffer the daemon may mutate semantically.",
          "type": "boolean"
        },
        "image_display": {
          "default": false,
          "description": "Can display raster/vector images inline.",
          "type": "boolean"
        },
        "inbox": {
          "description": "Can render and mutate the durable owner-scoped inbox.",
          "type": "boolean"
        },
        "mouse": {
          "default": false,
          "description": "Reports mouse input (every mouse affordance also has a keyboard path).",
          "type": "boolean"
        },
        "rich_text": {
          "default": false,
          "description": "Renders styled text (bold, colour spans, links).",
          "type": "boolean"
        },
        "session_library": {
          "description": "Can browse and navigate the cursor-paged Session Library.",
          "type": "boolean"
        },
        "true_color": {
          "default": false,
          "description": "Terminal/display supports 24-bit colour.",
          "type": "boolean"
        },
        "unicode": {
          "default": false,
          "description": "Terminal/display handles Unicode beyond ASCII.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ClientRole": {
      "description": "A client's authority over a session it observes (Chapter 03). Exclusivity is attached to specific resources (leases), not to the whole session.",
      "oneOf": [
        {
          "description": "Read-only.",
          "properties": {
            "type": {
              "enum": [
                "Observer"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "May submit input and steer.",
          "properties": {
            "type": {
              "enum": [
                "Contributor"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "May control runs (cancel, pause, resume).",
          "properties": {
            "type": {
              "enum": [
                "Controller"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "May resolve approvals.",
          "properties": {
            "type": {
              "enum": [
                "Approver"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "CodeGraphEdgeAssertion": {
      "description": "Who asserted an `agent_asserted` edge, and why.\n\nThe knowledge layer already records all three of these into `code_edges.evidence_artifact` (an `EvidenceRef::AgentAssertion`) on every `graph.assert_edge` call, and the tool *requires* the rationale. Without them on the wire a client can see only that some edge is agent-asserted — which is the claim, not the audit trail, and the audit trail is the reason a model is allowed to write to the graph at all. `evidence_kind` says an assertion happened; this says who made it and on what grounds, so a reviewer can go read the turn that said it.",
      "properties": {
        "rationale": {
          "description": "The reason the agent gave, verbatim. Free text, bounded by the tool at 400 characters — a renderer must wrap or truncate it rather than assume it fits a row.",
          "type": "string"
        },
        "run_id": {
          "description": "The run that made the claim.",
          "format": "uuid",
          "type": "string"
        },
        "session_id": {
          "description": "The session whose ledger holds the asserting turn.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "rationale",
        "run_id",
        "session_id"
      ],
      "type": "object"
    },
    "CodeGraphEdgeView": {
      "description": "One edge, projected for display with both endpoints already named (a client must not have to issue a second query to render an edge).",
      "properties": {
        "asserted_by": {
          "anyOf": [
            {
              "$ref": "#/definitions/CodeGraphEdgeAssertion"
            },
            {
              "type": "null"
            }
          ],
          "description": "Present exactly when this edge's stored evidence is an agent assertion — i.e. for an `agent_asserted` edge written by `graph.assert_edge`. Absent for every mechanically-derived edge (whose evidence points at bytes, not a judgement), so the common case stays byte-identical on the wire."
        },
        "confidence": {
          "format": "float",
          "type": "number"
        },
        "evidence_kind": {
          "type": "string"
        },
        "from_id": {
          "type": "string"
        },
        "from_name": {
          "type": "string"
        },
        "relation": {
          "type": "string"
        },
        "revision": {
          "type": "string"
        },
        "to_id": {
          "type": "string"
        },
        "to_name": {
          "type": "string"
        }
      },
      "required": [
        "confidence",
        "evidence_kind",
        "from_id",
        "from_name",
        "relation",
        "revision",
        "to_id",
        "to_name"
      ],
      "type": "object"
    },
    "CodeGraphGrammar": {
      "description": "One grammar this build carries, and the extensions that select it. Sent so a client can answer \"what *would* have been folded?\" without keeping its own copy of the roster.",
      "properties": {
        "extensions": {
          "description": "Lowercase, no leading dot.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "language": {
          "type": "string"
        }
      },
      "required": [
        "extensions",
        "language"
      ],
      "type": "object"
    },
    "CodeGraphLanguageCount": {
      "description": "One language's contribution to the graph, on the scan path and the stored path alike. `language` is the stored `code_nodes.language` scalar.",
      "properties": {
        "edges": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files": {
          "description": "Distinct source files this language contributed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "language": {
          "type": "string"
        },
        "nodes": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "files",
        "language",
        "nodes"
      ],
      "type": "object"
    },
    "CodeGraphNodeView": {
      "description": "One node, projected for display.",
      "properties": {
        "id": {
          "description": "The stored `code_nodes.id`. Naming it back in a [`CodeGraphQuery::node_id`] is scoped to the same repository — see that field's documentation.",
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "language": {
          "type": "string"
        },
        "package": {
          "type": [
            "string",
            "null"
          ]
        },
        "qualified_name": {
          "type": "string"
        },
        "revision": {
          "type": "string"
        },
        "source_path": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "kind",
        "language",
        "qualified_name",
        "revision"
      ],
      "type": "object"
    },
    "CodeGraphPage": {
      "description": "One page of `graph show` results.",
      "properties": {
        "edges": {
          "items": {
            "$ref": "#/definitions/CodeGraphEdgeView"
          },
          "type": "array"
        },
        "limit": {
          "description": "The limit actually applied after the server's clamp.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "nodes": {
          "items": {
            "$ref": "#/definitions/CodeGraphNodeView"
          },
          "type": "array"
        },
        "total_edges": {
          "description": "Edges incident to the filter's nodes, likewise **before** the limit and likewise over the whole filtered node set — a client renders it as the `M` in \"showing N of M\", so a total clamped to the page, or narrowed to the nodes one page showed, is a wrong number presented as a fact.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "total_nodes": {
          "description": "Nodes matching the filter **before** the limit, so a client can say \"showing 50 of 812\" rather than implying it showed everything.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "edges",
        "limit",
        "nodes",
        "total_edges",
        "total_nodes"
      ],
      "type": "object"
    },
    "CodeGraphQuery": {
      "description": "The filter `graph show` applies. Every field narrows; absent fields do not.\n\n**Scoping is not one of these fields.** The repository is carried by the command, resolved by the daemon from a filesystem path with its own single source of truth, and applied to every branch of this query — including [`node_id`](Self::node_id). There is no way to spell \"some other checkout\" here.",
      "properties": {
        "include_edges": {
          "description": "Include the edges incident to the selected nodes — **every** node the filter selects, not only the ones this page of nodes happens to show. The two row kinds are paged independently (see [`limit`](Self::limit)), so an edge between two nodes that both fall past the node page is still returned and still counted.",
          "type": "boolean"
        },
        "include_nodes": {
          "description": "Include the nodes themselves. Both false is treated as both true — a query that selects nothing is a client bug, not a legal request. Both flags default to `false`, so this is exactly what the default `{}` query asks for: everything.",
          "type": "boolean"
        },
        "kind": {
          "description": "Exact stored node-kind scalar (`function`, `type`, …).",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "description": "Exact stored language scalar (`rust`, `python`, …).",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Maximum rows of each kind — nodes and edges get a page each, not one shared budget. 0 asks for the server default; the server clamps any request to its own ceiling.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "name": {
          "description": "Case-insensitive substring of the qualified name.",
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "description": "Exactly one node, by its stored id.\n\nThis is the direct-by-id path, and it carries the **same** repository gate the list path does. A node id belonging to another checkout is answered identically to an id that does not exist anywhere (`graph.node-not-found`), so naming an id can never confirm that it exists elsewhere. A filter that is enforced only where a list is built is not a filter; it is an enumeration oracle with extra steps.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Repo-relative path prefix (`crates/cli/`), matched against `code_nodes.source_path`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CodeGraphScanReport": {
      "description": "What one on-demand `graph build` did — the report that makes an empty graph self-explanatory.\n\nAdditive by default: a field a future daemon does not compute must be absent-or-zero in a way the client renders as \"not measured\" rather than as a fact. Today every field is measured.",
      "properties": {
        "by_language": {
          "description": "Per-language breakdown of what landed, most nodes first.",
          "items": {
            "$ref": "#/definitions/CodeGraphLanguageCount"
          },
          "type": "array"
        },
        "cap_hit": {
          "description": "The fold reached the cap, so the graph is a **truncation** of the repository rather than the repository.",
          "type": "boolean"
        },
        "edges": {
          "description": "Rows in `code_edges` for this repository after the fold.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "elapsed_ms": {
          "description": "How long the fold took, so a slow scan is visible rather than inferred.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "file_cap": {
          "description": "The scan's own per-repository file cap.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files_folded": {
          "description": "Of those, the ones actually folded into the graph. `files_supported` minus this is the count that matched a grammar and still yielded nothing: unreadable, or a parse the extractor rejected.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files_ignored": {
          "description": "Files the ignore rules excluded, or that vanished between the walk and the read.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files_supported": {
          "description": "Of those, the ones whose extension maps to a grammar — the candidates.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files_unsupported": {
          "description": "Files no grammar covers. On a repository whose graph is empty, this number and [`not_folded`](Self::not_folded) **are** the explanation.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files_walked": {
          "description": "Every file the walk visited, before any filter. The denominator.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "grammars": {
          "description": "Every grammar this build has. Sent with the report because the useful answer to \"why did nothing fold?\" is the pair \"these extensions were seen\" and \"these are the ones that would have worked\".",
          "items": {
            "$ref": "#/definitions/CodeGraphGrammar"
          },
          "type": "array"
        },
        "nodes": {
          "description": "Rows in `code_nodes` for this repository after the fold.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "not_folded": {
          "description": "The unsupported extensions, most files first. **Bounded** by the extractor, so its `files` may sum to less than [`files_unsupported`](Self::files_unsupported) — a tree with a very long tail of one-off extensions is counted in full but named in part.",
          "items": {
            "$ref": "#/definitions/CodeGraphSkippedExtension"
          },
          "type": "array"
        },
        "repository_root": {
          "description": "The checkout the daemon resolved and folded — never the directory the client happened to be standing in.",
          "type": "string"
        },
        "revision": {
          "description": "The revision every node written by this scan was stamped with.",
          "type": "string"
        }
      },
      "required": [
        "by_language",
        "cap_hit",
        "edges",
        "elapsed_ms",
        "file_cap",
        "files_folded",
        "files_ignored",
        "files_supported",
        "files_unsupported",
        "files_walked",
        "nodes",
        "repository_root",
        "revision"
      ],
      "type": "object"
    },
    "CodeGraphSkippedExtension": {
      "description": "Files an extension contributed to the walk that no grammar covers. `extension` is lowercase and carries no leading dot; a file with no extension at all is not tallied here (it has nothing to tally under) but still counts in [`CodeGraphScanReport::files_unsupported`].",
      "properties": {
        "extension": {
          "type": "string"
        },
        "files": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "extension",
        "files"
      ],
      "type": "object"
    },
    "CodeGraphStatusView": {
      "description": "What the stored graph holds for one repository right now, with no re-scan.",
      "properties": {
        "by_kind": {
          "description": "Node kinds (`function`, `type`, `file`, …), most nodes first.",
          "items": {
            "$ref": "#/definitions/CodeGraphTally"
          },
          "type": "array"
        },
        "by_language": {
          "items": {
            "$ref": "#/definitions/CodeGraphLanguageCount"
          },
          "type": "array"
        },
        "edges": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "files": {
          "description": "Distinct `source_path` values across the repository's nodes.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "head_revision": {
          "description": "The checkout's current `HEAD` (or `workdir` where Git cannot answer).",
          "type": "string"
        },
        "nodes": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "repository_root": {
          "type": "string"
        },
        "revisions": {
          "description": "The revisions the stored nodes are stamped at, most nodes first. More than one entry means the graph is a mix — usually a full scan at a commit plus incremental `<head>+workdir` folds on top.",
          "items": {
            "$ref": "#/definitions/CodeGraphTally"
          },
          "type": "array"
        },
        "stale": {
          "description": "The graph does not describe the current working tree.",
          "type": "boolean"
        },
        "stale_reason": {
          "description": "Why, in one sentence, when `stale`. Absent when the graph is current.",
          "type": [
            "string",
            "null"
          ]
        },
        "working_tree_dirty": {
          "description": "The working tree has uncommitted changes to tracked files.",
          "type": "boolean"
        }
      },
      "required": [
        "by_kind",
        "by_language",
        "edges",
        "files",
        "head_revision",
        "nodes",
        "repository_root",
        "revisions",
        "stale",
        "working_tree_dirty"
      ],
      "type": "object"
    },
    "CodeGraphTally": {
      "description": "A labelled tally — a node kind, or a revision the graph was stamped at.",
      "properties": {
        "count": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "label": {
          "type": "string"
        }
      },
      "required": [
        "count",
        "label"
      ],
      "type": "object"
    },
    "CommandBody": {
      "description": "The specific change a command requests. A wire enum: internally tagged with an [`CommandBody::Unknown`] fallback so a command from a newer client deserializes and is rejected structurally rather than crashing the peer.",
      "oneOf": [
        {
          "description": "Verify and durably install a `.cody-ui.tgz` package disabled. The archive is base64 because JSON framing has no byte-string scalar and is bounded by the ordinary 16 MiB daemon frame limit.",
          "properties": {
            "allow_unsigned": {
              "default": false,
              "type": "boolean"
            },
            "artifact_base64": {
              "type": "string"
            },
            "manifest_toml": {
              "type": "string"
            },
            "type": {
              "enum": [
                "InstallUiPlugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "artifact_base64",
            "manifest_toml",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "plugin_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "SmokeTestUiPlugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "plugin_id": {
              "type": "string"
            },
            "scope": {
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "EnableUiPlugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "plugin_id",
            "scope",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ListUiPlugins"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "allow_unsigned": {
              "default": false,
              "type": "boolean"
            },
            "artifact_base64": {
              "type": "string"
            },
            "manifest_toml": {
              "type": "string"
            },
            "plugin_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "UpdateUiPlugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "artifact_base64",
            "manifest_toml",
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "approval_receipt": {
              "type": "string"
            },
            "plugin_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ApproveUiPluginUpdate"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_receipt",
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "approval_receipt": {
              "type": "string"
            },
            "plugin_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RejectUiPluginUpdate"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_receipt",
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "plugin_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RevokeUiPlugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Incident-response operation: atomically remove publisher trust, revoke all signed Remote UI records from that publisher, and stop their workers.",
          "properties": {
            "publisher_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RemoveTrustedUiPublisher"
              ],
              "type": "string"
            }
          },
          "required": [
            "publisher_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "List sessions the daemon knows, newest-updated first (Adoption 11 S1).",
          "properties": {
            "limit": {
              "default": null,
              "description": "Hard cap on returned rows (the daemon also caps at 200).",
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "type": {
              "enum": [
                "ListSessions"
              ],
              "type": "string"
            },
            "workspace": {
              "description": "Restrict to one workspace; `None` lists all.",
              "format": "uuid",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Ranked, cursor-paged search over the Session Library.",
          "properties": {
            "query": {
              "$ref": "#/definitions/SessionSearchQuery"
            },
            "type": {
              "enum": [
                "SearchSessions"
              ],
              "type": "string"
            }
          },
          "required": [
            "query",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read one stable page of session history.",
          "properties": {
            "cursor": {
              "type": [
                "string",
                "null"
              ]
            },
            "limit": {
              "default": 0,
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ReadSessionHistory"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Rename, pin, archive, restore, delete, or export a session.",
          "properties": {
            "action": {
              "$ref": "#/definitions/SessionLifecycleAction"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "MutateSessionLifecycle"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Start an ordinary attributable run from an editor-native action.",
          "properties": {
            "action": {
              "$ref": "#/definitions/EditorNativeAction"
            },
            "context": {
              "$ref": "#/definitions/EditorActionContext"
            },
            "model": {
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunEditorAction"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "context",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read one owner-authorized page of the durable inbox.",
          "properties": {
            "query": {
              "allOf": [
                {
                  "$ref": "#/definitions/InboxListQuery"
                }
              ],
              "default": {
                "filters": {}
              }
            },
            "type": {
              "enum": [
                "ListInbox"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Acknowledge or dismiss a durable inbox entry.",
          "properties": {
            "mutation": {
              "$ref": "#/definitions/InboxMutation"
            },
            "type": {
              "enum": [
                "MutateInbox"
              ],
              "type": "string"
            }
          },
          "required": [
            "mutation",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Query measured usage and quality aggregates.",
          "properties": {
            "query": {
              "allOf": [
                {
                  "$ref": "#/definitions/AnalyticsQuery"
                }
              ],
              "default": {
                "filters": {},
                "limit": 0
              }
            },
            "type": {
              "enum": [
                "QueryAnalytics"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Export a bounded analytics query as JSON or CSV.",
          "properties": {
            "request": {
              "$ref": "#/definitions/AnalyticsExportRequest"
            },
            "type": {
              "enum": [
                "ExportAnalytics"
              ],
              "type": "string"
            }
          },
          "required": [
            "request",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Create, read, list, update, or delete a trigger/schedule binding.",
          "properties": {
            "request": {
              "$ref": "#/definitions/AutomationBindingRequest"
            },
            "type": {
              "enum": [
                "ManageAutomationBinding"
              ],
              "type": "string"
            }
          },
          "required": [
            "request",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Export a versioned, redacted session/support bundle.",
          "properties": {
            "request": {
              "$ref": "#/definitions/BundleExportRequest"
            },
            "type": {
              "enum": [
                "ExportBundle"
              ],
              "type": "string"
            }
          },
          "required": [
            "request",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Import a verified bundle without restoring credentials or authority.",
          "properties": {
            "request": {
              "$ref": "#/definitions/BundleImportRequest"
            },
            "type": {
              "enum": [
                "ImportBundle"
              ],
              "type": "string"
            }
          },
          "required": [
            "request",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Search workspace file paths with fuzzy matching (Adoption 11 M2).",
          "properties": {
            "limit": {
              "default": null,
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "query": {
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "SearchWorkspaceFiles"
              ],
              "type": "string"
            }
          },
          "required": [
            "query",
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "repository": {
              "description": "The canonical filesystem root of the repository this session operates on, so the daemon can build its code graph on open (not only on the first run). `#[serde(default)]` keeps older clients (which send none) working.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "type": "string"
            },
            "type": {
              "enum": [
                "CreateSession"
              ],
              "type": "string"
            },
            "workspace": {
              "format": "uuid",
              "type": "string"
            }
          },
          "required": [
            "title",
            "type",
            "workspace"
          ],
          "type": "object"
        },
        {
          "description": "Close an existing session without deleting its ledger or projections. The daemon accepts repeated closes as semantic no-ops.",
          "properties": {
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "CloseSession"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "last_seen_sequence": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "repository": {
              "description": "The canonical filesystem root of the repository this session operates on, so the daemon can build its code graph on open (not only on the first run). `#[serde(default)]` keeps older clients (which send none) working.",
              "type": [
                "string",
                "null"
              ]
            },
            "requested_role": {
              "$ref": "#/definitions/ClientRole"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "subscriptions": {
              "items": {
                "$ref": "#/definitions/Subscription"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "AttachSession"
              ],
              "type": "string"
            }
          },
          "required": [
            "requested_role",
            "session_id",
            "subscriptions",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "envelope": {
              "anyOf": [
                {
                  "$ref": "#/definitions/InputEnvelope"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The full multimodal input this submission normalizes (voice v1, rubric 8): a typed [`InputEnvelope`] whose blocks may reference artifacts previously stored via [`PutArtifact`](CommandBody::PutArtifact) — e.g. an [`InputBlock::Audio`](crate::input::InputBlock::Audio) carrying a recorded voice note. When the envelope carries audio without a transcript, the daemon transcribes it (through its transcription seam, gated by the [`transcription_allowed`](crate::input::transcription_allowed) classification math) and the transcript text becomes the run input; the original audio stays linked to its transcript (the original-is-never-replaced invariant). Additive (`#[serde(default)]`): an older client omits it and `text` alone drives the run, exactly as before this field existed."
            },
            "mode": {
              "$ref": "#/definitions/AgentMode"
            },
            "model": {
              "description": "The model to **pin** this continuation to (mid-conversation model switch). When the operator re-picks a model in the `/model` picker, the very next follow-up in the SAME session carries it here so the switch is instant — no restart, no new session. `Some(id)` runs this continuation on exactly that model AND makes it the session's current pin (a later follow-up that carries none inherits it via [`session_run_provenance`](crate) recovery). `None` is unchanged behavior: the continuation inherits the session's existing model from its originating `StartRun`. Mirrors [`StartRun.model`](CommandBody::StartRun::model): `#[serde(default)]` keeps an older client (which sends none) working — the daemon then resolves the model exactly as before this field existed.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "SubmitUserInput"
              ],
              "type": "string"
            }
          },
          "required": [
            "mode",
            "session_id",
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "mode": {
              "$ref": "#/definitions/AgentMode"
            },
            "model": {
              "description": "The model to **pin** this run to (STEP MP2): when the operator picks a model in the `/model` picker, the run executes on exactly that model instead of the router's/resolver's choice. A pin overrides the daemon's *quality* judgment, never its *security* constraint — a pinned hosted model for classified data is refused (fail-closed), never silently run off-device (enforced in the executor). Mirrors [`repository`](CommandBody::StartRun::repository): `#[serde(default)]` keeps an older client (which sends none) working — the daemon then resolves the model exactly as before this field existed.",
              "type": [
                "string",
                "null"
              ]
            },
            "objective": {
              "type": "string"
            },
            "repository": {
              "description": "The canonical filesystem root of the repository this run operates on. A per-user daemon can serve several checkouts over one socket, so the run — not the daemon's startup working directory — must decide which repository its context map and curated memories are attributed to (issue #6 item 1). `#[serde(default)]` keeps an older client (which sends none) working: the daemon then falls back to its own directory, exactly as before this field existed.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "StartRun"
              ],
              "type": "string"
            }
          },
          "required": [
            "mode",
            "objective",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "decision": {
              "$ref": "#/definitions/ApprovalDecision"
            },
            "scope": {
              "$ref": "#/definitions/ApprovalScope"
            },
            "type": {
              "enum": [
                "ResolveApproval"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_id",
            "decision",
            "scope",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Resolve a parked question (adoption 03). Mirrors `ResolveApproval`: session-scoped, idempotent, revision-guarded.",
          "properties": {
            "outcome": {
              "$ref": "#/definitions/QuestionOutcome"
            },
            "question_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ResolveQuestion"
              ],
              "type": "string"
            }
          },
          "required": [
            "outcome",
            "question_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "CancelRun"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "PauseRun"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ResumeRun"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "QueueSteering"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Push the IDE's live context (active file, selection, open documents, and unsaved-buffer digests) for a session (Phase 3 STEP 3.4). Latest-wins and high-frequency (debounced ≥ 300 ms client-side), so the daemon stores it as a projection outside the event ledger rather than appending an event.",
          "properties": {
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "UpdateIdeContext"
              ],
              "type": "string"
            },
            "update": {
              "$ref": "#/definitions/IdeContextUpdate"
            }
          },
          "required": [
            "session_id",
            "type",
            "update"
          ],
          "type": "object"
        },
        {
          "description": "Create a new collaborative document (Docs Studio, rubric #4 — before this command existed the Docs Studio browsed a set nothing could ever populate). Handled at the connection level like `MutateDocument` (documents live outside the session ledger): the daemon creates the document at revision 1 through its `DocumentCreator` seam — importing `initial_markdown` into typed blocks when present, else an empty block list — and replies [`DocumentCreated`](crate::envelope::Payload::DocumentCreated) carrying the new document id. An Observer is role-denied; a daemon assembled without a creator rejects it `document.transport-unavailable`.",
          "properties": {
            "initial_markdown": {
              "description": "Markdown to seed the document's blocks from (`docs new --from file.md`, the agent's `docs.create`). Imported lossily-but- reasonably at block granularity; absent creates an empty document.",
              "type": [
                "string",
                "null"
              ]
            },
            "repository": {
              "description": "The canonical filesystem root of the repository a repository-scoped document belongs to. Mirrors [`StartRun.repository`](CommandBody::StartRun::repository): `#[serde(default)]` keeps an older client working — the daemon then falls back to its own startup root.",
              "type": [
                "string",
                "null"
              ]
            },
            "scope": {
              "description": "The scope to create the document in: `\"repository\"` (the default when absent — the document lives with the checkout), `\"system\"`, or `\"organization:<id>\"` (organization docs default to suggest-only agent collaboration). An unrecognized value is rejected `document.invalid-scope`, never guessed at.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "description": "The document title (non-empty; the daemon rejects a blank one).",
              "type": "string"
            },
            "type": {
              "enum": [
                "CreateDocument"
              ],
              "type": "string"
            }
          },
          "required": [
            "title",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Run the documentation staleness check (`/update-docs` glue, Phase 4 STEP 4.6 finally wired): resolve every document's `{{ symbol:… }}` links against the code graph, persist them, diff for signature changes/disappearances, and file each finding as a Maintain-mode suggestion (never a direct edit). Handled at the connection level like `MutateDocument`; the daemon replies [`DocsCheckCompleted`](crate::envelope::Payload::DocsCheckCompleted) with the sweep's counts. An Observer is role-denied (the sweep files suggestions); a daemon assembled without a checker rejects it `document.transport-unavailable`.",
          "properties": {
            "repository": {
              "description": "The canonical filesystem root of the repository whose code graph the links resolve against. Mirrors [`StartRun.repository`](CommandBody::StartRun::repository); absent falls back to the daemon's startup root.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "description": "A session to surface the result into: when set and the sweep found anything stale, the daemon appends a `NoteAppended` to this session's ledger so the finding count reaches the active conversation. Absent, the counts ride only on the reply.",
              "format": "uuid",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "CheckDocuments"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Apply a semantic mutation to a collaborative document (Phase 4 STEP 4.3).\n\nHandled at the connection level (documents live outside the session ledger, so this never flows through the event write path): the daemon applies it onto the authoritative Loro document through its `DocumentMutator` seam — mode-gated by the document's scope (content edits become suggestions outside `Edit` mode), single-writer enforced via the edit-lease `require` pre-check — and fans the resulting `DocumentSync` out to the document's subscribers. An Observer is role-denied; a daemon assembled without a mutator rejects it `document.transport-unavailable`.",
          "properties": {
            "document_id": {
              "format": "uuid",
              "type": "string"
            },
            "mutation": {
              "$ref": "#/definitions/DocumentMutation"
            },
            "type": {
              "enum": [
                "MutateDocument"
              ],
              "type": "string"
            }
          },
          "required": [
            "document_id",
            "mutation",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Acquire (or renew) an edit lease over a document block-range before editing it (Phase 4 STEP 4.3 client transport). One writer per block-range: a whole-document lease (`block_id = None`) covers structural edits and conflicts with any block lease. The daemon replies [`DocumentLeaseGranted`](crate::envelope::Payload::DocumentLeaseGranted) with the minted lease id + expiry, or `CommandRejected` `document.range-leased` when a different writer holds an overlapping range. Like `MutateDocument` this is intercepted at the connection level (documents live outside the session ledger) rather than flowing through the event write path.",
          "properties": {
            "lease": {
              "$ref": "#/definitions/DocumentEditLease"
            },
            "ttl_seconds": {
              "description": "How long the lease is valid, in seconds; the daemon applies a default when absent. A re-acquire by the same holder renews the expiry in place.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "type": {
              "enum": [
                "AcquireDocumentLease"
              ],
              "type": "string"
            }
          },
          "required": [
            "lease",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Release a previously acquired document lease by its id (Phase 4 STEP 4.3). Idempotent — releasing an already-released or unknown lease is accepted as a no-op — so a client that loses the acknowledgement can retry safely.",
          "properties": {
            "lease_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ReleaseDocumentLease"
              ],
              "type": "string"
            }
          },
          "required": [
            "lease_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Publish a document's current revision to a Git target (Phase 4 STEP 4.4, closing the deferred \"executing a `PublishPlan`\" roadmap item).\n\nHandled at the connection level like `MutateDocument`/`StartWorkflow` (documents live outside the session ledger): the daemon computes the deterministic publish plan, then durably parks its approval — carrying the plan's target, changed files, and resulting Git action, shown verbatim on the approval card before any write — through the assembly's `DocumentPublisher` seam, and replies [`DocumentPublishRequested`](crate::envelope::Payload::DocumentPublishRequested) with the parked plan. Nothing is written until a human resolves the approval through the ordinary `ResolveApproval` command; a rejection executes nothing. Requires the `Controller` role; a daemon assembled without a publisher rejects it `document.transport-unavailable`.",
          "properties": {
            "document_id": {
              "format": "uuid",
              "type": "string"
            },
            "target": {
              "$ref": "#/definitions/PublishTarget"
            },
            "type": {
              "enum": [
                "PublishDocument"
              ],
              "type": "string"
            }
          },
          "required": [
            "document_id",
            "target",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Start a durable workflow run from a compiled manifest (Phase 5 STEP 5.2).\n\nCarries the workflow **manifest YAML** (its content, not a path — the daemon never reads an arbitrary client-named file) and the typed `inputs` the manifest declares. Handled at the connection level like `MutateDocument` (a workflow run lives in its own durable store outside the session ledger): the daemon compiles the manifest, creates the run through its `WorkflowStarter` seam, and replies [`WorkflowRunStarted`](crate::envelope::Payload::WorkflowRunStarted) with the new run id — or `CommandRejected` when the manifest does not compile. A daemon assembled without a starter rejects it `workflow.transport-unavailable`. (Driving the created run is a later step; this command only makes runs durably creatable.)",
          "properties": {
            "inputs": {
              "description": "The typed inputs the manifest declares, as JSON. Defaults to null when omitted (a workflow with no required inputs)."
            },
            "manifest": {
              "description": "The workflow manifest YAML (the content of a `workflow.yaml`). Empty when [`workflow_id`](CommandBody::StartWorkflow::workflow_id) names a workflow the daemon resolves from its own sources instead.",
              "type": "string"
            },
            "repository": {
              "description": "The canonical filesystem root of the repository this workflow's agent nodes operate on. A per-user daemon can serve several checkouts over one socket, so the run — not the daemon's startup working directory — must decide which repository its agent nodes' isolated worktrees are carved from (Phase 5 T5, fixing P5-D1). Mirrors [`StartRun.repository`](CommandBody::StartRun): `#[serde(default)]` keeps an older client (which sends none) working — the daemon then falls back to its own startup repository root, never a wandering `current_dir()` at node-execution time.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "StartWorkflow"
              ],
              "type": "string"
            },
            "workflow_id": {
              "description": "A named workflow to resolve from the daemon's sources (embedded built-ins, the user config directory, and the run repository's `.codypendent/workflows`) rather than shipping the manifest inline — the path `/fix-ci` takes (`repair-github-check`). Additive (`#[serde(default)]`): an older client omits it and ships `manifest`. When set, `manifest` is ignored and the daemon enforces the workflow registry's version-stability + shadowing rules at resolution.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "manifest",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Pause a running durable workflow run (Phase 5 STEP 5.2 lifecycle command).\n\nLike `StartWorkflow`, handled at the connection level (a workflow run lives in its own durable store outside the session ledger): the daemon flips the run to `paused` through its `WorkflowLifecycle` seam so a live driver stops launching further nodes (cooperative pause — the in-flight wave finishes), and the run waits for a `ResumeWorkflow`. Controlling a run is a [`Controller`](crate::handshake::ClientRole::Controller) capability, so a lesser role is denied; a terminal run is rejected `workflow.illegal-transition`; a daemon without workflow transport rejects it `workflow.transport-unavailable`.",
          "properties": {
            "type": {
              "enum": [
                "PauseWorkflow"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Resume a paused durable workflow run (Phase 5 STEP 5.2). The daemon validates the run is paused and drives it onward from its ready frontier in the background, replying as soon as the resume is accepted. Only a paused run may be resumed (else `workflow.illegal-transition`); role/transport gating matches `PauseWorkflow`.",
          "properties": {
            "type": {
              "enum": [
                "ResumeWorkflow"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Re-drive a durable workflow run from a chosen node (Phase 5 STEP 5.2 retry-from-node). The daemon resets that node and everything transitively downstream of it to `pending`, sets the run `running`, and drives in the background. An unknown `node_id` (or a graph that changed under the run) is rejected; role/transport gating matches `PauseWorkflow`.",
          "properties": {
            "node_id": {
              "description": "The node id to re-drive from (its transitive dependents reset with it).",
              "type": "string"
            },
            "type": {
              "enum": [
                "RetryWorkflowNode"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "node_id",
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Cancel a durable workflow run (Phase 5 STEP 5.2 / T9 — the missing control: pause/resume/retry exist, cancel did not). Like `PauseWorkflow`, handled at the connection level and gated to the [`Controller`](crate::handshake::ClientRole::Controller) role. A cooperative drain (mirroring pause): the driver stops launching further nodes, any in-flight node's agent run is interrupted through the same cancellation machinery `CancelRun` uses, every still-`Pending` node becomes `Skipped`, and the run lands `Cancelled` — a **terminal** state (no resume from `Cancelled`; a later resume/pause is rejected `workflow.illegal-transition`). Idempotent on an already-cancelled run; a daemon without workflow transport rejects it `workflow.transport-unavailable`.",
          "properties": {
            "type": {
              "enum": [
                "CancelWorkflow"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Read a durable workflow run's observability snapshot (Phase 5 STEP 5.2 / T9): the run's current phase plus every node's full current view (state, attempt, measured cost, failure/block reason, budget warnings), in topological order. Like `ReadBlackboard`, intercepted at the connection level (a workflow run lives in its own durable store outside the session ledger) and served through the assembly's `WorkflowReader` seam; the daemon replies [`WorkflowRunSnapshot`](crate::envelope::Payload::WorkflowRunSnapshot). This is the catch-up baseline a client folds a `Subscription::Workflow` live stream on top of; reconstructed from the store, so a late subscriber after a restart still gets a truthful baseline. A **read** — any attached client (an Observer included) may issue it. An unknown run is rejected `workflow.run-not-found`; a daemon without workflow transport rejects it `workflow.transport-unavailable`.",
          "properties": {
            "type": {
              "enum": [
                "ReadWorkflowRun"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Draft a candidate for the evaluation-gated promotion pipeline (Phase 7 STEP 7.5 — nothing promotes itself, ADR-010).\n\nHandled at the connection level like `StartWorkflow` (a promotion candidate lives in its own durable store outside the session ledger): the daemon creates a draft through its `PromotionGateway` seam and replies [`Payload::PromotionProposed`](crate::envelope::Payload::PromotionProposed) with the new candidate id — or `CommandRejected` when a synthesized candidate needs permission review, or the daemon has no promotion transport (`promotion.transport-unavailable`). `kind` is the wire name of an `ArtifactKind` (e.g. `\"skill\"`, `\"router\"`); an unrecognized kind is rejected rather than guessed at.",
          "properties": {
            "kind": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "requires_permission_review": {
              "default": false,
              "type": "boolean"
            },
            "type": {
              "enum": [
                "ProposePromotion"
              ],
              "type": "string"
            },
            "version": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "name",
            "type",
            "version"
          ],
          "type": "object"
        },
        {
          "description": "Advance a candidate through the offline-regression / shadow / canary legs of the pipeline (Phase 7 STEP 7.5). `action` names exactly which transition to attempt; an illegal transition (wrong stage, or an unobserved canary trying to finish) is rejected verbatim as the underlying state-machine error, never silently coerced into success. Same connection-level handling and role gating as `ProposePromotion`.",
          "properties": {
            "action": {
              "$ref": "#/definitions/PromotionAction"
            },
            "candidate_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "AdvancePromotion"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "candidate_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "**Approve and promote a candidate.** The human-approval gate (ADR-010, exit criterion 2): the daemon authenticates the acting party as `Actor::Human` from the connection's role — over this local-first socket, a `Controller`-role connection **is** the human operator (the same mapping `ResolveApproval` already uses for `resolved_by`) — and only a `Controller` may issue this command; every other role, and necessarily every non-human actor, is refused structurally before the promotion seam is ever invoked. No field on the wire lets a caller *supply* an actor — that would defeat the whole point of ADR-010.",
          "properties": {
            "candidate_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ApprovePromotion"
              ],
              "type": "string"
            }
          },
          "required": [
            "candidate_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Manually roll back a promoted candidate to its predecessor version (Phase 7 STEP 7.5, exit criterion 4: reversible). Requires the `Controller` role like `ApprovePromotion`, and — unlike approval — the engine itself does not restrict rollback to a human actor (stopping a bad change needs no human, only promoting a good one does); the daemon still attributes the connection's mapped `Actor::Human` so a manual rollback is never confused with the system-attributed auto-rollback a canary regression produces on its own.",
          "properties": {
            "candidate_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RollbackPromotion"
              ],
              "type": "string"
            }
          },
          "required": [
            "candidate_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read a durable workflow run's blackboard (Phase 5 STEP 5.3): the typed artifacts its agents posted, optionally filtered by `kind`. Like `StartWorkflow`, intercepted at the connection level (a workflow run's board lives in its own durable store outside the session ledger): the daemon reads it through its `BlackboardReader` seam and replies [`BlackboardItems`](crate::envelope::Payload::BlackboardItems) with the matching [`BlackboardItemView`](crate::blackboard::BlackboardItemView)s. This is a **read** — any attached client, an Observer included, may issue it (there is no client-facing *post* command; only the workflow executor writes the board). A daemon assembled without a reader rejects it `workflow.transport-unavailable`.",
          "properties": {
            "board_repository": {
              "description": "Read a **repository task board** instead of a workflow run's board (Phase B kanban). When set, `workflow_run_id` is ignored: the daemon resolves the board to the synthetic run [`board_scope_id`](crate::blackboard::board_scope_id) names (an empty board for a repository never written to — a read creates nothing). Additive (`#[serde(default)]`): an older client omits it and reads a run board exactly as before.",
              "type": [
                "string",
                "null"
              ]
            },
            "include_superseded": {
              "description": "Include superseded revisions too; the default (`false`) returns only the live board (the \"live-only\" view the TUI shows).",
              "type": "boolean"
            },
            "kind": {
              "description": "A blackboard artifact kind to filter by (`finding`, `decision`, …), or all kinds when absent.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "ReadBlackboard"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Post a blackboard artifact from a **client** (Phase B kanban — the write path the board and NL backlog need; the review's \"deliberately no client post command\" stance is revised here for board use). Handled at the connection level like `ReadBlackboard` (the board lives outside the session ledger) through the assembly's `BlackboardWriter` seam, and gated to the [`Controller`](crate::handshake::ClientRole::Controller) role — the local human operator; an agent still writes only through its `blackboard.*`/`task.*` tools, and an Observer stays read-only. The daemon builds the item's author from the issuing connection (never from a client-supplied identity) and replies [`BlackboardItemApplied`](crate::envelope::Payload::BlackboardItemApplied) with the stored item. A daemon without workflow transport rejects it `workflow.transport-unavailable`.",
          "properties": {
            "item": {
              "allOf": [
                {
                  "$ref": "#/definitions/BlackboardItemDraft"
                }
              ],
              "description": "The artifact to store (kind, payload, evidence, board fields)."
            },
            "scope": {
              "allOf": [
                {
                  "$ref": "#/definitions/BlackboardScope"
                }
              ],
              "description": "The board to post onto: a workflow run's, or a repository's task board (created on first write)."
            },
            "type": {
              "enum": [
                "PostBlackboardItem"
              ],
              "type": "string"
            }
          },
          "required": [
            "item",
            "scope",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Update (supersede) a blackboard item from a client (Phase B kanban): a status/column move, a re-assignment, a re-order, or a payload edit. The same supersession discipline as an agent's correction — the store posts the replacement at the next revision and stamps the old row, never editing in place — so board history is preserved. Fields left `None` carry the old item's values forward. Role-gated and routed exactly like [`PostBlackboardItem`](CommandBody::PostBlackboardItem); replies [`BlackboardItemApplied`](crate::envelope::Payload::BlackboardItemApplied) with the replacement item.",
          "properties": {
            "assignee": {
              "description": "The new assignee, when re-assigning.",
              "type": [
                "string",
                "null"
              ]
            },
            "item_id": {
              "description": "The live item to supersede. An already-superseded item is refused (`blackboard.already-superseded`), so concurrent moves never fork.",
              "type": "string"
            },
            "ordinal": {
              "description": "The new within-column position, when re-ordering. When only `status` changes, the daemon appends to the end of the target column.",
              "format": "int64",
              "type": [
                "integer",
                "null"
              ]
            },
            "payload": {
              "description": "A replacement payload, when editing the card body."
            },
            "scope": {
              "allOf": [
                {
                  "$ref": "#/definitions/BlackboardScope"
                }
              ],
              "description": "The board holding the item."
            },
            "status": {
              "description": "The new column, when moving.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "UpdateBlackboardItem"
              ],
              "type": "string"
            }
          },
          "required": [
            "item_id",
            "scope",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read one page of a session's durable event history (fixing the >500-event catch-up gap: a `Catchup::Snapshot` carries no transcript, and no paged read existed). A **read** — any attached client, an Observer included, may issue it. The daemon replies [`SessionEventsPage`](crate::envelope::Payload::SessionEventsPage) with events `after_sequence < sequence <= after_sequence + limit` in ascending order; the client pages forward by passing the reply's `through` back as the next `after_sequence`. An unknown session is rejected `protocol.session-not-found`.",
          "properties": {
            "after_sequence": {
              "description": "Return events strictly **after** this sequence (0 = from the start).",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "limit": {
              "description": "Maximum events in the page. 0 (or absent) asks for the server default; the server clamps any request to its own page ceiling.",
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ReadSessionEvents"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Upload client-captured bytes into the daemon's content-addressed artifact store (voice v1, rubric 8): the client→daemon half of the multimodal input path. `bytes_base64` is base64 because JSON framing has no byte-string scalar (the `InstallUiPlugin` precedent) and is bounded by the ordinary 16 MiB daemon frame limit — comfortably enough for ~1 minute of 16 kHz mono WAV (~2 MB). `sensitivity` classifies the stored bytes (captured media should default to [`DEFAULT_MEDIA_CLASSIFICATION`](crate::input::DEFAULT_MEDIA_CLASSIFICATION), i.e. `Confidential`, so audio never leaves the device by accident); classification checks downstream always read the ref returned in [`ArtifactStored`](crate::envelope::Payload::ArtifactStored). Handled at the connection level (artifacts live outside the session ledger) and gated to the [`Controller`](crate::handshake::ClientRole::Controller) role — an upload is operator-supplied input, not an observer surface. Read one curated memory (Chapter 06's right to *inspect* what the fabric remembers). Handled at the connection level like `ReadBlackboard` — the memory store lives outside the session ledger — through the assembly's memory seam. A **read**: any handshaken client may issue it.\n\n`repository` names the checkout whose memories are in view; the daemon derives the repository identity from it with its own single source of truth (never from a client-supplied id) and answers only from the scopes that identity can see. A memory outside those scopes is refused **identically** to one that does not exist (`memory.not-found`), so this command is not an enumeration oracle for another checkout's memories.",
          "properties": {
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "InspectMemory"
              ],
              "type": "string"
            }
          },
          "required": [
            "id",
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Correct a memory (Chapter 06's right to *edit*). The store never edits in place: the correction is stored as a new record that `supersedes` the one it replaces, so the history of what was believed, and when, survives. Reserved to the [`Controller`](crate::handshake::ClientRole::Controller) role — an Observer may read a memory, never rewrite one. Refused `memory.not-found` for an absent, already-superseded, or out-of-scope id, all three identically. Replies [`Memory`](crate::envelope::Payload::Memory) with the NEW record.",
          "properties": {
            "confidence": {
              "format": "float",
              "type": "number"
            },
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "statement": {
              "type": "string"
            },
            "structured_value": true,
            "type": {
              "enum": [
                "CorrectMemory"
              ],
              "type": "string"
            }
          },
          "required": [
            "confidence",
            "id",
            "repository",
            "statement",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Forget one memory (Chapter 06's right to *delete* — the one operation that really removes rows rather than superseding them). `Controller`-only and refused identically for absent and out-of-scope, like [`CorrectMemory`](CommandBody::CorrectMemory). Replies [`MemoryForgotten`](crate::envelope::Payload::MemoryForgotten) with a content-free audit of what was removed.",
          "properties": {
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ForgetMemory"
              ],
              "type": "string"
            }
          },
          "required": [
            "id",
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Forget every memory in one of the caller's visible scopes — \"forget what you know about this repository\". `Controller`-only. The target is named by TIER, never by a scope key, so a bulk delete can only ever be aimed at a scope the caller can already see (see [`MemoryScopeTier`](crate::memory::MemoryScopeTier)).",
          "properties": {
            "repository": {
              "type": "string"
            },
            "tier": {
              "$ref": "#/definitions/MemoryScopeTier"
            },
            "type": {
              "enum": [
                "ForgetMemoryScope"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "tier",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Fetch the content behind one of a memory's evidence refs — Chapter 06's \"every retrieved memory opens its source\", actually fetched instead of merely named. `evidence_index` is a position in [`MemoryView::evidence`](crate::memory::MemoryView::evidence). A **read**, gated by exactly the same scope check as `InspectMemory`: the memory is re-fetched under the caller's scopes before its evidence is opened, so a client cannot reach an artifact by naming a memory it may not see.",
          "properties": {
            "evidence_index": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "OpenMemoryEvidence"
              ],
              "type": "string"
            }
          },
          "required": [
            "evidence_index",
            "id",
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Submit the eval evidence a promotion's regression gate consumes (the socket replacement for a client writing `eval_suite_reports` into the daemon's database itself — see [`PromotionAction::RunRegression`]). `Controller`-only, like every other promotion verb: an automated CI-triggered `eval run --candidate-id` binds the same role, so this does not need `Actor::Human` the way `ApprovePromotion` does.\n\nThe daemon re-derives the candidate's artifact kind/name/version from `promotion_candidates` and refuses a `report_json` that does not parse as a suite report or carries no cases at all — the verdict is computed by the daemon from the submitted cases, never taken as a caller-supplied pass/fail.",
          "properties": {
            "candidate_id": {
              "type": "string"
            },
            "report_json": {
              "description": "A serialized `codypendent_eval::SuiteReport`. Opaque here: protocol must not depend on `codypendent-eval`.",
              "type": "string"
            },
            "routing_policy": {
              "description": "The routing policy the suite ran under, or `daemon-default`.",
              "type": "string"
            },
            "suite": {
              "description": "The eval suite that ran. The regression gate consumes `core`.",
              "type": "string"
            },
            "type": {
              "enum": [
                "SubmitEvalEvidence"
              ],
              "type": "string"
            }
          },
          "required": [
            "candidate_id",
            "report_json",
            "routing_policy",
            "suite",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "bytes_base64": {
              "description": "The raw bytes, base64-encoded (standard alphabet, with padding).",
              "type": "string"
            },
            "media_type": {
              "description": "IANA media type of the bytes, e.g. `audio/wav`.",
              "type": "string"
            },
            "sensitivity": {
              "allOf": [
                {
                  "$ref": "#/definitions/DataClassification"
                }
              ],
              "description": "The stored occurrence's data classification."
            },
            "type": {
              "enum": [
                "PutArtifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "bytes_base64",
            "media_type",
            "sensitivity",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read one bounded range from an artifact after daemon-side ownership and integrity checks. The expected digest binds every chunk request to the reference originally observed by the client.",
          "properties": {
            "artifact_id": {
              "format": "uuid",
              "type": "string"
            },
            "expected_sha256": {
              "type": "string"
            },
            "limit": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "offset": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "ReadArtifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "artifact_id",
            "expected_sha256",
            "limit",
            "offset",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Fold the repository's code graph **now**, on demand, and report what the fold saw (`codypendent graph build`).\n\nBefore this command the graph was built only as a side effect of opening a session or starting a run; there was no way to ask for it, and no way to find out why it was empty. `index rebuild` rebuilds the *retrieval* indexes and explicitly does not touch the graph, which read to users as though it did.\n\nHandled at the connection level like `ReadBlackboard` — the code graph lives outside the session ledger — through the assembly's code-graph seam. Gated to the [`Controller`](crate::handshake::ClientRole::Controller) role: a build clears and rewrites the repository's whole graph, which is a write, so an Observer may read the graph but never rebuild it.\n\n`repository` is a **path**: the daemon resolves it to the checkout with its own single source of truth and derives the repository identity from that, exactly as [`InspectMemory`](CommandBody::InspectMemory) does. A client cannot name a repository identity directly. The fold is unconditional. There is no \"skip if already current\" flag: a command a user reaches for because the graph looks wrong must not sometimes decline to do the thing its name promises.",
          "properties": {
            "repository": {
              "description": "The directory to build from; resolved to its enclosing checkout.",
              "type": "string"
            },
            "type": {
              "enum": [
                "BuildCodeGraph"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Describe the stored code graph for a repository, with no re-scan (`codypendent graph status`): counts, per-language and per-kind breakdowns, the revisions the graph is stamped at, and whether it is stale relative to the working tree.\n\nA **read** — any handshaken client, an Observer included, may issue it. `repository` is resolved exactly as [`BuildCodeGraph`](CommandBody::BuildCodeGraph)'s.",
          "properties": {
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ReadCodeGraphStatus"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "List the graph's nodes and edges, filtered (`codypendent graph show`), so the graph is inspectable from a terminal rather than only through the TUI overlay.\n\nA **read**, like [`ReadCodeGraphStatus`](CommandBody::ReadCodeGraphStatus). The [`query`](CommandBody::ReadCodeGraph::query) narrows; it never widens: the repository gate is applied to every branch of it, including [`CodeGraphQuery::node_id`](crate::codegraph::CodeGraphQuery::node_id), and an id outside this repository is refused identically to one that does not exist.",
          "properties": {
            "query": {
              "allOf": [
                {
                  "$ref": "#/definitions/CodeGraphQuery"
                }
              ],
              "default": {}
            },
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ReadCodeGraph"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Restore a run's operating worktree to a recorded filesystem checkpoint (Adoption 04). Controller-only and **approval-gated**: the daemon parks a `ProposedAction::RestoreCheckpoint` approval and touches nothing until a human approves it; the restore itself is transactional (the current state is captured behind a private ref first and re-applied on any failure). Refused `checkpoint.run-active` while the run is live, `checkpoint.not-found` for an unknown id, and `checkpoint.worktree-missing` when the recorded worktree no longer exists on disk.",
          "properties": {
            "checkpoint": {
              "format": "uuid",
              "type": "string"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RestoreCheckpoint"
              ],
              "type": "string"
            }
          },
          "required": [
            "checkpoint",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Fork a session at a recorded run-launch checkpoint (Phase 5 STEP 5.6, `ForkSession{checkpoint}` from Chapter 03; Adoption 05). The daemon copies the session's ledger up to (excluding) the checkpointed run — remapping run ids into a fresh id space — into a NEW session that records its fork origin, and replies [`SessionForked`](crate::envelope::Payload::SessionForked). The source session is never modified. Runs launched in the fork carve their worktrees from the checkpointed filesystem state, so the two branches are isolated while sharing all immutable pre-fork artifacts. Controller-only. `checkpoint` must be an ordinal-1 (run-launch) checkpoint of a run in `session_id`; a mid-run steering checkpoint is rejected `fork.mid-run-checkpoint`, an absent or foreign checkpoint `checkpoint.not-found` (identically — no oracle).",
          "properties": {
            "checkpoint": {
              "format": "uuid",
              "type": "string"
            },
            "name": {
              "description": "The fork's title; absent derives `\"<source title> (fork)\"` with an opencode-style `#N` auto-increment on collision.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ForkSession"
              ],
              "type": "string"
            }
          },
          "required": [
            "checkpoint",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Queue a prompt on the session's server-side pending queue (Adoption 06). `delivery: Steer` targets the live run's next safe point; `Queue` waits for the session to go idle. Re-queuing text that is already queued updates the existing entry instead of duplicating it. Controller-only; blank text rejected `prompt-queue.empty`.",
          "properties": {
            "delivery": {
              "$ref": "#/definitions/PromptDelivery"
            },
            "mode": {
              "$ref": "#/definitions/AgentMode"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "QueuePrompt"
              ],
              "type": "string"
            }
          },
          "required": [
            "delivery",
            "mode",
            "session_id",
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Edit a queued prompt in place (Tab-edit in the queue UI). Absent fields keep their values; an emptied `text` is rejected `prompt-queue.empty`; an unknown id `prompt-queue.not-found`.",
          "properties": {
            "delivery": {
              "anyOf": [
                {
                  "$ref": "#/definitions/PromptDelivery"
                },
                {
                  "type": "null"
                }
              ]
            },
            "prompt_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "UpdateQueuedPrompt"
              ],
              "type": "string"
            }
          },
          "required": [
            "prompt_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Promote a queued prompt to steer: delivery becomes `Steer` and the entry moves to the front (Enter on a selected queue row).",
          "properties": {
            "prompt_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "PromoteQueuedPrompt"
              ],
              "type": "string"
            }
          },
          "required": [
            "prompt_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Remove a queued prompt without running it.",
          "properties": {
            "prompt_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "DeleteQueuedPrompt"
              ],
              "type": "string"
            }
          },
          "required": [
            "prompt_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Run a user-initiated shell command as a transcript-recorded turn (Spec 20 Action 18).",
          "properties": {
            "command": {
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunUserShell"
              ],
              "type": "string"
            }
          },
          "required": [
            "command",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Quick-add a curated memory directly from the composer (Spec 20 Action 20). Gated by the curator's secret and dedup filters; emitted to the session ledger as a `NoteAppended` event.",
          "properties": {
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "RememberMemory"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ConcurrencyPolicy": {
      "enum": [
        "allow",
        "skip",
        "queue",
        "replace",
        "unknown"
      ],
      "type": "string"
    },
    "DataClassification": {
      "description": "How sensitive an artifact's contents are.\n\nOrdered least to most restrictive; higher classifications gate model routing, export, and display. A wire enum, so it is internally tagged and carries an [`DataClassification::Unknown`] fallback for forward compatibility.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Public"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Internal"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Confidential"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Secret"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A classification a newer peer defined that this build does not know. Treated as at least as restrictive as `Secret` by receivers.",
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "DeduplicationPolicy": {
      "properties": {
        "identity_fields": {
          "description": "Names of normalized event fields which form the identity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "window_seconds": {
          "default": 86400,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Diagnostic": {
      "description": "One editor diagnostic, forwarded from the IDE for context.",
      "properties": {
        "message": {
          "type": "string"
        },
        "path": {
          "type": "string"
        },
        "range": {
          "$ref": "#/definitions/Range"
        },
        "severity": {
          "$ref": "#/definitions/DiagnosticSeverity"
        },
        "source": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "message",
        "path",
        "range",
        "severity"
      ],
      "type": "object"
    },
    "DiagnosticSeverity": {
      "description": "Severity of an editor diagnostic, mirroring the common LSP levels.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Error"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Warning"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Information"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Hint"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "DirtyBufferDigest": {
      "description": "A content digest for one unsaved (\"dirty\") editor buffer. The filesystem is not always the user's current truth; the IDE sends digests so the daemon can detect divergence and request the full contents only when required and authorized (Chapter 10, \"Unsaved buffers\").",
      "properties": {
        "byte_length": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "path": {
          "type": "string"
        },
        "sha256": {
          "description": "Lowercase hex SHA-256 of the buffer's current bytes.",
          "type": "string"
        }
      },
      "required": [
        "byte_length",
        "path",
        "sha256"
      ],
      "type": "object"
    },
    "DocumentEditLease": {
      "description": "A request to lease a block range for exclusive writing (Chapter 03 / STEP 4.3). One writer per block-range; readers are unlimited. Reuses the Phase-1 lease machinery in the daemon; this is only the wire request shape.",
      "properties": {
        "block_id": {
          "description": "The block the writer intends to edit; `None` leases the whole document structure (block insert/delete/reorder).",
          "type": [
            "string",
            "null"
          ]
        },
        "document_id": {
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "document_id"
      ],
      "type": "object"
    },
    "DocumentLeaseGrant": {
      "description": "A granted document lease — the daemon's reply to an accepted `AcquireDocumentLease` (STEP 4.3 client transport). The client holds the `lease_id` as the capability to renew (a re-acquire of the same range) and to `ReleaseDocumentLease` when it stops editing; `expires_at` is when the lease lapses if neither happens, so a crashed holder never blocks the range forever.",
      "properties": {
        "block_id": {
          "description": "The leased block, or `None` for a whole-document (structural) lease.",
          "type": [
            "string",
            "null"
          ]
        },
        "document_id": {
          "format": "uuid",
          "type": "string"
        },
        "expires_at": {
          "description": "When the lease lapses unless renewed.",
          "format": "date-time",
          "type": "string"
        },
        "lease_id": {
          "description": "The server-minted lease id, returned only to the acquirer.",
          "type": "string"
        }
      },
      "required": [
        "document_id",
        "expires_at",
        "lease_id"
      ],
      "type": "object"
    },
    "DocumentMutation": {
      "description": "A semantic mutation on a collaborative document. Internally tagged with an [`DocumentMutation::Unknown`] fallback so a newer client's mutation deserializes and is rejected structurally rather than crashing the peer.",
      "oneOf": [
        {
          "description": "Insert a block at `index` with the given stable `block_id` and opaque content (a serialized `BlockContent`).",
          "properties": {
            "block_id": {
              "type": "string"
            },
            "content": true,
            "index": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "op": {
              "enum": [
                "insert"
              ],
              "type": "string"
            }
          },
          "required": [
            "block_id",
            "content",
            "index",
            "op"
          ],
          "type": "object"
        },
        {
          "description": "Delete the block with `block_id`.",
          "properties": {
            "block_id": {
              "type": "string"
            },
            "op": {
              "enum": [
                "delete"
              ],
              "type": "string"
            }
          },
          "required": [
            "block_id",
            "op"
          ],
          "type": "object"
        },
        {
          "description": "A CRDT text edit inside a block: at character `position`, delete `delete_len` characters then insert `insert`.",
          "properties": {
            "block_id": {
              "type": "string"
            },
            "delete_len": {
              "default": 0,
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "insert": {
              "default": "",
              "type": "string"
            },
            "op": {
              "enum": [
                "edit_text"
              ],
              "type": "string"
            },
            "position": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "block_id",
            "op",
            "position"
          ],
          "type": "object"
        },
        {
          "description": "Propose a replacement over a character range of a block (a suggestion).",
          "properties": {
            "op": {
              "enum": [
                "annotate"
              ],
              "type": "string"
            },
            "suggestion": {
              "$ref": "#/definitions/SuggestionInput"
            }
          },
          "required": [
            "op",
            "suggestion"
          ],
          "type": "object"
        },
        {
          "description": "Accept a previously proposed suggestion, applying its range.",
          "properties": {
            "op": {
              "enum": [
                "accept_suggestion"
              ],
              "type": "string"
            },
            "suggestion_id": {
              "type": "string"
            }
          },
          "required": [
            "op",
            "suggestion_id"
          ],
          "type": "object"
        },
        {
          "description": "Reject a suggestion without applying it.",
          "properties": {
            "op": {
              "enum": [
                "reject_suggestion"
              ],
              "type": "string"
            },
            "suggestion_id": {
              "type": "string"
            }
          },
          "required": [
            "op",
            "suggestion_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "op": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "op"
          ],
          "type": "object"
        }
      ]
    },
    "EditorActionContext": {
      "description": "Current editor state attached to an editor-native action.",
      "properties": {
        "diagnostics": {
          "items": {
            "$ref": "#/definitions/Diagnostic"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "ide": {
          "$ref": "#/definitions/IdeContextUpdate"
        },
        "repository_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "ide"
      ],
      "type": "object"
    },
    "EditorNativeAction": {
      "description": "An ordinary run entry point contributed by an editor client.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "FixSelection"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ExplainSelection"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ReviewCurrentFile"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "GenerateTestsForSelection"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "diagnostic": {
              "$ref": "#/definitions/Diagnostic"
            },
            "type": {
              "enum": [
                "FixDiagnostic"
              ],
              "type": "string"
            }
          },
          "required": [
            "diagnostic",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "EditorSelection": {
      "description": "The editor's current selection: a range within one file.",
      "properties": {
        "path": {
          "type": "string"
        },
        "range": {
          "$ref": "#/definitions/Range"
        }
      },
      "required": [
        "path",
        "range"
      ],
      "type": "object"
    },
    "EventBody": {
      "description": "The body of a persisted event.\n\nInternally tagged with a `#[serde(other)] Unknown` fallback (RULE 1): an event type produced by a newer daemon deserializes to `Unknown` in an older client instead of failing the whole frame, and the client renders an \"unsupported item\" placeholder. Phase 0 variants are preserved so old ledger bytes parse forever.",
      "oneOf": [
        {
          "properties": {
            "title": {
              "type": "string"
            },
            "type": {
              "enum": [
                "SessionCreated"
              ],
              "type": "string"
            }
          },
          "required": [
            "title",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "description": "The run this note belongs to, when it is run-scoped (a run's context manifest or a curated-memory note). `None` for a session-level note (e.g. user input, an effect-reconciliation record), which a client attaches to whatever run is in focus. Without this, a run's note could land on the wrong transcript when runs interleave (issue #6 item 3). `#[serde(default)]` keeps old ledger bytes (which have no `run_id`) parsing to `None`.",
              "format": "uuid",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "NoteAppended"
              ],
              "type": "string"
            }
          },
          "required": [
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "SessionClosed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "mode": {
              "$ref": "#/definitions/AgentMode"
            },
            "objective": {
              "type": "string"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunStarted"
              ],
              "type": "string"
            }
          },
          "required": [
            "mode",
            "objective",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "state": {
              "$ref": "#/definitions/RunState"
            },
            "type": {
              "enum": [
                "RunStateChanged"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "state",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "text": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ModelStreamDelta"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "text",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The daemon's model request failed transiently and the driver is waiting out a backoff before retry `attempt` of `max_attempts`. Purely informational: a run that ultimately fails still ends with its own `RunStateChanged`/`RunCompleted`; a retry that succeeds is followed by ordinary `ModelStreamDelta`s. Additive: an older client deserializes this to `Unknown` (RULE 1) and renders a placeholder.",
          "properties": {
            "attempt": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "delay_ms": {
              "description": "The wait before the retry fires, in milliseconds.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "max_attempts": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "message": {
              "description": "Bounded classifier reason (e.g. \"provider is overloaded\").",
              "type": "string"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ModelRetrying"
              ],
              "type": "string"
            }
          },
          "required": [
            "attempt",
            "delay_ms",
            "max_attempts",
            "message",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "action": {
              "$ref": "#/definitions/ProposedAction"
            },
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ToolProposed"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "approval_id",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A model-proposed action that the policy engine refused before execution. Keeping the typed action makes policy-denial evaluation and audit evidence non-vacuous: observers can prove the unsafe operation was attempted and blocked, not merely absent from an execution list.",
          "properties": {
            "action": {
              "$ref": "#/definitions/ProposedAction"
            },
            "reasons": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "ToolDenied"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "args_digest": {
              "description": "Digest of the tool arguments (not the arguments themselves).",
              "type": "string"
            },
            "label": {
              "description": "A short, human-readable display label for the call — e.g. the file path a `workspace.read_file` targets, or the command a `shell.run` executes — so a client can render `workspace.read_file · services/main.py` instead of the bare tool name. Derived by the emitter (`codypendent_runtime::tools::tool_label`) from the same arguments `args_digest` hashes, BEFORE they are discarded: bounded, single-line, and never the full arguments or file contents. `#[serde(default)]` keeps old ledger bytes and an older daemon's events (neither carries this field) deserializing to `None` — additive and back-compatible.",
              "type": [
                "string",
                "null"
              ]
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "tool": {
              "description": "Tool name, e.g. `shell.run`.",
              "type": "string"
            },
            "type": {
              "enum": [
                "ToolStarted"
              ],
              "type": "string"
            }
          },
          "required": [
            "args_digest",
            "run_id",
            "tool",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "artifact": {
              "anyOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Bulk output, if any, as an artifact reference."
            },
            "outcome": {
              "$ref": "#/definitions/ToolOutcome"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "tool": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ToolCompleted"
              ],
              "type": "string"
            }
          },
          "required": [
            "outcome",
            "run_id",
            "tool",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "additions": {
              "description": "Added lines in the unified diff.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "artifact": {
              "allOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                }
              ],
              "description": "The patch/diff, stored as an artifact."
            },
            "changeset_id": {
              "format": "uuid",
              "type": "string"
            },
            "deletions": {
              "description": "Removed lines in the unified diff.",
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "files": {
              "description": "Repository-relative paths touched by the change set.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "preview": {
              "description": "A bounded unified-diff preview for immediate review in clients.",
              "type": "string"
            },
            "preview_truncated": {
              "description": "Whether the full artifact contains more diff than `preview`.",
              "type": "boolean"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "PatchProposed"
              ],
              "type": "string"
            }
          },
          "required": [
            "artifact",
            "changeset_id",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "action": {
              "$ref": "#/definitions/ProposedAction"
            },
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "pattern": {
              "type": [
                "string",
                "null"
              ]
            },
            "risk": {
              "$ref": "#/definitions/Risk"
            },
            "type": {
              "enum": [
                "ApprovalRequested"
              ],
              "type": "string"
            }
          },
          "required": [
            "action",
            "approval_id",
            "risk",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "decision": {
              "$ref": "#/definitions/ApprovalDecision"
            },
            "type": {
              "enum": [
                "ApprovalResolved"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_id",
            "decision",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "SteeringQueued"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "SteeringApplied"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "dimension": {
              "$ref": "#/definitions/BudgetDimension"
            },
            "limit": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "BudgetWarning"
              ],
              "type": "string"
            },
            "used": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "dimension",
            "limit",
            "run_id",
            "type",
            "used"
          ],
          "type": "object"
        },
        {
          "description": "Live context-window usage, emitted per turn while the model's window is known (deduped by percent). Distinct from BudgetWarning (a threshold warning) and RunUsage (a terminal measured tally).",
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "system_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "tool_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "transcript_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "ContextUsage"
              ],
              "type": "string"
            },
            "used_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "window_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "run_id",
            "system_tokens",
            "tool_tokens",
            "transcript_tokens",
            "type",
            "used_tokens",
            "window_tokens"
          ],
          "type": "object"
        },
        {
          "properties": {
            "chronicle": {
              "allOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                }
              ],
              "description": "The run chronicle, stored as a JSON artifact."
            },
            "disposition": {
              "$ref": "#/definitions/RunDisposition"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunCompleted"
              ],
              "type": "string"
            }
          },
          "required": [
            "chronicle",
            "disposition",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "What a run actually consumed, as **measured** by the daemon after the loop finished (outcome 20). This is the wire half of migration 0032's `runs.prompt_tokens` / `completion_tokens` / `cost_micros`: without it a client can only ever learn a run's cost by reading the daemon's database directly, which no client does.\n\n[`BudgetWarning`](EventBody::BudgetWarning) is deliberately NOT this event: it reports a limit being approached, so it says nothing at all about a run with no configured budget — the common case. Usage is a fact about the run; a warning is a fact about a policy.\n\nEvery dimension is optional because an unmeasured one must stay absent rather than be reported as zero: a provider that returns no token counts, or a model with no price on file, would otherwise make a run read as free. Clients render only what is present (`cost_micros` is USD millionths).",
          "properties": {
            "completion_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "cost_micros": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "prompt_tokens": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunUsage"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A content-free projection of newly curated learning after a successful run. Facts, provenance, and confidence stay in the governed learning store; clients receive only counts and opaque ids for review affordances.",
          "properties": {
            "activated_count": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "activated_ids": {
              "items": {
                "format": "uuid",
                "type": "string"
              },
              "type": "array"
            },
            "proposed_count": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "proposed_ids": {
              "items": {
                "format": "uuid",
                "type": "string"
              },
              "type": "array"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "LearningsCaptured"
              ],
              "type": "string"
            }
          },
          "required": [
            "activated_count",
            "proposed_count",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A client attached to or detached from the session (Phase 3 STEP 3.7). Emitted so every attached client can show who else is present — e.g. the TUI showing that VS Code has joined the same session during a handoff.",
          "properties": {
            "client_id": {
              "format": "uuid",
              "type": "string"
            },
            "present": {
              "description": "`true` when the client attached, `false` when it detached.",
              "type": "boolean"
            },
            "role": {
              "$ref": "#/definitions/ClientRole"
            },
            "type": {
              "enum": [
                "ClientPresenceChanged"
              ],
              "type": "string"
            }
          },
          "required": [
            "client_id",
            "present",
            "role",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The run asked the operator structured questions and parked (adoption 03).",
          "properties": {
            "question_id": {
              "format": "uuid",
              "type": "string"
            },
            "questions": {
              "items": {
                "$ref": "#/definitions/QuestionPrompt"
              },
              "type": "array"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "QuestionAsked"
              ],
              "type": "string"
            }
          },
          "required": [
            "question_id",
            "questions",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The parked question was answered, rejected, or expired.",
          "properties": {
            "outcome": {
              "$ref": "#/definitions/QuestionOutcome"
            },
            "question_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "QuestionResolved"
              ],
              "type": "string"
            }
          },
          "required": [
            "outcome",
            "question_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A filesystem checkpoint of the run's operating worktree was recorded (Adoption 04). `ordinal` is the 1-based user-turn ordinal within the run: 1 at launch, +1 per applied steering turn. `commit` is the checkpoint object's SHA; `kind` says how to restore it (`\"stash\"` needs `git stash apply`, `\"commit\"` is a plain reset target). The ref `refs/codypendent/checkpoints/<run_id>/<ordinal>` pins the object.",
          "properties": {
            "base_commit": {
              "description": "The commit the run's worktree was carved from — the \"state before this turn\" restore/fork target for a `commit`-kind checkpoint.",
              "type": "string"
            },
            "checkpoint_id": {
              "format": "uuid",
              "type": "string"
            },
            "commit": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/CheckpointKind"
            },
            "ordinal": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "CheckpointRecorded"
              ],
              "type": "string"
            }
          },
          "required": [
            "base_commit",
            "checkpoint_id",
            "commit",
            "kind",
            "ordinal",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A checkpoint restore finished (Adoption 04). `restored` is false when the transactional restore failed and was rolled back losslessly.",
          "properties": {
            "checkpoint_id": {
              "format": "uuid",
              "type": "string"
            },
            "restored": {
              "type": "boolean"
            },
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "CheckpointRestored"
              ],
              "type": "string"
            }
          },
          "required": [
            "checkpoint_id",
            "restored",
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "This session was created by forking another at a checkpoint (Adoption 05). Appended once, immediately after the copied history, so the fork's own ledger records its origin. Clients render it as a \"forked from …\" marker; `Unknown` on older builds (RULE 1).",
          "properties": {
            "checkpoint": {
              "format": "uuid",
              "type": "string"
            },
            "from_session": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "SessionForked"
              ],
              "type": "string"
            }
          },
          "required": [
            "checkpoint",
            "from_session",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Full snapshot of the session's server-side pending-prompt queue after a mutation (Adoption 06). Latest-wins: a client folds it by REPLACING its queue projection, so replaying history converges on the final queue. Emitted from the same transaction as the mutation it records.",
          "properties": {
            "prompts": {
              "items": {
                "$ref": "#/definitions/PendingPromptView"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "PendingPromptsChanged"
              ],
              "type": "string"
            }
          },
          "required": [
            "prompts",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Forward-compatibility fallback for an event type this build does not know (RULE 1). Receivers render a placeholder and continue.",
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "FileMatchWire": {
      "description": "Wire match item for workspace file fuzzy search (Adoption 11 M2).",
      "properties": {
        "indices": {
          "items": {
            "format": "uint32",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "path": {
          "type": "string"
        },
        "score": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "indices",
        "path",
        "score"
      ],
      "type": "object"
    },
    "GitHubRefKind": {
      "description": "The kind of GitHub entity a [`GitHubReference`] points to.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "pull-request"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "issue"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "commit"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "comment"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "IdeContextUpdate": {
      "description": "A debounced snapshot of the IDE's context, pushed client→daemon. Clients debounce these (≥ 300 ms) so a burst of keystrokes collapses to one update.",
      "properties": {
        "active_file": {
          "description": "The file the user is focused on, if any.",
          "type": [
            "string",
            "null"
          ]
        },
        "diagnostics_revision": {
          "default": 0,
          "description": "A monotonically increasing revision for the diagnostics set, so the daemon can tell whether it holds the latest without transferring them.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "dirty_buffers": {
          "description": "Digests of every unsaved buffer (contents are never sent unsolicited).",
          "items": {
            "$ref": "#/definitions/DirtyBufferDigest"
          },
          "type": "array"
        },
        "open_files": {
          "description": "Paths of all open documents.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "selection": {
          "anyOf": [
            {
              "$ref": "#/definitions/EditorSelection"
            },
            {
              "type": "null"
            }
          ],
          "description": "The current selection, if any."
        }
      },
      "type": "object"
    },
    "ImageRegion": {
      "description": "A rectangular region of an image (a crop or a coordinate reference).",
      "properties": {
        "height": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "width": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "x": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "y": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "height",
        "width",
        "x",
        "y"
      ],
      "type": "object"
    },
    "InboxDeepLink": {
      "description": "A typed navigation target. Clients never need to interpret an arbitrary URL.",
      "oneOf": [
        {
          "properties": {
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Approval"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "question_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Question"
              ],
              "type": "string"
            }
          },
          "required": [
            "question_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Session"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Run"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Workflow"
              ],
              "type": "string"
            },
            "workflow_id": {
              "format": "uuid",
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "plugin_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Plugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "repository_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InboxEntry": {
      "description": "Repository-authorized client projection of an inbox row.\n\nThere is intentionally no `owner_id`: authorization and owner scoping are repository concerns and cannot be selected or asserted by a client.",
      "properties": {
        "acknowledged_at": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "created_at": {
          "format": "date-time",
          "type": "string"
        },
        "deep_link": {
          "$ref": "#/definitions/InboxDeepLink"
        },
        "dismissed_at": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "kind": {
          "$ref": "#/definitions/InboxEntryKind"
        },
        "repository_id": {
          "format": "uuid",
          "type": "string"
        },
        "resolved_at": {
          "description": "Set only when the authoritative source operation resolves. Inbox acknowledgement and dismissal never decide an approval or question.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "$ref": "#/definitions/InboxSource"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/InboxEntryState"
            }
          ],
          "default": {
            "type": "Unread"
          }
        },
        "summary": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "created_at",
        "deep_link",
        "id",
        "kind",
        "repository_id",
        "source",
        "title"
      ],
      "type": "object"
    },
    "InboxEntryKind": {
      "description": "The human work or notification represented by an inbox entry.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "ApprovalRequest"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "AgentQuestion"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "RunCompleted"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "RunFailed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "BudgetWarning"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "WorkflowBlocked"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "PluginPermissionChanged"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "RunnerFailed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InboxEntryState": {
      "description": "Read/lifecycle state of a durable inbox entry.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Unread"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Acknowledged"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Dismissed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Resolved"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InboxListFilters": {
      "description": "Optional list restrictions. An empty value means all authorized entries.",
      "properties": {
        "kinds": {
          "items": {
            "$ref": "#/definitions/InboxEntryKind"
          },
          "type": "array"
        },
        "repository_ids": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "states": {
          "items": {
            "$ref": "#/definitions/InboxEntryState"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "InboxListQuery": {
      "description": "Cursor-based inbox list request.",
      "properties": {
        "cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "filters": {
          "allOf": [
            {
              "$ref": "#/definitions/InboxListFilters"
            }
          ],
          "default": {}
        },
        "limit": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "InboxMutation": {
      "description": "Idempotent state change requested for an inbox entry.",
      "oneOf": [
        {
          "properties": {
            "entry_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Acknowledge"
              ],
              "type": "string"
            }
          },
          "required": [
            "entry_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "entry_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Dismiss"
              ],
              "type": "string"
            }
          },
          "required": [
            "entry_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InboxPage": {
      "description": "A cursor page returned by an inbox list operation.",
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/InboxEntry"
          },
          "type": "array"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "InboxSource": {
      "description": "Stable provenance used by the repository to deduplicate an entry.",
      "properties": {
        "dedup_key": {
          "description": "Stable within an owner. Replaying the same source must reuse this key.",
          "type": "string"
        },
        "identity": {
          "$ref": "#/definitions/InboxSourceIdentity"
        },
        "run_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "workflow_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "dedup_key",
        "identity"
      ],
      "type": "object"
    },
    "InboxSourceIdentity": {
      "description": "Durable source identity from which the daemon derives the deduplication key.",
      "oneOf": [
        {
          "properties": {
            "approval_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Approval"
              ],
              "type": "string"
            }
          },
          "required": [
            "approval_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "question_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Question"
              ],
              "type": "string"
            }
          },
          "required": [
            "question_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Run"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "budget_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Budget"
              ],
              "type": "string"
            }
          },
          "required": [
            "budget_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Workflow"
              ],
              "type": "string"
            },
            "workflow_id": {
              "format": "uuid",
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "plugin_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Plugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "plugin_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "runner_id": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Runner"
              ],
              "type": "string"
            }
          },
          "required": [
            "runner_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InputBlock": {
      "description": "One typed block within an [`InputEnvelope`]. Internally tagged on the wire with a `block` discriminant (`{\"block\": \"...\", …fields}`) so the media variants carry structured, artifact-linked payloads inline; an unrecognized block decodes to [`InputBlock::Unknown`] for forward compatibility. The discriminant is `block` (not `kind`) precisely because inner payloads such as [`SymbolRef`]/[`GitHubReference`] carry their own `kind` field.",
      "oneOf": [
        {
          "description": "Free text.",
          "properties": {
            "block": {
              "enum": [
                "text"
              ],
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "block",
            "text"
          ],
          "type": "object"
        },
        {
          "description": "Captured audio and (once produced) its reviewed transcript.",
          "properties": {
            "block": {
              "enum": [
                "audio"
              ],
              "type": "string"
            },
            "duration_ms": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "original": {
              "allOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                }
              ],
              "description": "The preserved original audio blob (kept where policy allows)."
            },
            "sample_rate_hz": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "transcript": {
              "anyOf": [
                {
                  "$ref": "#/definitions/Transcript"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The transcript, once produced and (for submission) reviewed."
            }
          },
          "required": [
            "block",
            "original"
          ],
          "type": "object"
        },
        {
          "description": "A pasted/attached image and its preserved derived artifacts.",
          "properties": {
            "block": {
              "enum": [
                "image"
              ],
              "type": "string"
            },
            "extracted_text": {
              "anyOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                },
                {
                  "type": "null"
                }
              ],
              "description": "(2) Extracted text (OCR), when produced. A separate artifact, not a substitute for the image."
            },
            "height": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "observations": {
              "description": "(3) Model observations about the image.",
              "items": {
                "$ref": "#/definitions/ModelObservation"
              },
              "type": "array"
            },
            "original": {
              "allOf": [
                {
                  "$ref": "#/definitions/ArtifactRef"
                }
              ],
              "description": "(1) The original image — never replaced by a summary."
            },
            "regions": {
              "description": "(4) Crop / coordinate references into the image.",
              "items": {
                "$ref": "#/definitions/ImageRegion"
              },
              "type": "array"
            },
            "width": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "block",
            "original"
          ],
          "type": "object"
        },
        {
          "description": "An attached file.",
          "properties": {
            "block": {
              "enum": [
                "file"
              ],
              "type": "string"
            },
            "byte_length": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "id": {
              "format": "uuid",
              "type": "string"
            },
            "media_type": {
              "description": "IANA media type, e.g. `text/plain` or `application/json`.",
              "type": "string"
            },
            "sensitivity": {
              "$ref": "#/definitions/DataClassification"
            },
            "sha256": {
              "description": "Lowercase hex SHA-256 of the blob's bytes (the content address).",
              "type": "string"
            }
          },
          "required": [
            "block",
            "byte_length",
            "id",
            "media_type",
            "sensitivity",
            "sha256"
          ],
          "type": "object"
        },
        {
          "description": "The IDE's current editor selection.",
          "properties": {
            "block": {
              "enum": [
                "editor-selection"
              ],
              "type": "string"
            },
            "path": {
              "type": "string"
            },
            "range": {
              "$ref": "#/definitions/Range"
            }
          },
          "required": [
            "block",
            "path",
            "range"
          ],
          "type": "object"
        },
        {
          "description": "A reference to a code symbol.",
          "properties": {
            "block": {
              "enum": [
                "code-symbol"
              ],
              "type": "string"
            },
            "kind": {
              "description": "The symbol kind (`function`, `struct`, …), when known.",
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "path": {
              "type": "string"
            },
            "symbol": {
              "description": "The symbol name (e.g. `WorkflowDriver::advance`).",
              "type": "string"
            }
          },
          "required": [
            "block",
            "path",
            "symbol"
          ],
          "type": "object"
        },
        {
          "description": "A reference to a GitHub entity (PR / issue / commit).",
          "properties": {
            "block": {
              "enum": [
                "github-reference"
              ],
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/GitHubRefKind"
            },
            "number": {
              "description": "The PR/issue number, or `None` for a commit/repo reference.",
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "owner": {
              "type": "string"
            },
            "repo": {
              "type": "string"
            },
            "url": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "block",
            "kind",
            "owner",
            "repo"
          ],
          "type": "object"
        },
        {
          "description": "A block kind a newer peer defined that this build does not know.",
          "properties": {
            "block": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "block"
          ],
          "type": "object"
        }
      ]
    },
    "InputEnvelope": {
      "description": "A normalized unit of user input: where it came from, the typed blocks it carries, the scope it applies at, and any bulk attachments.",
      "properties": {
        "attachments": {
          "description": "Bulk artifacts referenced by the blocks (or attached alongside them).",
          "items": {
            "$ref": "#/definitions/ArtifactRef"
          },
          "type": "array"
        },
        "blocks": {
          "items": {
            "$ref": "#/definitions/InputBlock"
          },
          "type": "array"
        },
        "scope": {
          "$ref": "#/definitions/ScopeLevel"
        },
        "source": {
          "$ref": "#/definitions/InputSource"
        }
      },
      "required": [
        "blocks",
        "scope",
        "source"
      ],
      "type": "object"
    },
    "InputSource": {
      "description": "Where an input originated.",
      "oneOf": [
        {
          "description": "The Ratatui TUI.",
          "properties": {
            "type": {
              "enum": [
                "tui"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "An IDE extension (VS Code / Cursor / Zed).",
          "properties": {
            "type": {
              "enum": [
                "ide"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The headless CLI / JSONL client.",
          "properties": {
            "type": {
              "enum": [
                "cli"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A web client.",
          "properties": {
            "type": {
              "enum": [
                "web"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Voice capture (push-to-talk), before/with transcription.",
          "properties": {
            "type": {
              "enum": [
                "voice"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A source a newer peer defined that this build does not know.",
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "InvocationPolicy": {
      "description": "Per-binding invocation controls, independent of the workflow definition.",
      "properties": {
        "approval_mode": {
          "allOf": [
            {
              "$ref": "#/definitions/AutomationApprovalMode"
            }
          ],
          "default": "inherit"
        },
        "budget_ceiling": {
          "anyOf": [
            {
              "$ref": "#/definitions/BudgetCeiling"
            },
            {
              "type": "null"
            }
          ]
        },
        "concurrency": {
          "allOf": [
            {
              "$ref": "#/definitions/ConcurrencyPolicy"
            }
          ],
          "default": "allow"
        },
        "deduplication": {
          "allOf": [
            {
              "$ref": "#/definitions/DeduplicationPolicy"
            }
          ],
          "default": {
            "window_seconds": 86400
          }
        },
        "missed_run": {
          "allOf": [
            {
              "$ref": "#/definitions/MissedRunPolicy"
            }
          ],
          "default": "skip"
        },
        "retry": {
          "allOf": [
            {
              "$ref": "#/definitions/TriggerRetryPolicy"
            }
          ],
          "default": {
            "backoff_multiplier": 2,
            "initial_delay_seconds": 30,
            "max_attempts": 0
          }
        }
      },
      "type": "object"
    },
    "MeasurementCoverage": {
      "description": "Number of observations for which a dimension was measured.",
      "properties": {
        "measured": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "total": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "measured",
        "total"
      ],
      "type": "object"
    },
    "MemoryEvidence": {
      "description": "The content behind one of a memory's evidence refs — Chapter 06's \"every retrieved memory opens its source\", fetched rather than merely named.",
      "oneOf": [
        {
          "description": "The session-ledger events the ref names, inclusive of both ends.",
          "properties": {
            "events": {
              "items": {
                "$ref": "#/definitions/SessionEvent"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "Events"
              ],
              "type": "string"
            }
          },
          "required": [
            "events",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The stored artifact's bytes. Base64 for the same reason `PutArtifact` uses it: JSON framing has no byte-string scalar.",
          "properties": {
            "bytes_base64": {
              "type": "string"
            },
            "media_type": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Artifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "bytes_base64",
            "media_type",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "MemoryScope": {
      "description": "A memory's scope as the two scalars the store indexes (`scope_tier` / `scope_key`). `key` is absent for the keyless `system` tier.",
      "properties": {
        "key": {
          "type": [
            "string",
            "null"
          ]
        },
        "tier": {
          "type": "string"
        }
      },
      "required": [
        "tier"
      ],
      "type": "object"
    },
    "MemoryScopeTier": {
      "description": "Which of the caller's *visible* scopes a bulk forget targets. Deliberately not a scope key: see the module doc.",
      "oneOf": [
        {
          "description": "Built-in, daemon-authored memories.",
          "properties": {
            "type": {
              "enum": [
                "System"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The local operator's own user scope.",
          "properties": {
            "type": {
              "enum": [
                "User"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The repository named by the command.",
          "properties": {
            "type": {
              "enum": [
                "Repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "MemoryView": {
      "description": "One memory as a client sees it.",
      "properties": {
        "class": {
          "description": "The memory class as its stored wire name (`semantic`, `episodic`, …).",
          "type": "string"
        },
        "confidence": {
          "format": "float",
          "type": "number"
        },
        "evidence": {
          "description": "One human-legible label per evidence ref, in the SAME order [`OpenMemoryEvidence`](crate::command::CommandBody::OpenMemoryEvidence)'s `evidence_index` addresses them — so \"show me where this came from\" is a position in this list, and a client never has to reconstruct an `EvidenceRef` it cannot type.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "observed_at": {
          "format": "date-time",
          "type": "string"
        },
        "scope": {
          "$ref": "#/definitions/MemoryScope"
        },
        "sensitivity": {
          "$ref": "#/definitions/DataClassification"
        },
        "statement": {
          "type": "string"
        },
        "structured_value": true,
        "supersedes": {
          "description": "The memories this one replaced, when it is itself a correction.",
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "class",
        "confidence",
        "id",
        "observed_at",
        "scope",
        "sensitivity",
        "statement"
      ],
      "type": "object"
    },
    "MissedRunPolicy": {
      "oneOf": [
        {
          "enum": [
            "skip",
            "run_once",
            "unknown"
          ],
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "catch_up": {
              "properties": {
                "max_occurrences": {
                  "format": "uint32",
                  "minimum": 0.0,
                  "type": "integer"
                }
              },
              "required": [
                "max_occurrences"
              ],
              "type": "object"
            }
          },
          "required": [
            "catch_up"
          ],
          "type": "object"
        }
      ]
    },
    "ModelObservation": {
      "description": "A model's textual observation about an image.",
      "properties": {
        "model": {
          "type": [
            "string",
            "null"
          ]
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "PendingApprovalProjection": {
      "description": "The actionable part of a pending approval carried in a compact snapshot.",
      "properties": {
        "action": {
          "$ref": "#/definitions/ProposedAction"
        },
        "approval_id": {
          "format": "uuid",
          "type": "string"
        },
        "risk": {
          "$ref": "#/definitions/Risk"
        },
        "run_id": {
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "action",
        "approval_id",
        "risk",
        "run_id"
      ],
      "type": "object"
    },
    "PendingPromptView": {
      "description": "One pending prompt, as carried on the `PendingPromptsChanged` snapshot.",
      "properties": {
        "delivery": {
          "$ref": "#/definitions/PromptDelivery"
        },
        "id": {
          "format": "uuid",
          "type": "string"
        },
        "mode": {
          "$ref": "#/definitions/AgentMode"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "delivery",
        "id",
        "mode",
        "text"
      ],
      "type": "object"
    },
    "Position": {
      "description": "A zero-based position in a text document.",
      "properties": {
        "character": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "line": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "character",
        "line"
      ],
      "type": "object"
    },
    "PromotionAction": {
      "description": "One legal state-machine transition to attempt via `AdvancePromotion` (Phase 7 STEP 7.5). Mirrors `codypendent_eval::promote::Candidate`'s methods exactly. Regression and canary verdicts are computed from durable evidence by the daemon, never supplied as client booleans.",
      "oneOf": [
        {
          "description": "Evaluate the latest durable core SuiteReport bound to this candidate.",
          "properties": {
            "type": {
              "enum": [
                "RunRegression"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Record the Controller's permission review before evaluation.",
          "properties": {
            "type": {
              "enum": [
                "ReviewPermissions"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Begin the shadow run.",
          "properties": {
            "type": {
              "enum": [
                "StartShadow"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Begin the limited canary.",
          "properties": {
            "type": {
              "enum": [
                "StartCanary"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Record objective canary evidence. The daemon derives the verdict.",
          "properties": {
            "metrics": {
              "$ref": "#/definitions/CanaryMetrics"
            },
            "type": {
              "enum": [
                "ObserveCanary"
              ],
              "type": "string"
            }
          },
          "required": [
            "metrics",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Finish the canary and assemble the comparison. Refused until the server has accumulated the required measured sample population.",
          "properties": {
            "type": {
              "enum": [
                "FinishCanary"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "PromptDelivery": {
      "description": "How a pending prompt is delivered (Adoption 06, cline's `PendingPromptDelivery`). `Steer` feeds the live run's steering channel at its next safe point; `Queue` waits for the session to go idle and launches a continuation run.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Queue"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Steer"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ProposedAction": {
      "description": "A side-effecting action an agent proposes, pending policy evaluation and possibly approval.\n\nThis started as the Phase 1 minimal subset of the Chapter 14 shape; Phase 3 adds `GitHubMutation` for remote GitHub writes. Further variants (`InstallPlugin`, structured `CommandRequest` / `NetworkDestination`) arrive in later phases. Paths and destinations are carried as strings on the wire.",
      "oneOf": [
        {
          "properties": {
            "paths": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "ReadFiles"
              ],
              "type": "string"
            }
          },
          "required": [
            "paths",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "patch": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "WritePatch"
              ],
              "type": "string"
            }
          },
          "required": [
            "patch",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "args": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "cwd": {
              "default": null,
              "description": "The working directory the command runs in, when constrained.",
              "type": [
                "string",
                "null"
              ]
            },
            "environment": {
              "default": [],
              "description": "The child's *complete* environment as name/value pairs (empty means it inherits nothing). Carried on the action so the approver and the audit ledger see exactly what the command runs with: an unshown, model-controlled environment could otherwise smuggle execution-hijacking variables (`LD_PRELOAD`, `RUSTC_WRAPPER`, a shadowed `PATH`, …) past a benign-looking `run cargo test` approval. Defaulted so an older client that sends none still parses.",
              "items": {
                "items": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "string"
                  }
                ],
                "maxItems": 2,
                "minItems": 2,
                "type": "array"
              },
              "type": "array"
            },
            "program": {
              "type": "string"
            },
            "type": {
              "enum": [
                "ExecuteCommand"
              ],
              "type": "string"
            }
          },
          "required": [
            "args",
            "program",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "destination": {
              "type": "string"
            },
            "type": {
              "enum": [
                "NetworkRequest"
              ],
              "type": "string"
            }
          },
          "required": [
            "destination",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "repository": {
              "type": "string"
            },
            "type": {
              "enum": [
                "GitCommit"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "branch": {
              "type": "string"
            },
            "remote": {
              "type": "string"
            },
            "type": {
              "enum": [
                "GitPush"
              ],
              "type": "string"
            }
          },
          "required": [
            "branch",
            "remote",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A write to a remote GitHub resource (draft PR, review comment, PR update, check-run summary) via the GitHub API (Phase 3 STEP 3.1). Every such write is approval-gated and network-scoped to the GitHub API endpoint by the policy engine.",
          "properties": {
            "repository": {
              "description": "The `owner/repo` slug the mutation targets.",
              "type": "string"
            },
            "summary": {
              "description": "A short human-readable description of the write, rendered on the approval card (e.g. `create draft PR on owner/repo`).",
              "type": "string"
            },
            "type": {
              "enum": [
                "GitHubMutation"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Publish a document's deterministic Markdown render to a Git target (Phase 4 STEP 4.4). Every publish is approval-gated; the approval card renders `target`, `changed_files`, and `git_action` **verbatim** from the computed plan (STEP 4.4.2: \"every publish displays target, changed files, and resulting Git action before approval\").",
          "properties": {
            "changed_files": {
              "description": "The repo-relative files the publish changes.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "document_id": {
              "format": "uuid",
              "type": "string"
            },
            "git_action": {
              "description": "The resulting Git action (e.g. `commit docs/x.md on branch docs/publish`).",
              "type": "string"
            },
            "target": {
              "description": "A short human description of the target (e.g. `repository file docs/architecture.md`).",
              "type": "string"
            },
            "type": {
              "enum": [
                "PublishDocument"
              ],
              "type": "string"
            }
          },
          "required": [
            "changed_files",
            "document_id",
            "git_action",
            "target",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Post a typed artifact to a workflow run's blackboard (Phase 5 STEP 5.3) — the run-scoped coordination channel a workflow's agents share. Always permitted by policy within a workflow run (the `blackboard.post` tool is only offered when the run is a workflow node), but recorded as a proposed action so every board write is traced like any other tool call. Not a filesystem, repository, or remote write — it targets only the run's own board, so it never reaches the approval gate.",
          "properties": {
            "kind": {
              "description": "The artifact kind being posted (`finding`, `decision`, …).",
              "type": "string"
            },
            "type": {
              "enum": [
                "BlackboardPost"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "description": "The workflow run whose board is written (server-derived from the run context, never model-supplied).",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Query a workflow run's blackboard (Phase 5 STEP 5.3). A read of the run's own coordination channel; always permitted by policy within a workflow run and recorded so every board access is traced.",
          "properties": {
            "type": {
              "enum": [
                "BlackboardQuery"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "description": "The workflow run whose board is read (server-derived).",
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Call a tool on an external MCP server (PR B — MCP client). The server is operator-declared in the trusted `<config_dir>/codypendent/mcp.toml` (the model can never name one into existence) and the call is dispositioned by the policy engine's `[mcp]` section: allow-listed servers may skip the gate, everything else is approval-gated or denied. The approval card renders `summary` plus `args` **verbatim**; `args` is canonical JSON (recursively key-sorted) so the Run-scoped auto-approval digest matches exactly-identical repeats.",
          "properties": {
            "args": {
              "description": "The model-supplied arguments as canonical JSON text (a `String`, not a `Value`, so the enum stays `Eq` and the digest is stable).",
              "type": "string"
            },
            "server": {
              "description": "The server name from `mcp.toml` (server-derived from the tool name's `mcp.<server>.<tool>` prefix, never free-form model text).",
              "type": "string"
            },
            "summary": {
              "description": "A short human-readable description of the call, rendered on the approval card (e.g. `github.create_issue(\"…\")`).",
              "type": "string"
            },
            "tool": {
              "description": "The tool name on that server (from the server's `tools/list`).",
              "type": "string"
            },
            "type": {
              "enum": [
                "McpToolCall"
              ],
              "type": "string"
            }
          },
          "required": [
            "args",
            "server",
            "summary",
            "tool",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A permission request made by an external ACP agent. The agent owns its tool implementation, so Codypendent cannot truthfully coerce the call into one of its native command/filesystem variants. `details` is bounded, canonical JSON suitable for the approval card and stable action digest.",
          "properties": {
            "agent": {
              "description": "Official registry id of the connected agent.",
              "type": "string"
            },
            "details": {
              "description": "Canonical, bounded ACP tool-call description.",
              "type": "string"
            },
            "title": {
              "description": "Human-readable tool/call title reported by ACP.",
              "type": "string"
            },
            "type": {
              "enum": [
                "AcpToolCall"
              ],
              "type": "string"
            }
          },
          "required": [
            "agent",
            "details",
            "title",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Record a memory proposal note (the `memory.remember` core tool, smarter-memory M2). Appends a `NoteAppended` to the run's own ledger — no filesystem, command, network, or remote effect. Always policy-`Allow`ed (see the daemon policy engine's explicit arm); never serialized into a `ToolProposed` (never gated by approval), so it needs no golden wire vector.",
          "properties": {
            "type": {
              "enum": [
                "RecordMemory"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Search the knowledge registry for the tools/skills that fit a task (the `skills.search` core tool, rubric 9). A READ of the daemon's own registry — no filesystem, command, network, or remote effect, and no model-supplied path (a skill's package directory comes from its registry row). Always policy-`Allow`ed, exactly like [`Self::RecordMemory`], and likewise never serialized into a `ToolProposed`, so it needs no golden wire vector.",
          "properties": {
            "type": {
              "enum": [
                "SearchRegistry"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "An agent `docs.*` tool call touching a collaborative document (rubric #4 doc-writer). Targets only the knowledge fabric's document store — not the filesystem, a command, the network, or any remote — and every content edit is still gated by the document's collaboration mode (organization docs default to Suggest, so an agent edit lands as a reviewable suggestion), while publishing to Git stays behind the separate approval-gated `PublishDocument` pipeline. Always policy-`Allow`ed like [`RecordMemory`](Self::RecordMemory) and recorded purely so the access is traced; never serialized into a `ToolProposed`, so it needs no golden wire vector.",
          "properties": {
            "document_id": {
              "description": "The document the tool call targets; empty for `docs.create` (the document does not exist yet) and `docs.read` listings.",
              "type": "string"
            },
            "summary": {
              "description": "A short human description of the access (e.g. `docs.edit block p`), for the trace.",
              "type": "string"
            },
            "type": {
              "enum": [
                "DocumentEdit"
              ],
              "type": "string"
            }
          },
          "required": [
            "document_id",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Query a durable workflow run's graph state (the `workflow.query` runtime tool, rubric 5): nodes, states, dependency edges, and measured costs. A pure read of Codypendent's own durable store — no filesystem, command, network, or remote effect — so it is always policy-`Allow`ed (see the daemon policy engine's explicit arm) and recorded only so the access is traced like any other tool call.",
          "properties": {
            "type": {
              "enum": [
                "WorkflowQuery"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "description": "The workflow run being read, or empty when listing the repository's runs (server-derived from the run context / validated args).",
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "Persist a validated, reviewable workflow manifest in the user's workflow directory. The full structured draft stays tool-local; this bounded preview is what policy, approval UIs, and the audit ledger record.",
          "properties": {
            "summary": {
              "type": "string"
            },
            "type": {
              "enum": [
                "WorkflowCreate"
              ],
              "type": "string"
            },
            "workflow_id": {
              "type": "string"
            }
          },
          "required": [
            "summary",
            "type",
            "workflow_id"
          ],
          "type": "object"
        },
        {
          "description": "Start a named persisted workflow or a validated inline workflow in the current run's repository. Both forms can spend model/tool budget and are therefore explicitly approval-gated.",
          "properties": {
            "kind": {
              "description": "`named` or `inline`, so approval surfaces distinguish persistence from an ephemeral manifest without decoding tool arguments.",
              "type": "string"
            },
            "summary": {
              "type": "string"
            },
            "type": {
              "enum": [
                "WorkflowRun"
              ],
              "type": "string"
            },
            "workflow_id": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "summary",
            "type",
            "workflow_id"
          ],
          "type": "object"
        },
        {
          "description": "Write a card on the repository task board (the `task.create` / `task.update` / `task.move` runtime tools, rubric 10). Internal coordination state in Codypendent's own store — like a blackboard post it touches no file, command, network, or remote — so policy allows it without an approval gate; the action is recorded so every board write is traced and attributable.",
          "properties": {
            "repository": {
              "description": "The canonical repository whose board is written (server-derived from the run context, never model-supplied).",
              "type": "string"
            },
            "summary": {
              "description": "A short human rendering of the write (e.g. `create \"wire the DAG\"`).",
              "type": "string"
            },
            "type": {
              "enum": [
                "TaskWrite"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read the repository task board (the `task.list` runtime tool). A read of internal state; always policy-`Allow`ed and recorded for the trace.",
          "properties": {
            "repository": {
              "description": "The canonical repository whose board is read (server-derived).",
              "type": "string"
            },
            "type": {
              "enum": [
                "TaskRead"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Persist a validated multi-model council definition. The summary is a bounded policy preview; the full typed definition remains tool-local.",
          "properties": {
            "name": {
              "type": "string"
            },
            "summary": {
              "type": "string"
            },
            "type": {
              "enum": [
                "CouncilCreate"
              ],
              "type": "string"
            }
          },
          "required": [
            "name",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Convene a persisted council. This may fan out paid model requests, so it always reaches an explicit approval before execution.",
          "properties": {
            "name": {
              "type": "string"
            },
            "summary": {
              "type": "string"
            },
            "type": {
              "enum": [
                "CouncilRun"
              ],
              "type": "string"
            }
          },
          "required": [
            "name",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read a durable council result by stable result id or council name.",
          "properties": {
            "selector": {
              "type": "string"
            },
            "type": {
              "enum": [
                "CouncilResultRead"
              ],
              "type": "string"
            }
          },
          "required": [
            "selector",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Read the repository's derived code graph (the `graph.callers_of` / `graph.blast_radius` / `graph.tests_covering` runtime tools, outcome 5). A pure read of Codypendent's OWN derived projection — no filesystem, command, network, or remote effect, and the model-supplied symbol or path is matched against stored `code_nodes` rows, never opened. Always policy-`Allow`ed like [`Self::SearchRegistry`], and likewise never serialized into a `ToolProposed`, so it needs no golden wire vector.",
          "properties": {
            "repository": {
              "description": "The canonical repository whose graph is read (server-derived from the run context, never model-supplied).",
              "type": "string"
            },
            "summary": {
              "description": "A short human rendering of the question (e.g. `callers of Router::decide`), for the trace.",
              "type": "string"
            },
            "type": {
              "enum": [
                "CodeGraphQuery"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Assert an edge onto the repository's derived code graph (the `graph.assert_edge` runtime tool) — a relation the parser cannot see, such as a route handler to the service it dispatches to.\n\nA WRITE, and deliberately its own variant rather than a [`Self::CodeGraphQuery`]: recording it as a query would make the audit ledger say the agent read the graph when it changed it. What it writes is Codypendent's OWN derived projection — no filesystem, command, network or remote effect — and it cannot invent a node: both endpoints must already match stored `code_nodes` rows, which are never opened as paths. So it is allowed without an approval gate on the same reasoning as [`Self::TaskWrite`], and recorded so every assertion is traced and attributable. Never serialized into a `ToolProposed`, so it needs no golden wire vector.",
          "properties": {
            "repository": {
              "description": "The canonical repository whose graph is written (server-derived from the run context, never model-supplied).",
              "type": "string"
            },
            "summary": {
              "description": "A short human rendering of the assertion (e.g. `assert handle_charge calls ChargeService::run`), for the trace.",
              "type": "string"
            },
            "type": {
              "enum": [
                "CodeGraphAssert"
              ],
              "type": "string"
            }
          },
          "required": [
            "repository",
            "summary",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Ask the operator one or more structured questions (the `user.ask` core tool, adoption 03). Targets only the session's own human — no filesystem, command, network, or remote effect — so it is always policy-`Allow`ed like [`Self::RecordMemory`] and recorded purely so the ask is traced. Never serialized into a `ToolProposed`, so it needs no golden wire vector.",
          "properties": {
            "headers": {
              "description": "The bounded `header` of each question, for the trace.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "question_count": {
              "description": "How many questions the call carries.",
              "format": "uint",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "AskUser"
              ],
              "type": "string"
            }
          },
          "required": [
            "headers",
            "question_count",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Restore a run's worktree to a recorded filesystem checkpoint (Adoption 04). Destructive for work done after the checkpoint, so it is always approval-gated; the card names exactly what is rewound.",
          "properties": {
            "commit": {
              "description": "The checkpoint commit being restored to.",
              "type": "string"
            },
            "ordinal": {
              "description": "The checkpoint's turn ordinal within the run.",
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "run_id": {
              "description": "The run whose worktree is rewound (string form of the RunId).",
              "type": "string"
            },
            "type": {
              "enum": [
                "RestoreCheckpoint"
              ],
              "type": "string"
            },
            "worktree": {
              "description": "The worktree directory the reset/clean/apply will run in.",
              "type": "string"
            }
          },
          "required": [
            "commit",
            "ordinal",
            "run_id",
            "type",
            "worktree"
          ],
          "type": "object"
        },
        {
          "description": "Write bytes to the standard input of a process the run already spawned (the `shell.write_stdin` unified-exec tool, adoption 09). Sending stdin to a live PTY child (e.g. an approved interactive `bash`) can drive arbitrary execution inside it, so it carries the SAME authority as spawning a command and is approval-gated exactly like [`Self::ExecuteCommand`] — never silently allowed. The raw bytes are deliberately NOT carried (only their length) so a model-echoed secret never lands in the approval card or the audit ledger; the process id and byte count are enough to render the write honestly and to keep an approval single-use per distinct write.",
          "properties": {
            "byte_len": {
              "description": "The number of stdin bytes written (the payload itself is never carried, so no echoed secret reaches the ledger).",
              "format": "uint",
              "minimum": 0.0,
              "type": "integer"
            },
            "process_id": {
              "description": "The id of the already-running process whose stdin is written (server-tracked; the model names an existing process, never spawns one here).",
              "format": "int32",
              "type": "integer"
            },
            "type": {
              "enum": [
                "WriteProcessStdin"
              ],
              "type": "string"
            }
          },
          "required": [
            "byte_len",
            "process_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Offer the operator a mode switch and, on their approval, enqueue the next turn in `target` (adoption 19: `plan_enter` / `plan_exit`). Targets only the session's own human (a yes/no question) and its own prompt queue — no filesystem, command, network, or remote effect — so it is always policy-`Allow`ed like [`Self::AskUser`] and recorded purely so the transition offer is traced. Never serialized into a `ToolProposed`.",
          "properties": {
            "target": {
              "allOf": [
                {
                  "$ref": "#/definitions/AgentMode"
                }
              ],
              "description": "The mode the accepted continuation runs in (`Plan` from `plan_enter`, `Build` from `plan_exit`)."
            },
            "type": {
              "enum": [
                "PlanTransition"
              ],
              "type": "string"
            }
          },
          "required": [
            "target",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ProtocolVersion": {
      "properties": {
        "major": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "minor": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "type": "object"
    },
    "PublishTarget": {
      "description": "Where a document publish writes (Phase 4 STEP 4.4). Wire mirror of `codypendent-knowledge`'s `PublishTarget` domain type — the protocol crate cannot name the knowledge crate, so this carries the same three targets across the wire and the `codypendentd` assembly converts one into the other before computing the plan. Internally tagged with a [`PublishTarget::Unknown`] fallback so a target a newer peer added deserializes and is rejected structurally rather than crashing the peer.",
      "oneOf": [
        {
          "description": "Write the rendered Markdown to a repository file via an approval-gated change set on the working tree.",
          "properties": {
            "kind": {
              "enum": [
                "repository_file"
              ],
              "type": "string"
            },
            "path": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "path"
          ],
          "type": "object"
        },
        {
          "description": "Commit the rendered Markdown to a dedicated docs branch, worktree-safe (the primary checkout is never touched).",
          "properties": {
            "branch": {
              "type": "string"
            },
            "kind": {
              "enum": [
                "docs_branch_commit"
              ],
              "type": "string"
            },
            "path": {
              "type": "string"
            }
          },
          "required": [
            "branch",
            "kind",
            "path"
          ],
          "type": "object"
        },
        {
          "description": "Open (or update) a documentation pull request via the Phase 3 GitHub write path.",
          "properties": {
            "branch": {
              "type": "string"
            },
            "kind": {
              "enum": [
                "documentation_pr"
              ],
              "type": "string"
            },
            "path": {
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "required": [
            "branch",
            "kind",
            "path",
            "title"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "QuestionOption": {
      "description": "One selectable choice.",
      "properties": {
        "description": {
          "description": "Explanation of the choice (may be empty).",
          "type": "string"
        },
        "label": {
          "description": "Display text (1–5 words, concise).",
          "type": "string"
        }
      },
      "required": [
        "label"
      ],
      "type": "object"
    },
    "QuestionOutcome": {
      "description": "How a question was resolved.",
      "oneOf": [
        {
          "description": "One answer array per question, in question order; each answer is the selected labels (custom text is carried verbatim as a label).",
          "properties": {
            "answers": {
              "items": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "Answered"
              ],
              "type": "string"
            }
          },
          "required": [
            "answers",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The user dismissed the question; `feedback` is the optional typed correction fed back to the model (the CorrectedError port).",
          "properties": {
            "feedback": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "Rejected"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "QuestionPrompt": {
      "description": "One question as asked. `custom` is carried on the wire but deliberately NOT advertised in the tool schema — the model can never disable free-text answers (opencode's Prompt/Info split).",
      "properties": {
        "custom": {
          "default": true,
          "description": "Allow typing a custom answer (default true).",
          "type": "boolean"
        },
        "header": {
          "description": "Very short label (≤ 30 chars) shown as the card/tab title.",
          "type": "string"
        },
        "multiple": {
          "default": false,
          "description": "Allow selecting more than one option.",
          "type": "boolean"
        },
        "options": {
          "description": "Available choices (may be empty only when `custom` is true).",
          "items": {
            "$ref": "#/definitions/QuestionOption"
          },
          "type": "array"
        },
        "question": {
          "description": "The complete question.",
          "type": "string"
        }
      },
      "required": [
        "header",
        "options",
        "question"
      ],
      "type": "object"
    },
    "Range": {
      "description": "A half-open range within a single document.",
      "properties": {
        "end": {
          "$ref": "#/definitions/Position"
        },
        "start": {
          "$ref": "#/definitions/Position"
        }
      },
      "required": [
        "end",
        "start"
      ],
      "type": "object"
    },
    "Risk": {
      "description": "A structured risk assessment attached to a proposed action or approval request. Chapter 14 leaves the exact shape open at Phase 1; this is the minimal reasonable form — a severity level plus human-readable reasons.",
      "properties": {
        "level": {
          "$ref": "#/definitions/RiskLevel"
        },
        "reasons": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "level"
      ],
      "type": "object"
    },
    "RiskLevel": {
      "description": "Severity buckets for a [`Risk`].",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Low"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Medium"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "High"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Critical"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "RunDisposition": {
      "description": "The terminal outcome of a run, carried by `RunCompleted`.\n\nChapter 04 names the terminal `RunState`s but leaves the disposition detail open at Phase 1; this is the minimal reasonable shape — the terminal kind plus a short human-readable summary or reason.",
      "oneOf": [
        {
          "properties": {
            "summary": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "Completed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "reason": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "reason",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "reason": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "Cancelled"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "RunState": {
      "description": "The lifecycle state of a run (Chapter 04). Transitions are persisted before they are exposed to clients.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Queued"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Preparing"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Running"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "WaitingForApproval"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "WaitingForUserInput"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Paused"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Recovering"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Completed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Cancelled"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "ScopeLevel": {
      "description": "The scope hierarchy an input applies at (README: `System → Organisation → User → Workspace → Repository → Branch → Session → Task`).",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "system"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "organization"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "user"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "workspace"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "branch"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "session"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "task"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionDeepLink": {
      "description": "Stable navigation target for a session-library result.",
      "oneOf": [
        {
          "properties": {
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Session"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Run"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "sequence": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Event"
              ],
              "type": "string"
            }
          },
          "required": [
            "sequence",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "artifact_id": {
              "format": "uuid",
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Artifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "artifact_id",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "column": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "line": {
              "format": "uint32",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "path": {
              "type": "string"
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Path"
              ],
              "type": "string"
            }
          },
          "required": [
            "path",
            "session_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "path": {
              "type": [
                "string",
                "null"
              ]
            },
            "session_id": {
              "format": "uuid",
              "type": "string"
            },
            "symbol": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Symbol"
              ],
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "symbol",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionDeletionMode": {
      "description": "Retention behavior requested by a session deletion. The daemon remains the policy authority and may reject a mode rather than weakening retention.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "RetentionPolicy"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "TombstoneOnly"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionEvent": {
      "properties": {
        "actor": {
          "$ref": "#/definitions/Actor"
        },
        "body": {
          "$ref": "#/definitions/EventBody"
        },
        "causation_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "correlation_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "occurred_at": {
          "format": "date-time",
          "type": "string"
        },
        "sequence": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "actor",
        "body",
        "occurred_at",
        "sequence"
      ],
      "type": "object"
    },
    "SessionExportFormat": {
      "description": "Portable session export formats understood by clients.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Json"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Markdown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionExportOptions": {
      "description": "Controls bounded data included in a session export.",
      "properties": {
        "format": {
          "$ref": "#/definitions/SessionExportFormat"
        },
        "include_artifacts": {
          "type": "boolean"
        },
        "include_internal_sessions": {
          "type": "boolean"
        }
      },
      "required": [
        "format"
      ],
      "type": "object"
    },
    "SessionHistoryPage": {
      "description": "Cursor-paged durable session history.",
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/SessionEvent"
          },
          "type": "array"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "SessionLifecycleAction": {
      "description": "A lifecycle mutation. The containing command supplies the idempotency key.",
      "oneOf": [
        {
          "properties": {
            "title": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Rename"
              ],
              "type": "string"
            }
          },
          "required": [
            "title",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Pin"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unpin"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Archive"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Restore"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "mode": {
              "allOf": [
                {
                  "$ref": "#/definitions/SessionDeletionMode"
                }
              ],
              "default": {
                "type": "RetentionPolicy"
              }
            },
            "type": {
              "enum": [
                "Delete"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "options": {
              "$ref": "#/definitions/SessionExportOptions"
            },
            "type": {
              "enum": [
                "Export"
              ],
              "type": "string"
            }
          },
          "required": [
            "options",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionProjection": {
      "description": "A compact summary of session state sent in place of a long event history.\n\nChapter 03 references a `SessionProjection` without fixing its fields; this is the minimal reasonable Phase 1 shape — enough for a reconnecting client to render a session's identity and live runs before it resumes live-tailing. Richer per-view projections arrive with their subscriptions.",
      "properties": {
        "active_runs": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "closed": {
          "type": "boolean"
        },
        "last_sequence": {
          "description": "The highest event sequence folded into this snapshot.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "pending_approvals": {
          "description": "Approvals which are still actionable at the snapshot watermark. A compacted catch-up must preserve workflow state, not merely run ids.",
          "items": {
            "$ref": "#/definitions/PendingApprovalProjection"
          },
          "type": "array"
        },
        "pending_prompts": {
          "description": "Pending queued prompts at the snapshot watermark, so a >500-event catch-up still shows the queue (mirrors `pending_approvals`).",
          "items": {
            "$ref": "#/definitions/PendingPromptView"
          },
          "type": "array"
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "closed",
        "last_sequence",
        "session_id",
        "title"
      ],
      "type": "object"
    },
    "SessionSearchFilters": {
      "description": "Filters applied together by the ranked session search service.",
      "properties": {
        "created_after": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "created_before": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "model_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repository_ids": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "run_states": {
          "items": {
            "$ref": "#/definitions/RunState"
          },
          "type": "array"
        },
        "workflow_ids": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SessionSearchPage": {
      "description": "Cursor-paged ranked search results.",
      "properties": {
        "items": {
          "items": {
            "$ref": "#/definitions/SessionSearchResult"
          },
          "type": "array"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "items"
      ],
      "type": "object"
    },
    "SessionSearchQuery": {
      "description": "Request for ranked session search.",
      "properties": {
        "cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "filters": {
          "allOf": [
            {
              "$ref": "#/definitions/SessionSearchFilters"
            }
          ],
          "default": {}
        },
        "limit": {
          "default": 0,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "SessionSearchResult": {
      "description": "A ranked search hit with a durable identity and navigable target.",
      "properties": {
        "deep_link": {
          "$ref": "#/definitions/SessionDeepLink"
        },
        "excerpt": {
          "type": [
            "string",
            "null"
          ]
        },
        "scope": {
          "$ref": "#/definitions/SessionSearchScope"
        },
        "score": {
          "format": "double",
          "type": "number"
        },
        "session": {
          "$ref": "#/definitions/SessionSummary"
        },
        "source": {
          "$ref": "#/definitions/SessionSearchSource"
        },
        "stable_identity": {
          "type": "string"
        }
      },
      "required": [
        "deep_link",
        "scope",
        "score",
        "session",
        "source",
        "stable_identity"
      ],
      "type": "object"
    },
    "SessionSearchScope": {
      "description": "Authorization scope in which a search hit was found.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Session"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Repository"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Workspace"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "User"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionSearchSource": {
      "description": "The indexed material responsible for a search hit.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Title"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Transcript"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ToolObservation"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Patch"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Artifact"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "ChangedPath"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Symbol"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SessionSummary": {
      "description": "Summary shown by session pickers and the Session Library.\n\nThe first six fields are the original v0.9 contract. Everything after them is additive so historical payloads remain valid.",
      "properties": {
        "archived_at": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "created_at": {
          "format": "date-time",
          "type": "string"
        },
        "internal": {
          "type": "boolean"
        },
        "last_activity_at": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "last_run_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "parent_run_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "parent_session_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "pinned": {
          "type": "boolean"
        },
        "repository": {
          "type": [
            "string",
            "null"
          ]
        },
        "repository_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "run_state": {
          "anyOf": [
            {
              "$ref": "#/definitions/RunState"
            },
            {
              "type": "null"
            }
          ]
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "updated_at": {
          "format": "date-time",
          "type": "string"
        },
        "workspace": {
          "type": [
            "string",
            "null"
          ]
        },
        "workspace_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "created_at",
        "session_id",
        "state",
        "title",
        "updated_at"
      ],
      "type": "object"
    },
    "Subscription": {
      "description": "A projection view a client subscribes to, rather than receiving every internal event (Chapter 03). This is the Phase 1 subset; document, workflow, and GitHub views arrive with their features.",
      "oneOf": [
        {
          "description": "High-level session lifecycle and summary.",
          "properties": {
            "type": {
              "enum": [
                "SessionSummary"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The detailed trace of one run.",
          "properties": {
            "run_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "RunTrace"
              ],
              "type": "string"
            }
          },
          "required": [
            "run_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Agent activity across the session.",
          "properties": {
            "type": {
              "enum": [
                "AgentActivity"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Repository/worktree status.",
          "properties": {
            "type": {
              "enum": [
                "RepositoryStatus"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Budget usage and warnings.",
          "properties": {
            "type": {
              "enum": [
                "BudgetState"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A collaborative document's CRDT sync stream (Phase 4 STEP 4.3): as the authoritative replica advances, the daemon fans each `DocumentSync` out to subscribers over a per-document hub, delivered as [`Payload::DocumentSync`](crate::envelope::Payload). A subscriber's baseline comes from the document read path; this stream carries the post-subscribe updates it merges (idempotent CRDT merge, so no watermark is needed). Re-attaching with a different `Document` set replaces the previous forwarders.",
          "properties": {
            "document_id": {
              "format": "uuid",
              "type": "string"
            },
            "type": {
              "enum": [
                "Document"
              ],
              "type": "string"
            }
          },
          "required": [
            "document_id",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A workflow run's blackboard stream (Phase 5 STEP 5.3): as the run's agents post (and supersede) typed artifacts, the daemon fans each [`BlackboardItemView`](crate::blackboard::BlackboardItemView) out to subscribers over a per-run hub, delivered as [`Payload::BlackboardPosted`](crate::envelope::Payload). A subscriber's baseline comes from the blackboard read command ([`ReadBlackboard`](crate::command::CommandBody::ReadBlackboard)); this stream carries the post-subscribe artifacts it merges by id (a superseding revision arrives as its own delivery, so no watermark is needed). Mirrors [`Document`](Subscription::Document)'s per-id hub, keyed by workflow run.",
          "properties": {
            "type": {
              "enum": [
                "Blackboard"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "A workflow run's live node-lifecycle stream (Phase 5 STEP 5.2 / T9): as the driver advances the graph, the daemon fans each [`WorkflowEvent`](crate::workflow::WorkflowEvent) out to subscribers over a per-run hub, delivered as [`Payload::WorkflowEvent`](crate::envelope::Payload::WorkflowEvent). A subscriber's baseline comes from the run snapshot command ([`ReadWorkflowRun`](crate::command::CommandBody::ReadWorkflowRun)); this stream carries the post-subscribe node transitions + run-phase changes it merges by `node_id` (each transition is full-state, so an overlap is a harmless re-write — no watermark). Mirrors [`Blackboard`](Subscription::Blackboard)'s per-run hub, keyed by workflow run.",
          "properties": {
            "type": {
              "enum": [
                "Workflow"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "SuggestionInput": {
      "description": "A proposed replacement over `[range_start, range_end)` of a block's text.",
      "properties": {
        "block_id": {
          "type": "string"
        },
        "range_end": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "range_start": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "rationale": {
          "type": [
            "string",
            "null"
          ]
        },
        "replacement": {
          "type": "string"
        }
      },
      "required": [
        "block_id",
        "range_end",
        "range_start",
        "replacement"
      ],
      "type": "object"
    },
    "ToolOutcome": {
      "description": "The outcome of a completed tool call, carried by `ToolCompleted`.\n\nChapter 03 lists tool-completed as an event category without fixing its payload; this is the minimal reasonable shape — success, or failure with a short message. Bulk output travels as an `ArtifactRef`, never here.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Succeeded"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "message": {
              "type": "string"
            },
            "type": {
              "enum": [
                "Failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "message",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "Transcript": {
      "description": "A transcript of an [`AudioArtifact`], linked back to its source audio.",
      "properties": {
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/TranscriptionMode"
            }
          ],
          "description": "Where the transcription ran (local vs. off-device)."
        },
        "model": {
          "description": "The transcription model, if a hosted/known one produced it.",
          "type": [
            "string",
            "null"
          ]
        },
        "reviewed": {
          "default": false,
          "description": "Whether the user reviewed/edited the transcript before submission (Chapter 10: \"transcript review before submission\").",
          "type": "boolean"
        },
        "source_audio": {
          "description": "The audio artifact this transcript was produced from — the link that keeps the original reachable from the interpretation.",
          "format": "uuid",
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "mode",
        "source_audio",
        "text"
      ],
      "type": "object"
    },
    "TranscriptionMode": {
      "description": "Where a transcription (or any media interpretation) runs.",
      "oneOf": [
        {
          "description": "On-device (e.g. a local whisper-server). Always permitted.",
          "properties": {
            "type": {
              "enum": [
                "local"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Off-device (a hosted/cloud model). Gated by data classification.",
          "properties": {
            "type": {
              "enum": [
                "remote"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "TriggerFilters": {
      "description": "Common source filters. Values are public event metadata, never credentials.",
      "properties": {
        "actors": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "branches": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "metadata": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "TriggerRetryPolicy": {
      "properties": {
        "backoff_multiplier": {
          "default": 2,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "initial_delay_seconds": {
          "default": 30,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_attempts": {
          "default": 0,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_delay_seconds": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TriggerSource": {
      "description": "The event or schedule that can invoke a binding.",
      "oneOf": [
        {
          "properties": {
            "expression": {
              "type": "string"
            },
            "timezone": {
              "type": "string"
            },
            "type": {
              "enum": [
                "cron"
              ],
              "type": "string"
            }
          },
          "required": [
            "expression",
            "timezone",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "at": {
              "format": "date-time",
              "type": "string"
            },
            "type": {
              "enum": [
                "one_time"
              ],
              "type": "string"
            }
          },
          "required": [
            "at",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "endpoint_id": {
              "type": "string"
            },
            "events": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "installation_id": {
              "format": "uint64",
              "minimum": 0.0,
              "type": [
                "integer",
                "null"
              ]
            },
            "type": {
              "enum": [
                "git_hub_webhook"
              ],
              "type": "string"
            }
          },
          "required": [
            "endpoint_id",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "endpoint_id": {
              "type": "string"
            },
            "signature": {
              "$ref": "#/definitions/WebhookSignatureScheme"
            },
            "signing_key_ref": {
              "description": "Reference to daemon-owned secret material, never the secret itself.",
              "type": "string"
            },
            "type": {
              "enum": [
                "signed_webhook"
              ],
              "type": "string"
            }
          },
          "required": [
            "endpoint_id",
            "signature",
            "signing_key_ref",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "provider": {
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "ci_failure"
              ],
              "type": "string"
            },
            "workflows": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "repository_change"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "code_graph_change"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "ecosystems": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "type": {
              "enum": [
                "dependency_alert"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "manual"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "api"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "UiAccessibility": {
      "description": "Accessibility metadata required to give graphical and terminal clients the same semantic representation.",
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "focusOrder": {
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "headingLevel": {
          "format": "uint8",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "hidden": {
          "type": "boolean"
        },
        "keyboardHint": {
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "liveRegion": {
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "type": [
            "string",
            "null"
          ]
        },
        "textFallback": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiActionBinding": {
      "description": "A semantic event-to-command binding. The host validates `action_id` and capabilities again when an invocation arrives; this declaration is never authority to perform I/O by itself.",
      "properties": {
        "actionId": {
          "type": "string"
        },
        "confirmation": {
          "type": [
            "string",
            "null"
          ]
        },
        "disabled": {
          "default": false,
          "type": "boolean"
        },
        "event": {
          "type": "string"
        },
        "payload": true,
        "requires": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "actionId",
        "event"
      ],
      "type": "object"
    },
    "UiActionCancellation": {
      "description": "Idempotent cancellation of an in-flight mediated command invocation.",
      "properties": {
        "invocationId": {
          "type": "string"
        }
      },
      "required": [
        "invocationId"
      ],
      "type": "object"
    },
    "UiActionInvocation": {
      "description": "Validated command intent produced from an action binding. The host remains responsible for permission checks and rejects stale `revision` values.",
      "properties": {
        "actionId": {
          "type": "string"
        },
        "documentId": {
          "type": "string"
        },
        "formData": {
          "additionalProperties": true,
          "type": "object"
        },
        "interactionEventType": {
          "description": "Event class to which the one-shot authority was bound.",
          "type": [
            "string",
            "null"
          ]
        },
        "interactionToken": {
          "description": "Echo of the broker-minted authority from the active event context.",
          "type": [
            "string",
            "null"
          ]
        },
        "invocationId": {
          "type": "string"
        },
        "payload": true,
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "sourceNodeId": {
          "type": "string"
        }
      },
      "required": [
        "actionId",
        "documentId",
        "invocationId",
        "revision",
        "sourceNodeId"
      ],
      "type": "object"
    },
    "UiActionResult": {
      "description": "Resolution of a component-originated action/command invocation.",
      "properties": {
        "error": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiRemoteError"
            },
            {
              "type": "null"
            }
          ]
        },
        "invocationId": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "value": true
      },
      "required": [
        "invocationId",
        "status"
      ],
      "type": "object"
    },
    "UiCapabilities": {
      "description": "Client or host capability advertisement. All named sets are open-ended and negotiated by intersection. A missing feature means the plain-text baseline.",
      "properties": {
        "capabilities": {
          "description": "Additive host command capabilities, independent of presentation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "client": {
          "type": "string"
        },
        "clipboard": {
          "type": "boolean"
        },
        "colorDepth": {
          "type": "string"
        },
        "contributionPoints": {
          "description": "Additive plugin mount points understood by the host.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "daemon": {
          "$ref": "#/definitions/ClientCapabilities"
        },
        "keyboard": {
          "type": "boolean"
        },
        "limits": {
          "$ref": "#/definitions/UiHardLimits"
        },
        "media": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "primitives": {
          "$ref": "#/definitions/UiPrimitivesSchema"
        },
        "protocolVersions": {
          "items": {
            "$ref": "#/definitions/UiProtocolVersion"
          },
          "type": "array"
        },
        "reducedMotion": {
          "type": "boolean"
        },
        "screenReader": {
          "type": "boolean"
        },
        "terminalGraphics": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "viewport": {
          "$ref": "#/definitions/UiViewport"
        }
      },
      "required": [
        "client",
        "clipboard",
        "colorDepth",
        "daemon",
        "keyboard",
        "primitives",
        "protocolVersions",
        "reducedMotion",
        "screenReader",
        "viewport"
      ],
      "type": "object"
    },
    "UiCapabilitySelection": {
      "description": "Result of intersecting a producer/host offer with a rendering client.",
      "properties": {
        "capabilities": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "colorDepth": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "contributionPoints": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "imageProtocols": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limits": {
          "$ref": "#/definitions/UiHardLimits"
        },
        "mouse": {
          "type": "boolean"
        },
        "primitives": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protocolVersion": {
          "$ref": "#/definitions/UiProtocolVersion"
        },
        "screenReader": {
          "type": "boolean"
        },
        "unicode": {
          "type": "boolean"
        },
        "viewport": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiViewport"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "capabilities",
        "colorDepth",
        "contributionPoints",
        "imageProtocols",
        "limits",
        "mouse",
        "primitives",
        "protocolVersion",
        "screenReader",
        "unicode"
      ],
      "type": "object"
    },
    "UiCompatibility": {
      "description": "Compatibility requirements attached to a complete document.",
      "properties": {
        "fallback": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiFallback"
            },
            {
              "type": "null"
            }
          ]
        },
        "minimumProtocol": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiProtocolVersion"
            },
            {
              "type": "null"
            }
          ]
        },
        "requiredCapabilities": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "requiredPrimitives": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "UiContent": {
      "description": "Shared content properties for Text, Markdown, Code, Diff, Image, Audio, JsonTree, LogViewer, and custom content primitives.",
      "properties": {
        "alternateText": {
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "type": [
            "string",
            "null"
          ]
        },
        "lineWrap": {
          "type": [
            "string",
            "null"
          ]
        },
        "resource": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiResourceReference"
            },
            {
              "type": "null"
            }
          ]
        },
        "spans": {
          "items": {
            "$ref": "#/definitions/UiTextSpan"
          },
          "type": "array"
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiContributionRegistration": {
      "description": "Where and how one package-provided surface is mounted. `point` and `slot` are strings so new host surfaces do not require a protocol release.",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "extensionId": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "metadata": {
          "additionalProperties": true,
          "type": "object"
        },
        "point": {
          "type": "string"
        },
        "priority": {
          "default": 0,
          "format": "int32",
          "type": "integer"
        },
        "requires": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "slot": {
          "type": "string"
        },
        "when": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "documentId",
        "extensionId",
        "id",
        "point",
        "slot"
      ],
      "type": "object"
    },
    "UiData": {
      "description": "Renderer-neutral structured data. Schemas and items remain JSON so a new chart, graph, or plugin-specific renderer does not require a protocol bump.",
      "properties": {
        "columns": {
          "items": {
            "$ref": "#/definitions/UiDataColumn"
          },
          "type": "array"
        },
        "cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "items": {
          "items": true,
          "type": "array"
        },
        "kind": {
          "type": [
            "string",
            "null"
          ]
        },
        "schema": true,
        "selectedIds": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "total": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiDataColumn": {
      "description": "Column metadata used by tables and other structured-data primitives.",
      "properties": {
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "sortable": {
          "default": false,
          "type": "boolean"
        },
        "valueType": {
          "type": [
            "string",
            "null"
          ]
        },
        "width": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "id",
        "label"
      ],
      "type": "object"
    },
    "UiDimension": {
      "description": "Renderer-neutral dimension. `unit` is normally `cells`, `percent`, `fr`, or `auto`, but is intentionally open-ended.",
      "properties": {
        "unit": {
          "type": "string"
        },
        "value": {
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "unit",
        "value"
      ],
      "type": "object"
    },
    "UiDispose": {
      "description": "Host instruction to unmount one document at its current revision.",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "documentId",
        "revision"
      ],
      "type": "object"
    },
    "UiDocument": {
      "description": "A complete semantic UI tree at one revision.",
      "properties": {
        "capabilities": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiCapabilities"
            },
            {
              "type": "null"
            }
          ]
        },
        "compatibility": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiCompatibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "documentId": {
          "type": "string"
        },
        "metadata": {
          "additionalProperties": true,
          "type": "object"
        },
        "protocolVersion": {
          "$ref": "#/definitions/UiProtocolVersion"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "root": {
          "$ref": "#/definitions/UiNode"
        }
      },
      "required": [
        "documentId",
        "protocolVersion",
        "revision",
        "root"
      ],
      "type": "object"
    },
    "UiEdges": {
      "description": "Four-sided spacing in terminal cells or renderer logical units.",
      "properties": {
        "bottom": {
          "default": 0.0,
          "format": "double",
          "type": "number"
        },
        "left": {
          "default": 0.0,
          "format": "double",
          "type": "number"
        },
        "right": {
          "default": 0.0,
          "format": "double",
          "type": "number"
        },
        "top": {
          "default": 0.0,
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "UiEvent": {
      "description": "A host-normalized event emitted by a semantic node.",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "eventId": {
          "type": "string"
        },
        "interactionToken": {
          "description": "Opaque one-shot host authority. Renderers never mint this value; the broker adds it only to the owner-bound event forwarded to a worker.",
          "type": [
            "string",
            "null"
          ]
        },
        "modifiers": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiEventModifiers"
            },
            {
              "type": "null"
            }
          ]
        },
        "payload": true,
        "protocolVersion": {
          "$ref": "#/definitions/UiProtocolVersion"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "targetId": {
          "type": "string"
        },
        "timestamp": {
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "documentId",
        "eventId",
        "protocolVersion",
        "revision",
        "targetId",
        "type"
      ],
      "type": "object"
    },
    "UiEventModifiers": {
      "description": "Keyboard/pointer modifiers accompanying a semantic event.",
      "properties": {
        "alt": {
          "default": false,
          "type": "boolean"
        },
        "control": {
          "default": false,
          "type": "boolean"
        },
        "meta": {
          "default": false,
          "type": "boolean"
        },
        "shift": {
          "default": false,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "UiFallback": {
      "description": "Plain-text or simpler semantic fallback for unsupported client features.",
      "properties": {
        "behavior": {
          "type": [
            "string",
            "null"
          ]
        },
        "plainText": {
          "type": [
            "string",
            "null"
          ]
        },
        "replacement": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiNode"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "UiFeedback": {
      "description": "Progress, status, and tone shared by feedback primitives.",
      "properties": {
        "current": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "indeterminate": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "maximum": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "message": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "type": [
            "string",
            "null"
          ]
        },
        "tone": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiHardLimits": {
      "description": "Hard resource ceilings applied before a tree or patch reaches a renderer. Defaults are deliberately conservative enough for a full-screen app while bounding CPU, allocation, and pathological recursive/value input.",
      "properties": {
        "maxActionsPerNode": {
          "default": 64,
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxContributions": {
          "default": 1000,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxJsonDepth": {
          "default": 32,
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxJsonValues": {
          "default": 100000,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxNodes": {
          "default": 20000,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxPatchBytes": {
          "default": 4194304,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxPatchesPerBatch": {
          "default": 2000,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxPropertiesPerNode": {
          "default": 256,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxTextBytes": {
          "default": 2097152,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxTreeDepth": {
          "default": 64,
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "UiHotReload": {
      "description": "Development-runtime notification that compiled modules changed.",
      "properties": {
        "changedModules": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "generation": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "changedModules",
        "generation"
      ],
      "type": "object"
    },
    "UiInput": {
      "description": "Input state. Secret values must never be placed in a remote tree; secret entry is a host-owned contribution point.",
      "properties": {
        "defaultValue": true,
        "disabled": {
          "default": false,
          "type": "boolean"
        },
        "inputType": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "options": {
          "items": {
            "$ref": "#/definitions/UiInputOption"
          },
          "type": "array"
        },
        "placeholder": {
          "type": [
            "string",
            "null"
          ]
        },
        "readOnly": {
          "default": false,
          "type": "boolean"
        },
        "required": {
          "default": false,
          "type": "boolean"
        },
        "validationMessage": {
          "type": [
            "string",
            "null"
          ]
        },
        "value": true
      },
      "type": "object"
    },
    "UiInputOption": {
      "description": "One selectable value for input primitives.",
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "disabled": {
          "default": false,
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "value": true
      },
      "required": [
        "id",
        "label",
        "value"
      ],
      "type": "object"
    },
    "UiLayout": {
      "description": "Semantic layout hints. Hosts remain authoritative for clipping, cell width, responsive collapse, and terminal-safe placement.",
      "properties": {
        "align": {
          "type": [
            "string",
            "null"
          ]
        },
        "basis": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "columnGap": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "columns": {
          "items": {
            "$ref": "#/definitions/UiDimension"
          },
          "type": "array"
        },
        "direction": {
          "type": [
            "string",
            "null"
          ]
        },
        "gap": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "grow": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "height": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "justify": {
          "type": [
            "string",
            "null"
          ]
        },
        "margin": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiEdges"
            },
            {
              "type": "null"
            }
          ]
        },
        "maxHeight": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "maxWidth": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "minHeight": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "minWidth": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "overflow": {
          "type": [
            "string",
            "null"
          ]
        },
        "padding": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiEdges"
            },
            {
              "type": "null"
            }
          ]
        },
        "rowGap": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "rows": {
          "items": {
            "$ref": "#/definitions/UiDimension"
          },
          "type": "array"
        },
        "shrink": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "width": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDimension"
            },
            {
              "type": "null"
            }
          ]
        },
        "wrap": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiNavigation": {
      "description": "Navigation state shared by links, tabs, menus, trees, and command lists.",
      "properties": {
        "destination": {
          "type": [
            "string",
            "null"
          ]
        },
        "disabled": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "expanded": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "selected": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "target": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiNode": {
      "properties": {
        "children": {
          "default": [],
          "items": {
            "$ref": "#/definitions/UiNode"
          },
          "type": "array"
        },
        "fallback": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiNode"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "type": "string"
        },
        "props": {
          "allOf": [
            {
              "$ref": "#/definitions/UiNodeProps"
            }
          ],
          "default": {}
        },
        "requires": {
          "default": [],
          "items": {
            "$ref": "#/definitions/UiRequirement"
          },
          "type": "array"
        },
        "text": {
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "UiNodeProps": {
      "additionalProperties": true,
      "description": "Typed common properties plus flattened extension properties. This serializes as the SDK's ordinary `props` JSON object while allowing Rust renderers to consume the stable semantic subset without stringly typed field access.",
      "properties": {
        "accessibility": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiAccessibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "attributes": {
          "additionalProperties": true,
          "type": "object"
        },
        "content": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiContent"
            },
            {
              "type": "null"
            }
          ]
        },
        "eventBindings": {
          "items": {
            "$ref": "#/definitions/UiActionBinding"
          },
          "type": "array"
        },
        "feedback": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiFeedback"
            },
            {
              "type": "null"
            }
          ]
        },
        "input": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiInput"
            },
            {
              "type": "null"
            }
          ]
        },
        "layout": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiLayout"
            },
            {
              "type": "null"
            }
          ]
        },
        "navigation": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiNavigation"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "type": [
            "string",
            "null"
          ]
        },
        "structuredData": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiData"
            },
            {
              "type": "null"
            }
          ]
        },
        "style": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiStyle"
            },
            {
              "type": "null"
            }
          ]
        },
        "value": true
      },
      "type": "object"
    },
    "UiPatch": {
      "oneOf": [
        {
          "properties": {
            "node": {
              "$ref": "#/definitions/UiNode"
            },
            "op": {
              "enum": [
                "replaceRoot"
              ],
              "type": "string"
            }
          },
          "required": [
            "node",
            "op"
          ],
          "type": "object"
        },
        {
          "properties": {
            "index": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "node": {
              "$ref": "#/definitions/UiNode"
            },
            "op": {
              "enum": [
                "insert"
              ],
              "type": "string"
            },
            "parentId": {
              "type": "string"
            }
          },
          "required": [
            "index",
            "node",
            "op",
            "parentId"
          ],
          "type": "object"
        },
        {
          "properties": {
            "nodeId": {
              "type": "string"
            },
            "op": {
              "enum": [
                "remove"
              ],
              "type": "string"
            }
          },
          "required": [
            "nodeId",
            "op"
          ],
          "type": "object"
        },
        {
          "properties": {
            "node": {
              "$ref": "#/definitions/UiNode"
            },
            "nodeId": {
              "type": "string"
            },
            "op": {
              "enum": [
                "replace"
              ],
              "type": "string"
            }
          },
          "required": [
            "node",
            "nodeId",
            "op"
          ],
          "type": "object"
        },
        {
          "properties": {
            "nodeId": {
              "type": "string"
            },
            "op": {
              "enum": [
                "updateProps"
              ],
              "type": "string"
            },
            "set": {
              "additionalProperties": true,
              "default": {},
              "type": "object"
            },
            "unset": {
              "default": [],
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "nodeId",
            "op"
          ],
          "type": "object"
        },
        {
          "properties": {
            "nodeId": {
              "type": "string"
            },
            "op": {
              "enum": [
                "setText"
              ],
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "nodeId",
            "op",
            "text"
          ],
          "type": "object"
        },
        {
          "properties": {
            "index": {
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "nodeId": {
              "type": "string"
            },
            "op": {
              "enum": [
                "move"
              ],
              "type": "string"
            },
            "parentId": {
              "type": "string"
            }
          },
          "required": [
            "index",
            "nodeId",
            "op",
            "parentId"
          ],
          "type": "object"
        }
      ]
    },
    "UiPatchBatch": {
      "description": "Atomic, ordered set of mutations from `base_revision` to the immediately following `revision`.",
      "properties": {
        "atomic": {
          "type": "boolean"
        },
        "baseRevision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "documentId": {
          "type": "string"
        },
        "fallback": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiFallback"
            },
            {
              "type": "null"
            }
          ]
        },
        "issuedAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "patches": {
          "items": {
            "$ref": "#/definitions/UiPatch"
          },
          "type": "array"
        },
        "protocolVersion": {
          "$ref": "#/definitions/UiProtocolVersion"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "baseRevision",
        "documentId",
        "patches",
        "protocolVersion",
        "revision"
      ],
      "type": "object"
    },
    "UiPluginLifecycleStatus": {
      "description": "Durable Remote UI plugin lifecycle status returned by daemon management commands. Trust and execution authority remain daemon-owned; this is a display-only projection.",
      "properties": {
        "enabledScope": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "updateApprovalReceipt": {
          "type": [
            "string",
            "null"
          ]
        },
        "updatePermissionDiff": {
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "state",
        "version"
      ],
      "type": "object"
    },
    "UiPrimitivesSchema": {
      "anyOf": [
        {
          "$ref": "#/definitions/UiWildcardSchema"
        },
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      ]
    },
    "UiProjectionSubscription": {
      "description": "A component's mediated projection subscription. `kind` is open-ended (`session`, `run`, `artifact`, `command`, ...); the daemon authorizes each request against the plugin manifest before returning data.",
      "properties": {
        "kind": {
          "type": "string"
        },
        "parameters": {
          "additionalProperties": true,
          "type": "object"
        },
        "resourceId": {
          "type": [
            "string",
            "null"
          ]
        },
        "subscriptionId": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "subscriptionId"
      ],
      "type": "object"
    },
    "UiProjectionUnsubscription": {
      "description": "Owner-scoped teardown for one mediated projection subscription.",
      "properties": {
        "subscriptionId": {
          "type": "string"
        }
      },
      "required": [
        "subscriptionId"
      ],
      "type": "object"
    },
    "UiProjectionUpdate": {
      "description": "Latest-wins data delivered for one mediated subscription. The worker never receives a raw path, database handle, socket, or secret through this value.",
      "properties": {
        "removed": {
          "type": "boolean"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "subscriptionId": {
          "type": "string"
        },
        "value": true
      },
      "required": [
        "subscriptionId"
      ],
      "type": "object"
    },
    "UiProtocolVersion": {
      "description": "Version of the remote UI contract, negotiated independently of the daemon envelope protocol so renderers can evolve at their own pace.",
      "properties": {
        "major": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "minor": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "type": "object"
    },
    "UiRemoteError": {
      "description": "Structured renderer/protocol error with a safe fallback and recovery hint.",
      "properties": {
        "code": {
          "type": "string"
        },
        "details": true,
        "documentId": {
          "type": [
            "string",
            "null"
          ]
        },
        "fallback": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiFallback"
            },
            {
              "type": "null"
            }
          ]
        },
        "message": {
          "type": "string"
        },
        "nodeId": {
          "type": [
            "string",
            "null"
          ]
        },
        "patchIndex": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "recoverable": {
          "default": false,
          "type": "boolean"
        },
        "recovery": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "UiRequirement": {
      "description": "Presentation feature required by a node. Unknown features remain representable and are resolved through the node fallback.",
      "properties": {
        "feature": {
          "type": "string"
        },
        "optional": {
          "type": "boolean"
        }
      },
      "required": [
        "feature"
      ],
      "type": "object"
    },
    "UiResourceReference": {
      "description": "A resource is referenced, never embedded as unbounded bytes in the UI tree.",
      "properties": {
        "byteLength": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "digest": {
          "type": [
            "string",
            "null"
          ]
        },
        "mediaType": {
          "type": "string"
        },
        "uri": {
          "type": "string"
        }
      },
      "required": [
        "mediaType",
        "uri"
      ],
      "type": "object"
    },
    "UiResyncRequest": {
      "description": "Runtime request for a fresh snapshot after a missing or rejected patch.",
      "properties": {
        "documentId": {
          "type": "string"
        },
        "knownRevision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "documentId"
      ],
      "type": "object"
    },
    "UiSnapshot": {
      "description": "Full-state baseline used on mount, reconnect, patch rejection, and renderer recovery.",
      "properties": {
        "document": {
          "$ref": "#/definitions/UiDocument"
        },
        "reason": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "document"
      ],
      "type": "object"
    },
    "UiStyle": {
      "description": "Semantic styling references theme tokens; producers never emit ANSI escapes, CSS, or raw terminal control sequences.",
      "properties": {
        "background": {
          "type": [
            "string",
            "null"
          ]
        },
        "borderColor": {
          "type": [
            "string",
            "null"
          ]
        },
        "borderStyle": {
          "type": [
            "string",
            "null"
          ]
        },
        "emphasis": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "foreground": {
          "type": [
            "string",
            "null"
          ]
        },
        "opacity": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "tone": {
          "type": [
            "string",
            "null"
          ]
        },
        "truncate": {
          "type": [
            "string",
            "null"
          ]
        },
        "visibility": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "UiTextSpan": {
      "description": "One rich-text span within a content primitive.",
      "properties": {
        "accessibilityLabel": {
          "type": [
            "string",
            "null"
          ]
        },
        "link": {
          "type": [
            "string",
            "null"
          ]
        },
        "style": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiStyle"
            },
            {
              "type": "null"
            }
          ]
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "UiTheme": {
      "description": "Semantic theme tokens. Token values are JSON scalars or small structured values so future renderers can add gradients or terminal-specific palettes without changing the contract.",
      "properties": {
        "colorScheme": {
          "type": [
            "string",
            "null"
          ]
        },
        "highContrast": {
          "default": false,
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "reducedMotion": {
          "default": false,
          "type": "boolean"
        },
        "revision": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "tokens": {
          "additionalProperties": true,
          "type": "object"
        }
      },
      "required": [
        "id",
        "name",
        "revision"
      ],
      "type": "object"
    },
    "UiViewport": {
      "description": "Bounded viewport advertised by a rendering client.",
      "properties": {
        "density": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "height": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "pixelHeight": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "pixelWidth": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "width": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "height",
        "width"
      ],
      "type": "object"
    },
    "UiWildcardSchema": {
      "enum": [
        "*"
      ],
      "type": "string"
    },
    "UiWireMessage": {
      "description": "A forward-compatible top-level message for dedicated remote UI transports. `kind` selects the populated optional payload. Unknown kinds remain deserializable and can carry `extensions` plus a fallback/error.",
      "properties": {
        "action": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiActionInvocation"
            },
            {
              "type": "null"
            }
          ]
        },
        "actionResult": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiActionResult"
            },
            {
              "type": "null"
            }
          ]
        },
        "cancellation": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiActionCancellation"
            },
            {
              "type": "null"
            }
          ]
        },
        "capabilities": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiCapabilities"
            },
            {
              "type": "null"
            }
          ]
        },
        "contributions": {
          "items": {
            "$ref": "#/definitions/UiContributionRegistration"
          },
          "type": "array"
        },
        "dispose": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiDispose"
            },
            {
              "type": "null"
            }
          ]
        },
        "error": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiRemoteError"
            },
            {
              "type": "null"
            }
          ]
        },
        "event": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiEvent"
            },
            {
              "type": "null"
            }
          ]
        },
        "extensions": {
          "additionalProperties": true,
          "type": "object"
        },
        "hotReload": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiHotReload"
            },
            {
              "type": "null"
            }
          ]
        },
        "messageId": {
          "type": "string"
        },
        "patchBatch": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiPatchBatch"
            },
            {
              "type": "null"
            }
          ]
        },
        "projection": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiProjectionUpdate"
            },
            {
              "type": "null"
            }
          ]
        },
        "resync": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiResyncRequest"
            },
            {
              "type": "null"
            }
          ]
        },
        "selection": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiCapabilitySelection"
            },
            {
              "type": "null"
            }
          ]
        },
        "snapshot": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiSnapshot"
            },
            {
              "type": "null"
            }
          ]
        },
        "subscription": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiProjectionSubscription"
            },
            {
              "type": "null"
            }
          ]
        },
        "theme": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiTheme"
            },
            {
              "type": "null"
            }
          ]
        },
        "type": {
          "type": "string"
        },
        "unsubscription": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiProjectionUnsubscription"
            },
            {
              "type": "null"
            }
          ]
        },
        "viewport": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiViewport"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "messageId",
        "type"
      ],
      "type": "object"
    },
    "UserAction": {
      "description": "A machine-readable hint about how the user could resolve an error, so the client can render the right affordance instead of parsing `message`.",
      "oneOf": [
        {
          "description": "Retry the same request.",
          "properties": {
            "type": {
              "enum": [
                "Retry"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Re-establish identity or re-authorise.",
          "properties": {
            "type": {
              "enum": [
                "Reauthenticate"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "An approval is required before the action can proceed.",
          "properties": {
            "type": {
              "enum": [
                "GrantApproval"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The active policy must be widened or changed.",
          "properties": {
            "type": {
              "enum": [
                "AdjustPolicy"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The model or provider configuration needs attention.",
          "properties": {
            "type": {
              "enum": [
                "ReconfigureModel"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Nothing automatic; escalate to a human/support.",
          "properties": {
            "type": {
              "enum": [
                "ContactSupport"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "WebhookSignatureScheme": {
      "enum": [
        "hmac_sha256",
        "ed25519",
        "unknown"
      ],
      "type": "string"
    },
    "WorkflowEvent": {
      "description": "One live event on a workflow run's observability stream ([`Subscription::Workflow`](crate::handshake::Subscription::Workflow)), delivered as [`Payload::WorkflowEvent`](crate::envelope::Payload::WorkflowEvent).\n\nA newer peer's variant deserializes to [`Unknown`](WorkflowEvent::Unknown) so an additive event never breaks an older client.",
      "oneOf": [
        {
          "description": "A node changed state — carries the node's **full** new view, so a client merges it by `node_id` (overwrite). Every driver transition (running, completed, failed, blocked) and a cancel's skip lands as one of these.",
          "properties": {
            "attempt": {
              "description": "The 1-based attempt number (0 before the node has ever run).",
              "format": "uint32",
              "minimum": 0.0,
              "type": "integer"
            },
            "cost": {
              "description": "The node's **measured** cost so far (opaque JSON — `wall_time_secs`, `tool_calls`; only measured dimensions, never a fabricated token/USD figure), when a run has recorded one. `None` before the node completes an attempt."
            },
            "depends_on": {
              "description": "The node ids this node depends on — the graph's **edges**, so a client can draw the DAG rather than a flat list (rubric 5). Carried on snapshot reads (the daemon recompiles the run's stored manifest to recover them); a live [`WorkflowEvent::NodeTransitioned`] omits them (the graph shape is static per run), so a client merging live deliveries must preserve the edges it learned from the snapshot rather than overwriting them with an empty list. Additive (`#[serde(default)]`): an older daemon sends none and the field parses back empty.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "error": {
              "description": "The node's latest failure or budget-block reason, when its latest state is `Failed`/`Blocked` (a `Completed` transition clears it). `None` otherwise.",
              "type": [
                "string",
                "null"
              ]
            },
            "node_id": {
              "description": "The node (step) id, unique within its workflow.",
              "type": "string"
            },
            "state": {
              "allOf": [
                {
                  "$ref": "#/definitions/WorkflowNodeState"
                }
              ],
              "description": "The node's lifecycle state."
            },
            "type": {
              "enum": [
                "NodeTransitioned"
              ],
              "type": "string"
            },
            "warnings": {
              "description": "Budget-dimension warnings raised while charging this node (each crossed 80% of a limit but stayed within it), pre-rendered. Empty when none.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "workflow_run_id": {
              "description": "The workflow run this node belongs to.",
              "type": "string"
            }
          },
          "required": [
            "attempt",
            "node_id",
            "state",
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "description": "The run itself changed lifecycle phase (started running, paused, completed, failed, cancelled).",
          "properties": {
            "phase": {
              "$ref": "#/definitions/WorkflowRunPhase"
            },
            "type": {
              "enum": [
                "RunPhaseChanged"
              ],
              "type": "string"
            },
            "workflow_run_id": {
              "type": "string"
            }
          },
          "required": [
            "phase",
            "type",
            "workflow_run_id"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "WorkflowNodeState": {
      "description": "The lifecycle state of one workflow node, projected for a client. Mirrors `codypendent_workflow`'s `NodeState` across the wire; a value from a newer peer deserializes to [`Unknown`](WorkflowNodeState::Unknown) rather than failing the frame.",
      "oneOf": [
        {
          "description": "Not yet scheduled.",
          "properties": {
            "type": {
              "enum": [
                "Pending"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "An attempt is executing.",
          "properties": {
            "type": {
              "enum": [
                "Running"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Parked awaiting an approval.",
          "properties": {
            "type": {
              "enum": [
                "WaitingApproval"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "A budget dimension was exhausted; the run paused for a human decision.",
          "properties": {
            "type": {
              "enum": [
                "Blocked"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The node's work succeeded.",
          "properties": {
            "type": {
              "enum": [
                "Completed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Attempts exhausted; the node failed.",
          "properties": {
            "type": {
              "enum": [
                "Failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "The node will never run — a `CancelWorkflow` skipped a still-pending node (T9), the one no-producer state a cancel newly produces alongside a `Cancelled` run.",
          "properties": {
            "type": {
              "enum": [
                "Skipped"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "WorkflowNodeView": {
      "description": "One workflow node's full current state, projected for a client.\n\nCarried identically in a [`WorkflowRunSnapshot`] and in each live [`WorkflowEvent::NodeTransitioned`], so a client applies either by overwrite-by-`node_id` — an overlap between the snapshot baseline and the live stream is a harmless idempotent re-write. The `workflow_run_id` travels with the view so a client routes a live delivery to the right run without consulting the enclosing frame (the frame is not session-scoped).",
      "properties": {
        "attempt": {
          "description": "The 1-based attempt number (0 before the node has ever run).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "cost": {
          "description": "The node's **measured** cost so far (opaque JSON — `wall_time_secs`, `tool_calls`; only measured dimensions, never a fabricated token/USD figure), when a run has recorded one. `None` before the node completes an attempt."
        },
        "depends_on": {
          "description": "The node ids this node depends on — the graph's **edges**, so a client can draw the DAG rather than a flat list (rubric 5). Carried on snapshot reads (the daemon recompiles the run's stored manifest to recover them); a live [`WorkflowEvent::NodeTransitioned`] omits them (the graph shape is static per run), so a client merging live deliveries must preserve the edges it learned from the snapshot rather than overwriting them with an empty list. Additive (`#[serde(default)]`): an older daemon sends none and the field parses back empty.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "error": {
          "description": "The node's latest failure or budget-block reason, when its latest state is `Failed`/`Blocked` (a `Completed` transition clears it). `None` otherwise.",
          "type": [
            "string",
            "null"
          ]
        },
        "node_id": {
          "description": "The node (step) id, unique within its workflow.",
          "type": "string"
        },
        "state": {
          "allOf": [
            {
              "$ref": "#/definitions/WorkflowNodeState"
            }
          ],
          "description": "The node's lifecycle state."
        },
        "warnings": {
          "description": "Budget-dimension warnings raised while charging this node (each crossed 80% of a limit but stayed within it), pre-rendered. Empty when none.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "workflow_run_id": {
          "description": "The workflow run this node belongs to.",
          "type": "string"
        }
      },
      "required": [
        "attempt",
        "node_id",
        "state",
        "workflow_run_id"
      ],
      "type": "object"
    },
    "WorkflowRunPhase": {
      "description": "The lifecycle state of a workflow **run**, projected for a client. Mirrors `codypendent_workflow`'s `WorkflowRunState`; distinct from the agent-run [`RunState`](crate::run::RunState), which describes a single agent run rather than a durable multi-node workflow.",
      "oneOf": [
        {
          "properties": {
            "type": {
              "enum": [
                "Pending"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Running"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Paused"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Completed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Failed"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Terminal: a `CancelWorkflow` drained the run (T9). No resume from here.",
          "properties": {
            "type": {
              "enum": [
                "Cancelled"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "Unknown"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "WorkflowRunSnapshot": {
      "description": "A workflow run's full observable state — the catch-up baseline a mid-run subscriber reads (via [`ReadWorkflowRun`](crate::command::CommandBody::ReadWorkflowRun)) before folding the live [`WorkflowEvent`] stream on top. Reconstructed from the durable store, so a late subscriber after a daemon restart still gets a truthful baseline.",
      "properties": {
        "nodes": {
          "description": "Every node's full current view, in topological order.",
          "items": {
            "$ref": "#/definitions/WorkflowNodeView"
          },
          "type": "array"
        },
        "phase": {
          "allOf": [
            {
              "$ref": "#/definitions/WorkflowRunPhase"
            }
          ],
          "description": "The run's current lifecycle phase."
        },
        "workflow_run_id": {
          "description": "The run this snapshot is of.",
          "type": "string"
        }
      },
      "required": [
        "nodes",
        "phase",
        "workflow_run_id"
      ],
      "type": "object"
    }
  },
  "oneOf": [
    {
      "description": "Liveness probe.",
      "properties": {
        "type": {
          "enum": [
            "Ping"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "properties": {
        "type": {
          "enum": [
            "Pong"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Ask the daemon to describe itself.",
      "properties": {
        "type": {
          "enum": [
            "DaemonStatusRequest"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "properties": {
        "active_run_count": {
          "default": 0,
          "description": "Count of runs in a non-terminal state (`Queued`, `Preparing`, `Running`, `WaitingForApproval`, `WaitingForUserInput`, `Paused`, `Recovering` — every [`RunState`](crate::RunState) except `Completed`, `Failed`, and `Cancelled`). Used to gate an automatic daemon restart: only an idle daemon (`active_run_count == 0`) is safe to restart without losing in-flight work. Defaulted for wire compatibility with daemons predating this field.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "boot_count": {
          "format": "int64",
          "type": "integer"
        },
        "build_id": {
          "default": "",
          "description": "The running daemon's per-build id ([`codypendent_protocol::BUILD_ID`](crate::BUILD_ID)). Defaulted for wire compatibility with daemons predating this field (mirrors [`ServerHello::build_id`](crate::ServerHello::build_id)).",
          "type": "string"
        },
        "daemon_version": {
          "type": "string"
        },
        "database_path": {
          "type": "string"
        },
        "instance_id": {
          "format": "uuid",
          "type": "string"
        },
        "integration_issues": {
          "description": "Sanitized, de-duplicated optional-integration failures observed by the running daemon. Defaulted so older daemon/client pairs remain wire compatible. Secrets and raw extension output must never enter this list.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "pid": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "protocol_version": {
          "$ref": "#/definitions/ProtocolVersion"
        },
        "session_count": {
          "format": "int64",
          "type": "integer"
        },
        "socket_path": {
          "type": "string"
        },
        "started_at": {
          "format": "date-time",
          "type": "string"
        },
        "type": {
          "enum": [
            "DaemonStatusResponse"
          ],
          "type": "string"
        },
        "uptime_seconds": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "boot_count",
        "daemon_version",
        "database_path",
        "instance_id",
        "pid",
        "protocol_version",
        "session_count",
        "socket_path",
        "started_at",
        "type",
        "uptime_seconds"
      ],
      "type": "object"
    },
    {
      "description": "Ask the daemon to shut down gracefully.",
      "properties": {
        "type": {
          "enum": [
            "Shutdown"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "properties": {
        "type": {
          "enum": [
            "ShutdownAck"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Ask the daemon to shut down ONLY if it is idle (no active runs) — the daemon-side half of the auto-restart safety gate. Unlike [`Payload::Shutdown`] (which stops unconditionally), the daemon re-checks its own `active_run_count` atomically against concurrent run admission and, when any run is active, refuses with [`Payload::ShutdownRefused`] instead of killing in-flight work. Introduced at protocol v1.3; a client only sends it to a daemon whose negotiated minor is ≥ 3.",
      "properties": {
        "type": {
          "enum": [
            "ShutdownIfIdle"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "description": "The daemon declined a [`Payload::ShutdownIfIdle`] because a run is active; carries the count it observed so the client can warn precisely. The daemon keeps running.",
      "properties": {
        "active_run_count": {
          "default": 0,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "ShutdownRefused"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Structured protocol-level error (never parse human text to decide behaviour).",
      "properties": {
        "code": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "retryable": {
          "type": "boolean"
        },
        "type": {
          "enum": [
            "Error"
          ],
          "type": "string"
        }
      },
      "required": [
        "code",
        "message",
        "retryable",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Client's opening handshake message.",
      "properties": {
        "capabilities": {
          "$ref": "#/definitions/ClientCapabilities"
        },
        "client_name": {
          "type": "string"
        },
        "client_version": {
          "type": "string"
        },
        "resume_token": {
          "description": "Present when resuming a prior connection.",
          "type": [
            "string",
            "null"
          ]
        },
        "supported_protocols": {
          "description": "Protocol versions the client can speak, best first.",
          "items": {
            "$ref": "#/definitions/ProtocolVersion"
          },
          "type": "array"
        },
        "type": {
          "enum": [
            "ClientHello"
          ],
          "type": "string"
        }
      },
      "required": [
        "capabilities",
        "client_name",
        "client_version",
        "supported_protocols",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Daemon's handshake reply.",
      "properties": {
        "build_id": {
          "default": "",
          "description": "The running daemon's per-build id ([`codypendent_protocol::BUILD_ID`](crate::BUILD_ID)), used by the client to detect that the connected daemon is a different build than the client binary (a stale in-memory daemon after a reinstall). Defaulted for wire compatibility with daemons predating this field — an empty string is treated by the client as \"unknown build\" (i.e. a mismatch).",
          "type": "string"
        },
        "daemon_instance": {
          "format": "uuid",
          "type": "string"
        },
        "daemon_version": {
          "type": "string"
        },
        "heartbeat_interval_ms": {
          "description": "How often the client should expect (and send) heartbeats.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "resume_token": {
          "description": "A fresh [`ResumeToken`] for this connection's identity. The client stores it opaquely and presents it on its next `ClientHello`, so a reconnect resumes the same client identity even across a client-process restart. Optional and defaulted for wire compatibility with older daemons/clients.",
          "type": [
            "string",
            "null"
          ]
        },
        "selected_protocol": {
          "$ref": "#/definitions/ProtocolVersion"
        },
        "type": {
          "enum": [
            "ServerHello"
          ],
          "type": "string"
        }
      },
      "required": [
        "daemon_instance",
        "daemon_version",
        "heartbeat_interval_ms",
        "selected_protocol",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A client request for a state change (idempotent).",
      "properties": {
        "body": {
          "$ref": "#/definitions/CommandBody"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "expected_revision": {
          "description": "Optimistic-concurrency guard: apply only if the session is still at this revision.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "idempotency_key": {
          "description": "Client-chosen key; the same key must never apply twice.",
          "type": "string"
        },
        "type": {
          "enum": [
            "Command"
          ],
          "type": "string"
        }
      },
      "required": [
        "body",
        "command_id",
        "idempotency_key",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "The command was accepted and applied; carries the resulting ledger sequence when the command produced events.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "created_run": {
          "description": "The run a `StartRun` created, so the issuing client can bind to exactly that run (never a concurrent client's run that happened to start first). Absent on every other command; defaulted for wire compatibility with older daemons.",
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "sequence": {
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "type": {
          "enum": [
            "CommandAccepted"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "The command was rejected; carries the full structured error.",
      "properties": {
        "code": {
          "description": "Stable machine-readable code. Never parse `message` to decide behaviour.",
          "type": "string"
        },
        "correlation_id": {
          "format": "uuid",
          "type": "string"
        },
        "details": {
          "description": "Free-form structured context (offending path, limits, ...). Defaults to JSON `null` when there is nothing to add."
        },
        "message": {
          "description": "Human-readable explanation.",
          "type": "string"
        },
        "retryable": {
          "description": "Whether an identical retry could succeed.",
          "type": "boolean"
        },
        "type": {
          "enum": [
            "CommandRejected"
          ],
          "type": "string"
        },
        "user_action": {
          "anyOf": [
            {
              "$ref": "#/definitions/UserAction"
            },
            {
              "type": "null"
            }
          ],
          "description": "A suggested next step the client can surface as an affordance."
        }
      },
      "required": [
        "code",
        "correlation_id",
        "message",
        "retryable",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "An `AcquireDocumentLease` command was accepted; carries the minted lease id and expiry the client holds to renew and release (Phase 4 STEP 4.3). A distinct reply from `CommandAccepted` because the client needs the granted lease back, not just an acknowledgement.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "grant": {
          "$ref": "#/definitions/DocumentLeaseGrant"
        },
        "type": {
          "enum": [
            "DocumentLeaseGranted"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "grant",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `CreateDocument` command was accepted; carries the new document's id (Docs Studio creation). A distinct reply from `CommandAccepted` for the same reason as `WorkflowRunStarted`: the client needs the id back to select, subscribe to, and edit the document it just created.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "document_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "DocumentCreated"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "document_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `ForkSession`: the freshly created fork (Adoption 05).",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "SessionForked"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "session_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `ListSessions`: sessions known to the daemon (Adoption 11 S1).",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "sessions": {
          "items": {
            "$ref": "#/definitions/SessionSummary"
          },
          "type": "array"
        },
        "type": {
          "enum": [
            "SessionList"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "sessions",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `SearchSessions`.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/SessionSearchPage"
        },
        "type": {
          "enum": [
            "SessionSearchResults"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `ReadSessionHistory`.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/SessionHistoryPage"
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "SessionHistory"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "session_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Authoritative projection after a lifecycle mutation.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "session": {
          "$ref": "#/definitions/SessionSummary"
        },
        "type": {
          "enum": [
            "SessionLifecycleApplied"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "session",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Tombstone receipt after retention-aware deletion.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "tombstoned": {
          "default": false,
          "type": "boolean"
        },
        "type": {
          "enum": [
            "SessionDeleted"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "session_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Artifact created by a lifecycle export action.",
      "properties": {
        "artifact": {
          "$ref": "#/definitions/ArtifactRef"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "SessionExported"
          ],
          "type": "string"
        }
      },
      "required": [
        "artifact",
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "An editor-native action entered the ordinary run pipeline.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "run_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "EditorActionAccepted"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "run_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `ListInbox`.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/InboxPage"
        },
        "type": {
          "enum": [
            "InboxPage"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Authoritative projection after an inbox mutation.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "entry": {
          "$ref": "#/definitions/InboxEntry"
        },
        "type": {
          "enum": [
            "InboxEntryApplied"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "entry",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `QueryAnalytics`.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/AnalyticsPage"
        },
        "type": {
          "enum": [
            "AnalyticsResults"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `ExportAnalytics`.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "result": {
          "$ref": "#/definitions/AnalyticsExportResult"
        },
        "type": {
          "enum": [
            "AnalyticsExported"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "result",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Created, read, or updated automation binding.",
      "properties": {
        "binding": {
          "$ref": "#/definitions/AutomationBinding"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "AutomationBindingResult"
          ],
          "type": "string"
        }
      },
      "required": [
        "binding",
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Cursor page returned by an automation list request.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/AutomationBindingPage"
        },
        "type": {
          "enum": [
            "AutomationBindingPage"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Durable deletion receipt for an automation binding.",
      "properties": {
        "binding_id": {
          "format": "uuid",
          "type": "string"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "AutomationBindingDeleted"
          ],
          "type": "string"
        }
      },
      "required": [
        "binding_id",
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Completed bundle export receipt.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "receipt": {
          "$ref": "#/definitions/BundleExportReceipt"
        },
        "type": {
          "enum": [
            "BundleExported"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "receipt",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Completed bundle import receipt.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "receipt": {
          "$ref": "#/definitions/BundleImportReceipt"
        },
        "type": {
          "enum": [
            "BundleImported"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "receipt",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Reply to `SearchWorkspaceFiles`: fuzzy matching file paths (Adoption 11 M2).",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "matches": {
          "items": {
            "$ref": "#/definitions/FileMatchWire"
          },
          "type": "array"
        },
        "query": {
          "type": "string"
        },
        "truncated": {
          "default": false,
          "type": "boolean"
        },
        "type": {
          "enum": [
            "FileSearchResults"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "matches",
        "query",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `CheckDocuments` command's reply: the staleness sweep's counts (`/update-docs` glue over the Phase 4 STEP 4.6 engine). A distinct reply from `CommandAccepted` because the client reports the counts to the operator, not just an acknowledgement.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "documents_checked": {
          "description": "Documents the sweep examined.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "links_resolved": {
          "description": "Symbol links resolved (and persisted) against the code graph.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "stale_findings": {
          "description": "Staleness findings (signature changed / symbol disappeared).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "suggestions_filed": {
          "description": "Maintain-mode suggestions filed from those findings (never direct edits; each still needs a human accept).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "DocsCheckCompleted"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "documents_checked",
        "links_resolved",
        "stale_findings",
        "suggestions_filed",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `StartWorkflow` command was accepted; carries the new durable workflow-run id (Phase 5 STEP 5.2). A distinct reply from `CommandAccepted` because the client needs the run id back to track / show the run it just started.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "WorkflowRunStarted"
          ],
          "type": "string"
        },
        "workflow_run_id": {
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "type",
        "workflow_run_id"
      ],
      "type": "object"
    },
    {
      "description": "A `PublishDocument` command was accepted: its deterministic plan was computed and a durable approval parked (Phase 4 STEP 4.4). Carries the plan's human-reviewable content **verbatim** — a short target description, the changed files, and the resulting Git action — so the client can render it immediately; the same content the parked `ApprovalRequested` event's approval card carries. Nothing is written yet: the approval must still resolve via the ordinary `ResolveApproval` command, and a rejection performs no write.",
      "properties": {
        "approval_id": {
          "format": "uuid",
          "type": "string"
        },
        "changed_files": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "git_action": {
          "type": "string"
        },
        "target": {
          "type": "string"
        },
        "type": {
          "enum": [
            "DocumentPublishRequested"
          ],
          "type": "string"
        }
      },
      "required": [
        "approval_id",
        "changed_files",
        "command_id",
        "git_action",
        "target",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ProposePromotion` command was accepted; carries the new promotion candidate's id (Phase 7 STEP 7.5). A distinct reply from `CommandAccepted` for the same reason as `WorkflowRunStarted`: the client needs the id back to advance/approve/roll back that exact candidate.",
      "properties": {
        "candidate_id": {
          "type": "string"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "PromotionProposed"
          ],
          "type": "string"
        }
      },
      "required": [
        "candidate_id",
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Result of a daemon-owned Remote UI plugin lifecycle command.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "plugins": {
          "items": {
            "$ref": "#/definitions/UiPluginLifecycleStatus"
          },
          "type": "array"
        },
        "type": {
          "enum": [
            "UiPluginLifecycle"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "plugins",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `PutArtifact` command's reply (voice v1, rubric 8): the freshly minted [`ArtifactRef`](crate::artifact::ArtifactRef) for the stored bytes. A distinct reply from `CommandAccepted` because the client needs the ref back — it is what an [`InputEnvelope`](crate::input::InputEnvelope) audio block references on the next `SubmitUserInput`.",
      "properties": {
        "artifact": {
          "$ref": "#/definitions/ArtifactRef"
        },
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "type": {
          "enum": [
            "ArtifactStored"
          ],
          "type": "string"
        }
      },
      "required": [
        "artifact",
        "command_id",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Bounded bytes returned for `ReadArtifact`. `offset` is the requested starting position; `eof` tells the caller whether another request is needed. The stored digest is repeated for end-to-end verification.",
      "properties": {
        "artifact_id": {
          "format": "uuid",
          "type": "string"
        },
        "bytes_base64": {
          "type": "string"
        },
        "eof": {
          "type": "boolean"
        },
        "offset": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "sha256": {
          "type": "string"
        },
        "type": {
          "enum": [
            "ArtifactChunk"
          ],
          "type": "string"
        }
      },
      "required": [
        "artifact_id",
        "bytes_base64",
        "eof",
        "offset",
        "sha256",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "An `InspectMemory` / `CorrectMemory` reply: the memory as it stands *now*. For a correction that is the NEW record (the correction supersedes rather than overwrites), so a client re-renders from the reply instead of guessing what the store did.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "memory": {
          "$ref": "#/definitions/MemoryView"
        },
        "type": {
          "enum": [
            "Memory"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "memory",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ForgetMemory` / `ForgetMemoryScope` reply: a content-free audit of what was removed. Ids only — the statements are gone, and echoing them back would defeat the point of forgetting them.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "forgotten": {
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "type": "array"
        },
        "type": {
          "enum": [
            "MemoryForgotten"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "forgotten",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "An `OpenMemoryEvidence` reply: the actual content behind one of a memory's provenance refs (Chapter 06's \"every retrieved memory opens its source\").",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "evidence": {
          "$ref": "#/definitions/MemoryEvidence"
        },
        "type": {
          "enum": [
            "MemoryEvidence"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "evidence",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ReadBlackboard` command's reply (Phase 5 STEP 5.3): the matching typed artifacts on the workflow run's board. A distinct reply from `CommandAccepted` because the client needs the items back, not just an acknowledgement.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "items": {
          "items": {
            "$ref": "#/definitions/BlackboardItemView"
          },
          "type": "array"
        },
        "type": {
          "enum": [
            "BlackboardItems"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "items",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "One blackboard artifact that just landed on a run's board, delivered to the clients subscribed to it (`Subscription::Blackboard`) as the run's agents post/supersede (Phase 5 STEP 5.3). The item carries its own `workflow_run_id`, so — like [`DocumentSync`](Payload::DocumentSync) — the frame is not session-scoped; a receiver merges it into the run's board by id (a superseding revision arrives as its own delivery).",
      "properties": {
        "assignee": {
          "description": "Who the card is assigned to, if anyone.",
          "type": [
            "string",
            "null"
          ]
        },
        "author": {
          "description": "Who produced it — the daemon builds this server-side from the authoring node's run context (`{role, run_id, node_id, workflow_run_id}`), never from model-supplied identity."
        },
        "board_scope": {
          "description": "The repository this item's board serves, when the item lives on a repository task board rather than a real workflow run (its `workflow_run_id` is then the synthetic [`board_scope_id`]). Additive: an older daemon sends none and every field below parses back defaulted.",
          "type": [
            "string",
            "null"
          ]
        },
        "confidence": {
          "description": "The author's self-reported confidence in `[0, 1]`, if given.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "evidence": {
          "description": "Evidence references grounding the artifact (opaque JSON). Claim-like kinds require at least one; the store enforces it.",
          "items": true,
          "type": "array"
        },
        "id": {
          "description": "The artifact's stable id (a UUIDv7 string).",
          "type": "string"
        },
        "kind": {
          "description": "The typed artifact kind (`finding`, `decision`, `hypothesis`, …), as the manifest-facing string the `BlackboardStore` records.",
          "type": "string"
        },
        "ordinal": {
          "description": "The card's position within its column (lower sorts first).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "payload": {
          "description": "The artifact body (opaque JSON — a client renders it)."
        },
        "revision": {
          "description": "The artifact's revision within its supersession chain (1 for an original).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "status": {
          "description": "The board column (`todo` / `doing` / `review` / `done`, or a validated free string), when this item is a board card.",
          "type": [
            "string",
            "null"
          ]
        },
        "superseded_by": {
          "description": "The id of the item that superseded this one, if any — a live item has `None`.",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "enum": [
            "BlackboardPosted"
          ],
          "type": "string"
        },
        "workflow_run_id": {
          "description": "The workflow run whose board holds it.",
          "type": "string"
        }
      },
      "required": [
        "author",
        "id",
        "kind",
        "payload",
        "revision",
        "type",
        "workflow_run_id"
      ],
      "type": "object"
    },
    {
      "description": "A `PostBlackboardItem` / `UpdateBlackboardItem` command's reply (Phase B kanban): the stored (or superseding) item. A distinct reply from `CommandAccepted` because the writing client needs the minted item id and revision back — e.g. to select the card it just created.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "item": {
          "$ref": "#/definitions/BlackboardItemView"
        },
        "type": {
          "enum": [
            "BlackboardItemApplied"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "item",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ReadSessionEvents` command's reply: one ascending page of the session's durable event history. `through` is the highest sequence in the page (equal to the request's `after_sequence` when the page is empty) — the client passes it back as the next `after_sequence`; `has_more` says whether events beyond `through` existed at read time, so a pager knows when to stop without a probe read.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "events": {
          "items": {
            "$ref": "#/definitions/SessionEvent"
          },
          "type": "array"
        },
        "has_more": {
          "type": "boolean"
        },
        "session_id": {
          "format": "uuid",
          "type": "string"
        },
        "through": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "SessionEventsPage"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "events",
        "has_more",
        "session_id",
        "through",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ReadWorkflowRun` command's reply (Phase 5 STEP 5.2 / T9): the run's observability snapshot — its current phase plus every node's full current view. A distinct reply from `CommandAccepted` because the client needs the snapshot back as its live-stream baseline, not just an acknowledgement.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "snapshot": {
          "$ref": "#/definitions/WorkflowRunSnapshot"
        },
        "type": {
          "enum": [
            "WorkflowRunSnapshot"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "snapshot",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "One live event on a workflow run's node-lifecycle stream, delivered to the clients subscribed to it (`Subscription::Workflow`) as the driver advances the graph (Phase 5 STEP 5.2 / T9): a node transition (carrying the node's full new view) or a run-phase change. The event carries its own `workflow_run_id`, so the frame is not session-scoped; a receiver merges a node transition into the run's graph by `node_id` (each transition is full-state, so an overlap is harmless). Wrapped in a named field so the internally-tagged event's `type` tag never collides with the payload tag (as [`Catchup`](Payload::Catchup) is).",
      "properties": {
        "event": {
          "$ref": "#/definitions/WorkflowEvent"
        },
        "type": {
          "enum": [
            "WorkflowEvent"
          ],
          "type": "string"
        }
      },
      "required": [
        "event",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A persisted session event published to a subscribed client.",
      "properties": {
        "actor": {
          "$ref": "#/definitions/Actor"
        },
        "body": {
          "$ref": "#/definitions/EventBody"
        },
        "causation_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "correlation_id": {
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "occurred_at": {
          "format": "date-time",
          "type": "string"
        },
        "sequence": {
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "Event"
          ],
          "type": "string"
        }
      },
      "required": [
        "actor",
        "body",
        "occurred_at",
        "sequence",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A collaborative document's CRDT sync update, delivered to the clients subscribed to that document (`Subscription::Document`) as the authoritative replica advances (Phase 4 STEP 4.3). Opaque CRDT bytes ride in [`DocumentSync::update`]; a receiver merges them into its local replica.",
      "properties": {
        "document_id": {
          "format": "uuid",
          "type": "string"
        },
        "revision": {
          "description": "The document revision this update advances to (for ordering/UX).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "DocumentSync"
          ],
          "type": "string"
        },
        "update": {
          "description": "Opaque CRDT bytes. Serialized on the wire as a JSON array of byte values (see the [`byte_vec`] module) — the framing layer emits plain `serde_json`, so there is no base64 step; a client sends the raw bytes and they round-trip as numbers. Every sync currently carries a **full CRDT snapshot** (`ExportMode::Snapshot`), not an incremental delta — the client replica relies on this (an empty or lagged replica converges on the next sync, and re-importing a snapshot is an idempotent no-op). If a future change sends deltas to stay under the frame-size bound, the client replica (`knowledge::docs::DocumentReplica`) must be updated in lockstep, or a replica missing a delta's causal dependencies would silently fail to converge.",
          "items": {
            "$ref": "#/definitions/Byte"
          },
          "type": "array"
        }
      },
      "required": [
        "document_id",
        "revision",
        "type",
        "update"
      ],
      "type": "object"
    },
    {
      "description": "A `BuildCodeGraph` reply: what the on-demand fold actually saw. A distinct reply from `CommandAccepted` because the report *is* the feature — a build that only acknowledged itself would leave an empty graph exactly as unexplained as it was before the command existed.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "report": {
          "$ref": "#/definitions/CodeGraphScanReport"
        },
        "type": {
          "enum": [
            "CodeGraphBuilt"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "report",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ReadCodeGraphStatus` reply: what the stored graph holds right now, and whether it still describes the working tree.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "status": {
          "$ref": "#/definitions/CodeGraphStatusView"
        },
        "type": {
          "enum": [
            "CodeGraphStatus"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "status",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "A `ReadCodeGraph` reply: one filtered page of nodes and edges.",
      "properties": {
        "command_id": {
          "format": "uuid",
          "type": "string"
        },
        "page": {
          "$ref": "#/definitions/CodeGraphPage"
        },
        "type": {
          "enum": [
            "CodeGraphPage"
          ],
          "type": "string"
        }
      },
      "required": [
        "command_id",
        "page",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Attach-time catch-up (missed events or a snapshot). Wrapped in a named field so its internal `type` tag never collides with the payload tag.",
      "properties": {
        "catchup": {
          "$ref": "#/definitions/Catchup"
        },
        "type": {
          "enum": [
            "Catchup"
          ],
          "type": "string"
        }
      },
      "required": [
        "catchup",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Renderer-independent component traffic. The nested message has its own kind and revision semantics; keeping it inside one envelope variant lets terminal, VS Code, and future graphical clients share the ordinary authenticated connection without granting component code direct access to the daemon command channel.",
      "properties": {
        "message": {
          "$ref": "#/definitions/UiWireMessage"
        },
        "type": {
          "enum": [
            "RemoteUi"
          ],
          "type": "string"
        }
      },
      "required": [
        "message",
        "type"
      ],
      "type": "object"
    },
    {
      "description": "Forward-compatibility fallback: a payload tag this build does not know deserializes to `Unknown` instead of failing the whole frame, so the receiver can reject it structurally and keep the connection alive (additive 1.x payloads must never break an older peer).",
      "properties": {
        "type": {
          "enum": [
            "Unknown"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    }
  ],
  "title": "Payload"
}
